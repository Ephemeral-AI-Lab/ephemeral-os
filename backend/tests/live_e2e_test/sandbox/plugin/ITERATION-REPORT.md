# Plugin Live E2E Iteration Report

## Iteration 39 - 2026-06-02 07:45 CST

- Checkout/target summary: continued the Phase 3T Rust plugin implementation after the repeated-callback slice; focused on generic health-probe failure isolation for daemon-managed `workspace_snapshot_refresh` services.
- Plan path and target files: `docs/plans/sandbox-plugin-service-adversarial-plan.md`; `docs/plans/sandbox-rust-external-migration-PROGRESS.md`; `backend/scripts/bench_rust_daemon_plugin.py`; `backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Coverage gap found: status health probing proved healthy services and route cleanup in focused daemon tests, but the live generic plugin suite did not yet prove that one arbitrary service rejecting a daemon health frame is isolated from unrelated plugin services.
- Tests, probes, and audit fields added: Added live `health_fail_harness` with `refresh_strategy="remount_workspace"` plus `plugin.generic.health_fail_ping`; the harness rejects `daemon.workspace_snapshot_refresh` `Health { manifest_key }` with `intentional health failure`, and the gate asserts route removal, `state=stopped`, and unrelated connected routes staying present.
- Exact commands run:
  - `python3 -m py_compile backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run ruff check backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run pytest --collect-only -q backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `cargo fmt --check`.
  - `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: `.omc/results/rust-daemon-plugin-generic-20260601T234314Z-87525.json`, `.omc/results/rust-daemon-plugin-generic-20260601T234314Z-87525.md`, `.omc/results/plugin-refresh-strategies-20260601T234314Z-87525.json`, `.omc/results/plugin-refresh-strategies-20260601T234314Z-87525.md`, plus pytest output.
- Pass/fail/skip status: passed. The focused live test finished `1 passed in 45.19s`; packaged `eosd-linux-amd64` SHA was `79f2592a55e565f00e2917c86cbeca2bfa26c073c316a1be6b6239d5830509fb`; py_compile, ruff, collect-only, and cargo fmt check passed.
- Findings summary: The live Rust plugin case now proves health-failure isolation for an arbitrary long-lived read-only service. `plugin.generic.health_fail_ping` was connected after ensure, `health_fail_harness` rejected the daemon `Health { manifest_key }` probe with `intentional health failure`, status removed only `plugin.generic.health_fail_ping` from connected routes, marked `health_fail_harness.state == "stopped"`, and left unrelated services connected and healthy.
- Issues found and fixes applied: No daemon runtime failure. Added a failing-health live harness route, benchmark gate checks, markdown artifact fields, pytest assertions, and plan/progress evidence updates.
- Correctness result: PASS. A bad service cannot poison the shared plugin service registry or other connected services during daemon-owned health probing.
- Performance/O(1) result: PASS for this slice. The refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `6.064 ms` versus `commit_to_workspace` p95 `3.704 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed. The Rust plugin report showed final active leases `5` before cleanup, post-cleanup active leases `0`, and final/post-cleanup orphan and missing layer counts all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond the current representative Pyright set, true operation-level out-of-order multiplexing if future protocols require concurrent in-flight plugin operations on one stream, and broader crash recovery beyond the covered health probe, failed-health isolation, closed PPC stream, PPC timeout, and next-dispatch restart paths.

## Iteration 38 - 2026-06-02 07:35 CST

- Checkout/target summary: continued the Phase 3T Rust plugin implementation in the dirty multi-agent worktree; touched the daemon PPC router, live Rust plugin benchmark, pytest wrapper, and plugin progress docs only.
- Plan path and target files: `docs/plans/sandbox-plugin-service-adversarial-plan.md`; `sandbox/crates/eos-daemon/src/plugin/ppc_router.rs`, `backend/scripts/bench_rust_daemon_plugin.py`, `backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`, `docs/plans/sandbox-rust-external-migration-PROGRESS.md`.
- Coverage gap found: PPC callback-frame coverage proved one plugin-originated callback before a final reply, but did not prove repeated daemon-managed callbacks on the same plugin request. This left the self-managed write path under-specified for arbitrary services that split a write operation into multiple daemon callback publishes.
- Tests, probes, and audit fields added: Added `ppc_client_services_multiple_callbacks_before_final_reply`; added live `plugin.generic.apply_multi` with two `daemon.occ.apply_changeset` callbacks; asserted connected route, `callback_count == 2`, per-callback success, changed paths, and LayerStack readbacks for `live_plugin_multi_a.txt` and `live_plugin_multi_b.txt`.
- Exact commands run:
  - `python3 -m py_compile backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `cargo test -p eos-daemon ppc_client_services_multiple_callbacks_before_final_reply -- --test-threads=1`.
  - `uv run ruff check backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run pytest --collect-only -q backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `cargo fmt --check`.
  - `cargo run -p xtask -- package --target x86_64-unknown-linux-musl --out-dir dist --builder rust-lld`.
  - `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: `.omc/results/rust-daemon-plugin-generic-20260601T233450Z-72557.json`, `.omc/results/rust-daemon-plugin-generic-20260601T233450Z-72557.md`, `.omc/results/plugin-refresh-strategies-20260601T233450Z-72557.json`, `.omc/results/plugin-refresh-strategies-20260601T233450Z-72557.md`, plus pytest and packaging output.
- Pass/fail/skip status: passed. The focused live test finished `1 passed in 40.33s`; packaged `eosd-linux-amd64` SHA was `79f2592a55e565f00e2917c86cbeca2bfa26c073c316a1be6b6239d5830509fb`; py_compile, ruff, collect-only, cargo fmt check, and the focused PPC unit test passed.
- Findings summary: The generic Rust plugin benchmark now proves a single connected self-managed plugin request can issue more than one daemon-owned OCC callback before sending its final reply. `plugin.generic.apply_multi` was connected at ensure time, reported `callback_count=2`, committed `live_plugin_multi_a.txt` at manifest version `3`, committed `live_plugin_multi_b.txt` at manifest version `4`, and read back both files from LayerStack with the expected content. Existing refresh, package-adapter, Pyright LSP, self-managed rename, restart, crash, timeout, recovery, one-shot overlay, and cleanup gates stayed green.
- Issues found and fixes applied: No runtime failure. Added a repeated-callback PPC unit test, factored the live harness OCC callback sender, added `plugin.generic.apply_multi` to the manifest and benchmark sequence, extended the benchmark gate/markdown artifact, and added pytest assertions for the connected route, callback count, callback success, changed paths, and readbacks.
- Correctness result: PASS. Repeated plugin-originated callback frames are serviced on the same PPC stream before the final operation reply, and each callback routes through the daemon-owned OCC writer with LayerStack readback proof.
- Performance/O(1) result: PASS for this slice. The refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `3.867 ms` versus `commit_to_workspace` p95 `3.276 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed. The Rust plugin report showed retained service leases `5` before cleanup, post-cleanup active leases `0`, final/post-cleanup orphan and missing layer counts all `0`, and direct readback resource workspace/run/upperdir tree fields stayed `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond the current representative Pyright set, true operation-level out-of-order multiplexing if future protocols require concurrent in-flight plugin operations on one stream, and broader crash recovery beyond the covered health probe, closed PPC stream, PPC timeout, and next-dispatch restart paths.

## Iteration 37 - 2026-06-02 07:18 CST

- Exact commands run:
  - Failed diagnostics-backed code-action try: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `python3 -m py_compile backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run ruff check backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run pytest --collect-only -q backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - Passing live rerun: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `cargo test -p eos-daemon read_only_service_recovers_on_next_dispatch_after_ppc_failure -- --test-threads=1`.
- Artifact paths inspected: failed Rust plugin artifact `.omc/results/rust-daemon-plugin-generic-20260601T230846Z-27251.json`, passing refresh artifacts `.omc/results/plugin-refresh-strategies-20260601T231710Z-43320.json` and `.omc/results/plugin-refresh-strategies-20260601T231710Z-43320.md`, passing Rust plugin artifacts `.omc/results/rust-daemon-plugin-generic-20260601T231710Z-43320.json` and `.omc/results/rust-daemon-plugin-generic-20260601T231710Z-43320.md`, plus pytest output.
- Pass/fail/skip status: first live try failed because Pyright published the expected `"List" is not defined` diagnostic but returned `action_count=0` for `textDocument/codeAction`; the passing rerun finished `1 passed in 44.37s`. Python py_compile, ruff, collect-only, and the focused daemon recovery test also passed.
- Findings summary: The live Rust plugin case now proves a Pyright `textDocument/publishDiagnostics` notification over the same daemon-managed refreshed read-only service. `plugin.generic.pyright_diagnostics` was connected at ensure time, reused `pyright_harness`, reported `workspace_mounted=true`, returned `protocol="lsp-jsonrpc"`, consumed the diagnostic for `live_plugin_diagnostics.py` at line `0`, character `9`, and normalized `diagnostic_codes=["reportUndefinedVariable"]` plus message `"List" is not defined`. Pyright still advertises `codeActionProvider`, but two deterministic live seeds have returned zero code actions in this environment; the plan should not claim code-action behavior until a real non-empty Pyright action is observed.
- Issues found and fixes applied: Replaced the non-load-bearing `pyright_code_action` gate with `pyright_diagnostics`, keeping the same undefined `List` source seed but gating on the reliable asynchronous publish-diagnostics notification. Updated the benchmark manifest, PPC handler, report gate, markdown output, and pytest assertions to require connected routing, mounted workspace evidence, exact file/position, non-empty diagnostics, the `reportUndefinedVariable` code, and a message containing `List`.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and now covers document symbols, workspace symbols, completion, completion item resolve, diagnostics notifications, signature help, hover, type definition, declaration, call hierarchy, document highlight, prepare rename, definition, references, and self-managed rename/OCC publish.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `5.625 ms` versus `commit_to_workspace` p95 `3.307 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed final active leases `5` before cleanup, post-cleanup active leases `0`, and final/post-cleanup orphan and missing layer counts all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `workspace/symbol` + `completion` + `completionItem/resolve` + `publishDiagnostics` + `signatureHelp` + `hover` + `typeDefinition` + `declaration` + `callHierarchy` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and broader crash recovery beyond the now-covered health probe, closed PPC stream, PPC timeout, and next-dispatch restart paths.

## Iteration 36 - 2026-06-02 06:58 CST

- Exact commands run:
  - `python3 -m py_compile backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run ruff check backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run pytest --collect-only -q backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `cargo test -p eos-daemon read_only_service_recovers_on_next_dispatch_after_ppc_failure -- --test-threads=1`.
  - `cargo fmt --check` (failed on formatting only), then `cargo fmt`, then `cargo fmt --check`.
  - `cargo test -p eos-plugin`.
  - `cargo test -p eos-daemon plugin -- --test-threads=1`.
  - `cargo clippy -p eos-daemon --lib --no-deps -- -D warnings`.
  - `cargo run -p xtask -- package --target x86_64-unknown-linux-musl --out-dir dist --builder rust-lld`.
  - `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T225700Z-11297.json`, `.omc/results/plugin-refresh-strategies-20260601T225700Z-11297.md`, `.omc/results/rust-daemon-plugin-generic-20260601T225700Z-11297.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T225700Z-11297.md`.
- Pass/fail/skip status: passed. Focused live test finished `1 passed in 42.65s`; Rust package SHA was `bd3ddef16b98238666012084283cf7c0dcbe1a5653b66a4f5d387469828034e3`. Python py_compile, ruff, collect-only, `eos-plugin`, `eos-daemon plugin`, focused recovery test, format check after formatting, and daemon clippy all passed.
- Findings summary: The live Rust plugin case now proves next-dispatch recovery for an arbitrary read-only `workspace_snapshot_refresh` service. A new `recover_harness` acknowledged status health on the retained daemon snapshot, then `plugin.generic.recover_probe` intentionally exited once and failed closed with `ppc channel error: plugin PPC stream closed before reply`. Status after that failure removed `plugin.generic.recover_probe` from connected routes and marked only `recover_harness.state == "stopped"`. The next `plugin.generic.recover_probe` dispatch restarted the previously ready service, returned `from_recovered_service=true` with `workspace_mounted=true`, restored the connected route, and reported `recover_harness.restart_count == 1`.
- Issues found and fixes applied: Added daemon recovery logic that restarts a previously ready read-only `workspace_snapshot_refresh` service when a later dispatch finds no connected PPC client after a fail-closed teardown. Added a focused daemon unit test, a live `recover_harness`/`recover_probe` route, a one-shot marker under `/eos/plugin/recover_probe_once.flag`, live JSON/Markdown evidence, and pytest assertions for first-failure route removal plus second-dispatch recovery.
- Correctness result: PASS. Generic read-only services now cover healthy status probing, stale remount refresh, stale restart fallback, crash fail-closed, timeout fail-closed, and next-dispatch recovery after a PPC/process failure.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `3.830 ms` versus `commit_to_workspace` p95 `3.341 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=5`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `workspace/symbol` + `completion` + `completionItem/resolve` + `signatureHelp` + `hover` + `typeDefinition` + `declaration` + `callHierarchy` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and broader crash recovery beyond the now-covered health probe, closed PPC stream, PPC timeout, and next-dispatch restart paths.

## Iteration 35 - 2026-06-02 06:41 CST

- Exact commands run:
  - `cargo test -p eos-daemon status_probe -- --test-threads=1`.
  - `.venv/bin/python -m py_compile backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `cargo test -p eos-plugin`.
  - `cargo test -p eos-daemon plugin -- --test-threads=1`.
  - `uv run ruff check backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run pytest --collect-only -q backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - Failed stale-artifact try: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `cargo run -p xtask -- package --target x86_64-unknown-linux-musl --out-dir dist --builder rust-lld`.
  - Passing rerun: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: failed stale-binary artifact `.omc/results/rust-daemon-plugin-generic-20260601T223822Z-78270.json`, passing refresh artifacts `.omc/results/plugin-refresh-strategies-20260601T224020Z-81844.json` and `.omc/results/plugin-refresh-strategies-20260601T224020Z-81844.md`, passing Rust plugin artifacts `.omc/results/rust-daemon-plugin-generic-20260601T224020Z-81844.json` and `.omc/results/rust-daemon-plugin-generic-20260601T224020Z-81844.md`, plus pytest output.
- Pass/fail/skip status: first live try failed because the packaged `sandbox/dist/eosd-linux-amd64` artifact was stale and did not expose the new `service_health` status field; rebuilt package SHA `899db0237279e32c5a5fdcf4b510ddb565c95eacfffad2e8e670ca4e6c60ce54`; passing live rerun finished `1 passed in 40.16s`. Python py_compile, ruff, collect-only, focused `eos-plugin`, focused `eos-daemon plugin`, and the narrower `status_probe` test run also passed.
- Findings summary: The live Rust plugin case now proves generic on-demand service health probing through `api.plugin.status probe_services=true`. The daemon sends `daemon.workspace_snapshot_refresh` `Health { manifest_key }` to connected services with retained daemon snapshots and the passing artifact reported `success=true` acknowledgements for `harness`, `restart_harness`, `adapter_harness`, `pyright_harness`, `crash_harness`, and `hang_harness`, all on manifest key `1:f071e2d096b352b67daeb0f2e2f6dc503335246a98e209fa9f199d06314b5cb5`. Existing generic PPC/OCC/refresh/remount-read/package-adapter/Pyright-read/Pyright-rename/restart/crash/timeout/mounted-workspace/oneshot-overlay checks stayed green.
- Issues found and fixes applied: Added opt-in daemon status probing that reaps dead service processes before building `loaded_plugins`, collects connected services with retained snapshots, sends the generic health frame without holding the daemon registry lock, reports per-service `service_health`, and tears down only a service that fails the health acknowledgement. Added focused Rust tests for successful health probing and failed-health route/snapshot cleanup. The live harness and pytest wrapper now require all six long-lived services to acknowledge health. The first live failure exposed the stale packaged daemon artifact; rebuilding `eosd-linux-amd64` fixed that.
- Correctness result: PASS. Generic long-lived plugin services can now be explicitly probed through the same daemon-managed refresh protocol used for stale snapshot refresh, and failed health acknowledgements are fail-closed instead of leaving a connected route that may answer later.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `4.159 ms` versus `commit_to_workspace` p95 `3.290 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=4`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `workspace/symbol` + `completion` + `completionItem/resolve` + `signatureHelp` + `hover` + `typeDefinition` + `declaration` + `callHierarchy` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and broader crash recovery beyond the covered status health probe, closed PPC stream, and PPC timeout paths.

## Iteration 34 - 2026-06-02 06:22 CST

- Exact commands run:
  - `.venv/bin/python -m py_compile backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run ruff check backend/scripts/bench_rust_daemon_plugin.py backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `uv run pytest --collect-only -q backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T222224Z-50381.json`, `.omc/results/plugin-refresh-strategies-20260601T222224Z-50381.md`, `.omc/results/rust-daemon-plugin-generic-20260601T222224Z-50381.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T222224Z-50381.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 41.85s`; py_compile, ruff, and collect-only also passed.
- Findings summary: The live Rust plugin case now proves Pyright `completionItem/resolve` on the same daemon-managed refreshed read-only service. `plugin.generic.pyright_completion_resolve` was connected at ensure time, reused `pyright_harness`, reported `completion_resolve=true`, returned `protocol="lsp-jsonrpc"`, preserved Pyright completion item data, and resolved the `live_value` completion from `live_plugin_completion.py` line `3`, character `14` with `request_label == resolved_label == "live_value"` and documentation text `def live_value() -> int`. Existing Pyright document-symbol, workspace-symbol, completion, signature-help, hover, type-definition, declaration, call-hierarchy, document-highlight, prepare-rename, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green.
- Issues found and fixes applied: No runtime failure. Added a Pyright adapter `completionItem/resolve` method that keeps raw completion items for the resolve request, declared `plugin.generic.pyright_completion_resolve`, recorded `completionProvider.resolveProvider` in the capability snapshot, and extended the benchmark plus pytest gates to require connected routing, mounted workspace evidence, exact file/position, request/resolved label equality, and resolve support.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer document, workspace, completion, completion-item resolve, signature, hover, type-definition, declaration, call-hierarchy, document-highlight, prepare-rename, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `4.520 ms` versus `commit_to_workspace` p95 `3.216 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=4`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `workspace/symbol` + `completion` + `completionItem/resolve` + `signatureHelp` + `hover` + `typeDefinition` + `declaration` + `callHierarchy` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 33 - 2026-06-02 06:09 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T220928Z-27142.json`, `.omc/results/plugin-refresh-strategies-20260601T220928Z-27142.md`, `.omc/results/rust-daemon-plugin-generic-20260601T220928Z-27142.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T220928Z-27142.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 41.76s`.
- Findings summary: The live Rust plugin case now proves Pyright `textDocument/declaration` on the same daemon-managed refreshed read-only service. `plugin.generic.pyright_declaration` was connected at ensure time, reused `pyright_harness`, reported `declaration=true`, returned `protocol="lsp-jsonrpc"`, and resolved the call in `live_plugin_pyright.py` at line `3`, character `12` back to the function declaration range starting at line `0`, character `4`. Existing Pyright document-symbol, workspace-symbol, completion, signature-help, hover, type-definition, call-hierarchy, document-highlight, prepare-rename, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green.
- Issues found and fixes applied: No runtime failure. Added a Pyright adapter `textDocument/declaration` method, declared `plugin.generic.pyright_declaration`, recorded `declarationProvider` in the capability snapshot, and extended the benchmark plus pytest gates to require connected routing, mounted workspace evidence, exact file/position, `declaration_count >= 1`, and an in-workspace location for `live_plugin_pyright.py`.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer document, workspace, completion, signature, hover, type-definition, declaration, call-hierarchy, document-highlight, prepare-rename, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `5.587 ms` versus `commit_to_workspace` p95 `3.553 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=4`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `workspace/symbol` + `completion` + `signatureHelp` + `hover` + `typeDefinition` + `declaration` + `callHierarchy` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 32 - 2026-06-02 05:59 CST

- Exact commands run:
  - Failed `foldingRange` try: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - Failed `codeAction` try: same command.
  - Corrected `callHierarchy` rerun: same command.
- Artifact paths inspected: failed folding artifact `.omc/results/rust-daemon-plugin-generic-20260601T214828Z-97425.json`, failed code-action artifact `.omc/results/rust-daemon-plugin-generic-20260601T215311Z-2585.json`, passing refresh artifacts `.omc/results/plugin-refresh-strategies-20260601T215929Z-11121.json` and `.omc/results/plugin-refresh-strategies-20260601T215929Z-11121.md`, passing Rust plugin artifacts `.omc/results/rust-daemon-plugin-generic-20260601T215929Z-11121.json` and `.omc/results/rust-daemon-plugin-generic-20260601T215929Z-11121.md`, plus pytest output.
- Pass/fail/skip status: first try failed because Pyright returned `-32601` for `textDocument/foldingRange`; second try failed because `textDocument/codeAction` was advertised but returned `action_count=0` for the deterministic organize-imports seed; corrected rerun passed with focused plugin live test `1 passed in 40.73s`.
- Findings summary: The live Rust plugin case now proves Pyright call hierarchy on the same daemon-managed refreshed read-only service. `plugin.generic.pyright_call_hierarchy` was connected at ensure time, reused `pyright_harness`, reported `call_hierarchy=true`, returned `protocol="lsp-jsonrpc"`, prepared `live_callee` in `live_plugin_call_hierarchy.py` at line `0`, character `11`, and returned `incoming_names=["live_caller"]`. Existing Pyright document-symbol, workspace-symbol, completion, signature-help, hover, type-definition, document-highlight, prepare-rename, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green.
- Issues found and fixes applied: Removed the non-deterministic route attempts from the gate and added a stable Pyright adapter `textDocument/prepareCallHierarchy` plus `callHierarchy/incomingCalls` method. The harness now normalizes call-hierarchy item URIs back to workspace-relative paths with an escape guard, seeds a callee/caller file, declares `plugin.generic.pyright_call_hierarchy`, and gates on connected routing, mounted workspace evidence, exact file/position, `item_names=["live_callee"]`, and `incoming_names=["live_caller"]`.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer document, workspace, completion, signature, hover, type-definition, call-hierarchy, document-highlight, prepare-rename, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `5.902 ms` versus `commit_to_workspace` p95 `3.373 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=4`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `workspace/symbol` + `completion` + `signatureHelp` + `hover` + `typeDefinition` + `callHierarchy` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 31 - 2026-06-02 05:37 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T213629Z-76832.json`, `.omc/results/plugin-refresh-strategies-20260601T213629Z-76832.md`, `.omc/results/rust-daemon-plugin-generic-20260601T213629Z-76832.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T213629Z-76832.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 41.37s`.
- Findings summary: The live Rust plugin case now proves Pyright `textDocument/typeDefinition` through the same daemon-managed refreshed read-only service. `plugin.generic.pyright_type_definition` was connected at ensure time, reused `pyright_harness`, returned `protocol="lsp-jsonrpc"`, and resolved `thing` in `live_plugin_type.py` at line `4`, character `11` back to the class definition range starting at line `0`, character `6`. Existing Pyright document-symbol, workspace-symbol, completion, signature-help, hover, document-highlight, prepare-rename, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green.
- Issues found and fixes applied: No runtime failure. Added a Pyright adapter `textDocument/typeDefinition` method, a dedicated seeded type file, `plugin.generic.pyright_type_definition` route coverage, and benchmark/pytest gates requiring connected routing, mounted workspace evidence, exact request file/position, `type_definition_count >= 1`, and an in-workspace location for `live_plugin_type.py`.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer document, workspace, completion, signature, hover, type-definition, document-highlight, prepare-rename, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `3.934 ms` versus `commit_to_workspace` p95 `3.366 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=4`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `workspace/symbol` + `completion` + `signatureHelp` + `hover` + `typeDefinition` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 30 - 2026-06-02 05:26 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T212538Z-62097.json`, `.omc/results/plugin-refresh-strategies-20260601T212538Z-62097.md`, `.omc/results/rust-daemon-plugin-generic-20260601T212538Z-62097.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T212538Z-62097.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 41.96s`.
- Findings summary: The live Rust plugin case now proves a workspace-wide Pyright read-only LSP request on the same daemon-managed refreshed service. `plugin.generic.pyright_workspace_symbols` was connected at ensure time, reused `pyright_harness` and the same Pyright pid as the text-document operations, returned `protocol="lsp-jsonrpc"`, `symbol_count=1`, `symbol_names=["live_value"]`, and `symbol_paths=["live_plugin_pyright.py"]`. Existing Pyright document-symbol, completion, signature-help, hover, document-highlight, prepare-rename, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green.
- Issues found and fixes applied: No runtime failure. Added a Pyright adapter `workspace/symbol` method, normalized workspace-symbol locations back to workspace-relative paths with an escape guard, declared `plugin.generic.pyright_workspace_symbols` in the manifest, called it after document symbols and before completion, and extended both the benchmark gate and pytest assertions to require connected routing, mounted workspace evidence, the `live_value` symbol, and `live_plugin_pyright.py` as the reported path.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer both text-document and workspace-wide symbol requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; refresh p95 was `4.413 ms` versus `commit_to_workspace` p95 `3.304 ms`; raw workspace watch remained stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=4`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `workspace/symbol` + `completion` + `signatureHelp` + `hover` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 29 - 2026-06-02 05:13 CST

- Exact commands run:
  - Failed first try: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
  - Corrected rerun: same command.
- Artifact paths inspected: failed first-try artifact `.omc/results/rust-daemon-plugin-generic-20260601T211157Z-40497.json`, passing artifacts `.omc/results/plugin-refresh-strategies-20260601T211335Z-43003.json`, `.omc/results/plugin-refresh-strategies-20260601T211335Z-43003.md`, `.omc/results/rust-daemon-plugin-generic-20260601T211335Z-43003.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T211335Z-43003.md`.
- Pass/fail/skip status: first try failed at the Rust benchmark gate; corrected rerun passed with focused plugin live test `1 passed in 41.82s`.
- Findings summary: The live Rust plugin case now proves another real Pyright read-only LSP operation on the same daemon-managed refreshed service. `plugin.generic.pyright_signature_help` was connected at ensure time, reused `pyright_harness`, returned `protocol="lsp-jsonrpc"`, `signature_count=1`, `active_parameter=1`, and label `(left: int, right: str) -> str` for `live_plugin_signature.py` at line `3`, character `28`. Existing Pyright document-symbol, completion, hover, document-highlight, prepare-rename, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green.
- Issue found and fix applied: The first gate incorrectly required Pyright's signatureHelp label to include the function name `live_signature`. The actual Pyright LSP payload returned a stable parameter-only label `(left: int, right: str) -> str`, so the benchmark and pytest gates now assert the useful signatureHelp contract: connected route, mounted workspace, exact file/position, `signature_count >= 1`, `active_parameter == 1`, and a label containing both `left` and `right`.
- Correctness result: PASS after the correction. The read-only Pyright service remains current after daemon remount and can answer symbol, completion, signature-help, hover, document-highlight, prepare-rename, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; raw workspace watch remains stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=4`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `completion` + `signatureHelp` + `hover` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 28 - 2026-06-02 05:02 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T210252Z-25379.json`, `.omc/results/plugin-refresh-strategies-20260601T210252Z-25379.md`, `.omc/results/rust-daemon-plugin-generic-20260601T210252Z-25379.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T210252Z-25379.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 42.33s`.
- Findings summary: The live Rust plugin case now proves another real Pyright read-only LSP operation on the same daemon-managed refreshed service. `plugin.generic.pyright_completion` was connected at ensure time, reused `pyright_harness`, returned `protocol="lsp-jsonrpc"`, and produced `matching_labels=["live_value"]` for `live_plugin_completion.py` at line `3`, character `14`. Existing Pyright document-symbol, hover, document-highlight, prepare-rename, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green.
- Issue found and fix applied: Added a completion-specific seeded Python file and Pyright adapter `textDocument/completion` method, declared `plugin.generic.pyright_completion` in the manifest, called it before the write path, and extended both the benchmark gate and pytest assertions to require connected routing, mounted workspace evidence, the exact completion file/position, and a `live_value` completion label.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer symbol, completion, hover, document-highlight, prepare-rename, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`; raw workspace watch remains stale without materialization; auto-squash plus post-drain commit passed; the Rust plugin report showed `retained_service_leases_before_cleanup=4`; and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `completion` + `hover` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 27 - 2026-06-02 04:52 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T205249Z-9744.json`, `.omc/results/plugin-refresh-strategies-20260601T205249Z-9744.md`, `.omc/results/rust-daemon-plugin-generic-20260601T205249Z-9744.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T205249Z-9744.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 42.17s`.
- Findings summary: The live Rust plugin case now proves a sixth real Pyright read-only LSP operation on the same daemon-managed refreshed service. `plugin.generic.pyright_prepare_rename` was connected at ensure time, reused `pyright_harness`, returned `protocol="lsp-jsonrpc"`, and resolved a prepare-rename range for `live_plugin_pyright.py` at line `3`, characters `9..19`. Existing Pyright document-symbol, hover, document-highlight, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green.
- Issue found and fix applied: The previous artifact showed `final_metrics.active_leases=4` because it sampled metrics while four long-lived services were intentionally still ready and retaining snapshot leases. The benchmark now records `processes_before_cleanup`, kills service harnesses, calls `api.plugin.status` to reap service snapshots, records `post_cleanup_metrics`, and gates on `post_cleanup_metrics.active_leases == 0` plus zero post-cleanup orphan/missing layers.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer symbol, hover, document-highlight, prepare-rename, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`, raw workspace watch remains stale without materialization, auto-squash plus post-drain commit passed, the Rust plugin report showed `retained_service_leases_before_cleanup=4`, and post-cleanup active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `hover` + `documentHighlight` + `prepareRename` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 26 - 2026-06-02 04:42 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T204231Z-92374.json`, `.omc/results/plugin-refresh-strategies-20260601T204231Z-92374.md`, `.omc/results/rust-daemon-plugin-generic-20260601T204231Z-92374.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T204231Z-92374.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 39.32s`.
- Findings summary: The live Rust plugin case now proves a fifth real Pyright read-only LSP operation on the same daemon-managed refreshed service. `plugin.generic.pyright_document_highlight` was connected at ensure time, reused `pyright_harness`, returned `protocol="lsp-jsonrpc"` with `highlight_count=2`, and identified `live_plugin_pyright.py` ranges at start lines `0` and `3` for the call-site symbol. Existing Pyright document-symbol, hover, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green; final active leases, orphan layers, and missing layers were all `0`.
- Issues found and fixes applied: Added a Pyright adapter `textDocument/documentHighlight` method, declared `plugin.generic.pyright_document_highlight` in the manifest, called it after hover and before definition/references/rename, and extended both the benchmark gate and pytest assertions to require connected routing, mounted workspace evidence, at least two highlights, and start lines `{0, 3}`.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer symbol, hover, document-highlight, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`, raw workspace watch remains stale without materialization, auto-squash plus post-drain commit passed, and final live plugin reports ended with zero active leases, orphan layers, and missing layers.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `hover` + `documentHighlight` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 25 - 2026-06-02 04:31 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T203150Z-74771.json`, `.omc/results/plugin-refresh-strategies-20260601T203150Z-74771.md`, `.omc/results/rust-daemon-plugin-generic-20260601T203150Z-74771.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T203150Z-74771.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 40.44s`.
- Findings summary: The live Rust plugin case now proves a fourth real Pyright read-only LSP operation on the same daemon-managed refreshed service. `plugin.generic.pyright_hover` was connected at ensure time, reused `pyright_harness`, returned `protocol="lsp-jsonrpc"`, and produced hover text `(function) def live_value() -> int` for the call site at line `3`, character `12` in `live_plugin_pyright.py`. Existing Pyright document-symbol, definition, references, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green; final active leases, orphan layers, and missing layers were all `0`.
- Issues found and fixes applied: Added a Pyright adapter `textDocument/hover` method to the benchmark harness, normalized LSP markup hover content into `hover_text`, declared `plugin.generic.pyright_hover` in the manifest, called it before definition/references/rename, and extended the pytest/live gate to assert connected routing plus `live_value` and `int` hover evidence.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer symbol, hover, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`, raw workspace watch remains stale without materialization, auto-squash plus post-drain commit passed, and final live plugin reports ended with zero active leases, orphan layers, and missing layers.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `hover` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 24 - 2026-06-02 04:24 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T202409Z-62233.json`, `.omc/results/plugin-refresh-strategies-20260601T202409Z-62233.md`, `.omc/results/rust-daemon-plugin-generic-20260601T202409Z-62233.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T202409Z-62233.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 39.36s`.
- Findings summary: The live Rust plugin case now proves a third real Pyright read-only LSP operation on the same daemon-managed refreshed service. `plugin.generic.pyright_references` was connected at ensure time, reused `pyright_harness`, returned `protocol="lsp-jsonrpc"` with `reference_count=2`, and resolved `live_plugin_pyright.py` locations at range start lines `0` and `3` with `include_declaration=true`. Existing Pyright document-symbol, definition, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green; final active leases, orphan layers, and missing layers were all `0`.
- Issues found and fixes applied: Added a Pyright adapter `textDocument/references` method to the benchmark harness, declared `plugin.generic.pyright_references` in the manifest, called it before the Pyright rename write path, and extended the pytest/live gate to assert connected routing plus declaration and call-site reference evidence.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer symbol, definition, and references requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`, raw workspace watch remains stale without materialization, auto-squash plus post-drain commit passed, and final live plugin reports ended with zero active leases, orphan layers, and missing layers.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `definition` + `references` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 23 - 2026-06-02 04:14 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T201403Z-41162.json`, `.omc/results/plugin-refresh-strategies-20260601T201403Z-41162.md`, `.omc/results/rust-daemon-plugin-generic-20260601T201403Z-41162.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T201403Z-41162.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 38.30s`.
- Findings summary: The live Rust plugin case now proves a second real Pyright read-only LSP operation on the same daemon-managed refreshed service. `plugin.generic.pyright_definition` was connected at ensure time, reused `pyright_harness`, returned `protocol="lsp-jsonrpc"` with `definition_count=1`, and resolved the call at line `3`, character `12` in `live_plugin_pyright.py` back to the definition range starting at line `0`, character `4`. Existing Pyright document-symbol, self-managed rename/OCC publish, package-adapter, restart, crash, timeout, and one-shot checks stayed green; final active leases, orphan layers, and missing layers were all `0`.
- Issues found and fixes applied: Added a Pyright adapter `textDocument/definition` method plus LSP `Location`/`LocationLink` normalization to the benchmark harness, declared `plugin.generic.pyright_definition` in the manifest, and extended the pytest gate to assert connected routing plus target path/range evidence.
- Correctness result: PASS. The read-only Pyright service remains current after daemon remount and can answer both symbol and definition requests before the same service computes a rename edit that publishes through the daemon OCC callback.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`, raw workspace watch remains stale without materialization, auto-squash plus post-drain commit passed, and final live plugin reports ended with zero active leases, orphan layers, and missing layers.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `definition` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 22 - 2026-06-02 04:06 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T200629Z-29385.json`, `.omc/results/plugin-refresh-strategies-20260601T200629Z-29385.md`, `.omc/results/rust-daemon-plugin-generic-20260601T200629Z-29385.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T200629Z-29385.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 40.37s`.
- Findings summary: The live Rust plugin case now proves a hung long-lived service fails closed through the generic PPC timeout path. The final Rust report used `eosd-linux-amd64` SHA `44d6dd908136753d3aa0f6a48afdd745b33096a5ffc4a2ea387ae73de045b7e2`, had `gate_pass=true`, included connected `plugin.generic.hang_probe` before the timeout, observed the intentional sleep as `expected_failure=true` with `daemon io error: Resource temporarily unavailable (os error 11)`, removed `plugin.generic.hang_probe` from connected routes after the timeout, marked only `hang_harness.state == "stopped"` with the same `last_error`, kept unrelated `harness` and `pyright_harness` services ready, and ended with active leases, orphan layers, and missing layers all `0`.
- Issues found and fixes applied: Added a dedicated `hang_harness` service and `plugin.generic.hang_probe` operation to the live Rust plugin benchmark instead of reusing the crash service, so timeout teardown is isolated from crash teardown. The pytest gate now asserts the route is initially connected, absent after timeout, and that `hang_harness` is stopped.
- Correctness result: PASS. The timeout path no longer leaves a stale connected route or ready service status, and existing crash, Pyright rename, restart, adapter, and one-shot checks stayed green in the same run.
- Performance/O(1) result: PASS for this coverage slice. The paired refresh-strategy report still recommends `workspace_snapshot_refresh`, raw workspace watch remains stale without materialization, auto-squash plus post-drain commit passed, and final live plugin reports ended with zero active leases, orphan layers, and missing layers.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `rename`, true out-of-order multiplexing if required, and heartbeat/crash hardening beyond the covered closed PPC stream and PPC timeout paths.

## Iteration 21 - 2026-06-02 03:54 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T195427Z-9685.json`, `.omc/results/plugin-refresh-strategies-20260601T195427Z-9685.md`, `.omc/results/rust-daemon-plugin-generic-20260601T195427Z-9685.json`, `.omc/results/rust-daemon-plugin-generic-20260601T195427Z-9685.md`, plus failed crash-probe/debug artifacts `.omc/results/rust-daemon-plugin-generic-20260601T194647Z-1105.json` and `.omc/results/rust-daemon-plugin-generic-20260601T195011Z-4002.json`.
- Pass/fail/skip status: passed after fixes; focused plugin live test `1 passed in 39.93s`.
- Findings summary: The live Rust plugin case now proves a closed-PPC service crash fails closed in addition to generic PPC/OCC/refresh/remount/package-adapter/Pyright-read/Pyright-rename/restart/oneshot coverage. The final Rust report used `eosd-linux-amd64` SHA `44d6dd908136753d3aa0f6a48afdd745b33096a5ffc4a2ea387ae73de045b7e2`, had `gate_pass=true`, included connected `plugin.generic.crash_probe` before the crash, observed the intentional harness exit as `expected_failure=true` with `ppc channel error: plugin PPC stream closed before reply`, removed `plugin.generic.crash_probe` from connected routes after the failure, marked only `crash_harness.state == "stopped"` with the same `last_error`, and ended with active leases, orphan layers, and missing layers all `0`.
- Issues found and fixes applied: The first crash-probe run proved the daemon removed the broken PPC route but left the service status `ready`; the daemon now tears down failed connected services through a shared helper that removes the PPC client, service process, retained snapshot, process group, and retained snapshot lease while recording the stop reason. A second run still failed because the live benchmark uploaded a stale packaged `eosd-linux-amd64`; the artifact was rebuilt. The first rebuilt run then exposed the runner raw-argv guard for the daemon-owned internal plugin overlay worker. That path now uses the dedicated `plugin_service` runner verb for argv commands, so the legacy `cp4s_legacy_argv` shell escape hatch is no longer needed.
- Correctness result: PASS. The crash path no longer leaves a stale connected route or ready service status, and the representative Pyright rename still publishes through `daemon.occ.apply_changeset` and reads back `def live_total() -> int:\n    return 42\n\nRESULT = live_total()\n`.
- Performance/O(1) result: PASS for this coverage slice. The refresh-strategy report still recommends `workspace_snapshot_refresh`; raw workspace watch remains stale without materialization; auto-squash plus post-drain commit passed; and final live plugin reports ended with zero active leases, orphan layers, and missing layers.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `rename`, true out-of-order multiplexing if required, and crash/teardown hardening beyond the closed PPC stream path.

## Iteration 20 - 2026-06-02 03:37 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T193729Z-84883.json`, `.omc/results/rust-daemon-plugin-generic-20260601T193729Z-84883.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T193729Z-84883.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 45.29s`.
- Findings summary: The live Rust plugin case now proves a representative Pyright WRITE_ALLOWED/self-managed path on top of the generic daemon-managed service contract. Connected routes included `plugin.generic.pyright_rename`; the same `pyright_harness` that served `plugin.generic.pyright_symbols` returned a real LSP `textDocument/rename` `documentChanges` edit for `live_plugin_pyright.py`, converted it into a generic write changeset, published through `daemon.occ.apply_changeset`, and read back `def live_total() -> int:\n    return 42\n\nRESULT = live_total()\n` from LayerStack. The callback reported `success=true`, file status `committed`, and published manifest version `4`; final manifest version was `5`; final orphan/missing-layer counts stayed `0`.
- Issues found and fixes applied: Added `plugin.generic.pyright_rename` to the live harness manifest as WRITE_ALLOWED with `auto_workspace_overlay=false`; added Pyright adapter helpers to request LSP rename, decode WorkspaceEdit `changes`/`documentChanges`, apply text edits in memory, and send the resulting daemon OCC callback without writing directly into the service overlay. The pytest gate now asserts the connected route, callback success, changed path, LSP protocol/new name, and final LayerStack readback.
- Correctness result: PASS. The rename path used Pyright for the edit plan, daemon PPC callbacks for publish, and LayerStack readback for proof.
- Performance/O(1) result: PASS for this coverage slice. The refresh-strategy benchmark still recommended `workspace_snapshot_refresh`, raw workspace watch remained stale without materialization, auto-squash plus post-drain commit passed, and both plugin reports ended with zero orphan/missing layers.
- Remaining risk or next iteration target: Broader AV-10 LSP parity beyond representative `documentSymbol` + `rename`, plus any required out-of-order PPC multiplexing and crash/teardown hardening.

## Iteration 19 - 2026-06-02 03:21 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T192035Z-59861.json`, `.omc/results/plugin-refresh-strategies-20260601T192035Z-59861.md`, `.omc/results/rust-daemon-plugin-generic-20260601T192035Z-59861.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T192035Z-59861.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 41.79s`.
- Findings summary: The live Rust plugin case now proves a real Pyright read-only LSP adapter over the generic daemon-managed service contract. The Rust plugin report used `eosd-linux-amd64` SHA `dc43de2baeaa407ccd07e1f0dd0de0efd7999eccf48006369860268efc826c17`; connected routes included `plugin.generic.pyright_symbols`; the `pyright_harness` used `refresh_strategy="remount_workspace_and_notify"`, reached `state=ready` with `refresh_count=1`, and returned a `pyright-langserver` LSP document-symbol response containing `live_value` after a daemon `api.v1.write_file` publish and namespace remount; package adapter, restart fallback, one-shot overlay, and final orphan/missing-layer gates stayed green.
- Issues found and fixes applied: Added a lazy Pyright adapter to the PPC harness so `api.plugin.ensure` still connects quickly and the first Pyright op owns Node/Pyright setup. The setup script is staged under `/eos/plugin/*`, extracts Node with `--no-same-owner --no-same-permissions`, changes into `/tmp/eos-node22` before npm work to avoid `uv_cwd` after namespace remount, and the benchmark records Pyright failures as structured report data. The pytest gate now asserts connected Pyright routing, mounted workspace evidence, LSP protocol/server fields, the `live_value` symbol, and `pyright_harness.refresh_count >= 1`.
- Remaining risk or next iteration target: Full AV-10 still needs canonical Python-importlib vs Rust-PPC LSP parity for representative WRITE_ALLOWED/self-managed paths, plus any required out-of-order PPC multiplexing or broader crash-hardening work.

## Iteration 18 - 2026-06-02 03:16 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 EOS_RUST_PLUGIN_BENCH_TIMEOUT_S=600 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`, followed by standalone debug reruns of `backend/scripts/bench_rust_daemon_plugin.py --keep-container`.
- Artifact paths inspected: pytest failure output, `.omc/results/plugin-refresh-strategies-20260601T191631Z-54290.json`, `.omc/results/plugin-refresh-strategies-20260601T191631Z-54290.md`, kept-container `/eos/plugin/rust_ppc_harness.jsonl`, `.omc/results/rust-daemon-plugin-debug-pyright.json`, and `.omc/results/rust-daemon-plugin-debug-pyright.md`.
- Pass/fail/skip status: failed; first integrated Pyright attempt failed during `plugin.generic.pyright_symbols` with `plugin PPC stream closed before reply`; the standalone debug rerun then exposed structured setup failures.
- Findings summary: The daemon refresh/remount sequence reached `pyright_harness` correctly before the failure: the harness log showed `PrepareRefresh -> Quiesce -> SwapWorkspace -> NotifyRefresh -> Resume -> Health` for manifest `3:a8dd3f87879678807820ca4703cf88b7ab2f7f8255ee9162c968e11e5a71f204`. The first failure was Node archive extraction under the service user namespace: `tar` tried to restore uid/gid ownership and exited with `Cannot change ownership`. The second debug failure was npm starting from a remounted/detached cwd and failing with `ENOENT: no such file or directory, uv_cwd`.
- Issues found and fixes applied: Changed the Pyright setup tar extraction to `tar --no-same-owner --no-same-permissions`, changed the setup script to `cd "$NODE_HOME"` before npm commands, and changed the benchmark to catch Pyright call exceptions into `report["pyright_symbols"]` so future failures produce complete JSON/Markdown evidence instead of aborting before report write.
- Remaining risk or next iteration target: Rerun the full pytest wrapper after the setup fixes and verify the Pyright LSP symbol response under the integrated fixture-created container.

## Iteration 17 - 2026-06-02 03:00 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T190008Z-10433.json`, `.omc/results/plugin-refresh-strategies-20260601T190008Z-10433.md`, `.omc/results/rust-daemon-plugin-generic-20260601T190008Z-10433.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T190008Z-10433.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 25.20s`.
- Findings summary: The live Rust plugin case now proves a generic package-adapter service on top of the daemon-managed `workspace_snapshot_refresh` contract. The Rust plugin report used `eosd-linux-amd64` SHA `dc43de2baeaa407ccd07e1f0dd0de0efd7999eccf48006369860268efc826c17`; connected routes included `plugin.generic.adapter_query`; the `adapter_harness` used `refresh_strategy="remount_workspace"`, reached `state=ready` with `refresh_count=1`, and returned a cached vanilla package response with `protocol="line-json-v1"` and `content="from live rust plugin\n"` after the daemon remounted its workspace; the original remount/notify harness still reached `refresh_count=1`; the restart harness still reached `restart_count=1`; one-shot overlay execution still committed `live_plugin_oneshot_result.txt`; and final orphan/missing-layer counts stayed `0`.
- Issues found and fixes applied: Added a vanilla JSON-lines subprocess (`rust_vanilla_package.py`) behind the PPC harness to model an arbitrary package with its own cache. The harness now starts the adapter only for `adapter_harness`, reloads it on the generic `SwapWorkspace` refresh frame after daemon remount, exposes `plugin.generic.adapter_query`, stages/cleans the adapter under `/eos/plugin/*`, and the pytest gate asserts connected routing, adapter protocol, cached post-refresh content, mounted workspace evidence, and adapter `refresh_count >= 1`.
- Remaining risk or next iteration target: Port a real Pyright/LSP adapter on top of the same daemon refresh/remount protocol, then close AV-10 and any required out-of-order PPC multiplexing or broader crash-hardening work.

## Iteration 16 - 2026-06-02 02:54 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T185413Z-90374.json`, `.omc/results/plugin-refresh-strategies-20260601T185413Z-90374.md`, `.omc/results/rust-daemon-plugin-generic-20260601T185413Z-90374.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T185413Z-90374.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 24.54s`.
- Findings summary: The live Rust plugin case now proves daemon-owned in-place namespace remount for a generic long-lived read-only service. The Rust plugin report used `eosd-linux-amd64` SHA `dc43de2baeaa407ccd07e1f0dd0de0efd7999eccf48006369860268efc826c17`; after self-managed apply published `live_plugin_result.txt`, the original `remount_workspace_and_notify` harness reached `refresh_count=1`, stayed `state=ready`, and read `live_plugin_result.txt` as `from live rust plugin\n` through its remounted `EOS_PLUGIN_WORKSPACE_ROOT`; the separate `restart_service` harness still reached `restart_count=1` with `refresh_count=0`; one-shot overlay execution still committed `live_plugin_oneshot_result.txt`; and final orphan/missing-layer counts stayed `0`.
- Issues found and fixes applied: Added generic Rust remount support instead of a package-specific LSP hook. `eos-overlay` now exposes lazy overlay unmount; `eosd ns-runner --remount-overlay` replaces the workspace overlay in the current mount namespace; `eos-daemon` enters the service wrapper namespace with `nsenter -t <pid> -U -m --preserve-credentials` between `Quiesce` and `SwapWorkspace`; the live harness now asserts the refreshed original service can read the post-write file, not only the restarted service; and a focused daemon unit test now proves an exited tracked service process is removed and fails closed before dispatch.
- Remaining risk or next iteration target: Add package adapter harnesses and Pyright/LSP parity on top of the generic service contract, then close AV-10 and any required out-of-order PPC multiplexing/crash-hardening work.

## Iteration 15 - 2026-06-02 02:33 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T183242Z-26831.json`, `.omc/results/plugin-refresh-strategies-20260601T183242Z-26831.md`, `.omc/results/rust-daemon-plugin-generic-20260601T183242Z-26831.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T183242Z-26831.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 24.20s`.
- Findings summary: The live Rust plugin case now proves long-lived read-only services are launched inside a private overlay namespace on Linux. The Rust plugin report used `eosd-linux-amd64` SHA `ddb923eb0f1a3e6b1cd367ab978f7056088175532a26c6b262a94d3ff029b6e7`; `plugin.generic.ping` reported `workspace_mounted=true`; after self-managed apply, the restarted read-only service reported `workspace_mounted=true` and read `live_plugin_result.txt` as `from live rust plugin\n` through `EOS_PLUGIN_WORKSPACE_ROOT`; remount/notify still reached `refresh_count=1`; restart fallback still reached `restart_count=1`; and final orphan/missing-layer counts stayed `0`.
- Issues found and fixes applied: Added a `plugin_service` runner verb so the daemon can start vanilla long-lived service commands through the existing single-threaded `eosd ns-runner` overlay mount path. Linux packaging caught a target-only sanitizer reference that host tests could not compile; added a local plugin-module sanitizer and reran package/live. The live harness now asserts mounted workspace evidence instead of only status counters.
- Remaining risk or next iteration target: Implement in-place remount into an already-running private service namespace, add package adapter/Pyright parity, and close AV-10.

## Iteration 14 - 2026-06-02 02:18 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T181732Z-84591.json`, `.omc/results/plugin-refresh-strategies-20260601T181732Z-84591.md`, `.omc/results/rust-daemon-plugin-generic-20260601T181732Z-84591.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T181732Z-84591.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 25.38s`.
- Findings summary: The live Rust plugin case now proves both stale-service refresh mechanisms for arbitrary read-only services. The Rust plugin report used `eosd-linux-amd64` SHA `f200673fd47526257e5ea0f2172526702cfb7d7800a0158deb7fdc60beaf9d5e`; connected routes were `plugin.generic.apply`, `plugin.generic.ping`, and `plugin.generic.restart_ping`; self-managed apply published the post-write manifest; the remount/notify harness reached `refresh_count=1`; the separate restart harness reached `restart_count=1` with `refresh_count=0`; one-shot overlay execution still committed `live_plugin_oneshot_result.txt`; and final orphan/missing-layer counts stayed `0`.
- Issues found and fixes applied: Added focused `restart_service` fallback coverage in Rust and live Docker. The Rust test had to model a service harness that reconnects only after the peer write makes the service stale, avoiding an initial-listener backlog race. The live harness now declares a second read-only `restart_harness` service and asserts the daemon restarts it on the next read-only request instead of sending refresh frames.
- Remaining risk or next iteration target: Implement true private-namespace remount for `workspace_snapshot_refresh`, add package adapter/Pyright parity, and close AV-10.

## Iteration 13 - 2026-06-02 02:04 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T180306Z-47847.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T180306Z-47847.json`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 24.68s`.
- Findings summary: The live Rust plugin case now proves the first `workspace_snapshot_refresh` freshness gate. The Rust plugin report used `eosd-linux-amd64` SHA `decc8718adb19ed9f25417f5151978ac57dde39e311db53271e59128577659fb`; the long-lived harness service started `ready` on manifest key `1:f071e2d096b352b67daeb0f2e2f6dc503335246a98e209fa9f199d06314b5cb5`, self-managed apply published a new manifest, the next read-only ping went through refresh and returned `ready` with `refresh_count=1` on manifest key `2:5d504c31077a155d53f2829ae1a6b26eec61671f55e46688480935f4492684ef`, and one-shot overlay plus final orphan/missing-layer checks stayed green.
- Issues found and fixes applied: Added retained daemon snapshot records for started plugin services and a pre-dispatch freshness gate for connected read-only PPC routes. The daemon now runs the generic refresh sequence before sending a stale service a read-only request, swaps the retained lease only after harness acknowledgement, and records refresh status. The live harness now handles `daemon.workspace_snapshot_refresh` frames and asserts `refresh_count >= 1` after a peer publish.
- Remaining risk or next iteration target: Launch the long-lived service inside a private overlay namespace and implement true namespace remount/restart adapters, then add package adapter/Pyright parity and AV-10.

## Iteration 12 - 2026-06-02 01:45 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T174448Z-3110.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T174448Z-3110.json`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 24.38s`.
- Findings summary: The plugin live test now covers refresh-strategy correctness plus Rust-runtime generic plugin PPC, self-managed OCC callback, and daemon-owned one-shot WRITE_ALLOWED overlay execution. The Rust plugin report used `eosd-linux-amd64` SHA `739f71c1f50061d32f2233db69086309caa63a0da0fec54974bb203a428101fa`, registered `plugin.generic.apply`, `plugin.generic.oneshot_write`, and `plugin.generic.ping`, connected only the long-lived PPC routes `plugin.generic.apply` and `plugin.generic.ping`, committed `live_plugin_result.txt` through `daemon.occ.apply_changeset`, committed `live_plugin_oneshot_result.txt` through the one-shot overlay worker, and ended with zero orphan/missing layers.
- Issues found and fixes applied: Linux packaging caught target-specific references hidden from the host test build; restored the target-only `Lease` and `OverlayWritableDirs` imports and converted the plugin overlay response helper to pass a `PathBuf` into `LayerStack::open`. The benchmark now stages both the PPC harness and one-shot worker through `/tmp` before copying into `/eos/plugin/*`.
- Remaining risk or next iteration target: Implement daemon `workspace_snapshot_refresh` remount/restart, add package adapter harnesses, Pyright/LSP parity, and AV-10.

## Iteration 11 - 2026-06-02 01:25 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output, `.omc/results/plugin-refresh-strategies-20260601T172544Z-50755.json`, and `.omc/results/rust-daemon-plugin-generic-20260601T172544Z-50755.json`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 24.52s`.
- Findings summary: The plugin live test now covers both refresh-strategy correctness and a Rust-runtime generic process-backed plugin service. The Rust plugin report used current `eosd-linux-amd64` SHA `2f96ab5a69ba2665dc9812fcea94793bdd2cb4bf585f25b22f1f96ea4f473bf5`, connected `plugin.generic.ping` and `plugin.generic.apply`, committed `live_plugin_result.txt` through `daemon.occ.apply_changeset`, read back `from live rust plugin\n`, and ended with zero orphan/missing layers.
- Issues found and fixes applied: First live attempt failed because `api.build_workspace_base` requires the target workspace root to exist; the benchmark now creates `/eos/plugin/rust-workspace` before binding it. The next attempt failed because Docker archive upload into `/eos` was invisible to the live exec namespace's `/eos` tmpfs; the harness now uploads through `/tmp` and copies into `/eos/plugin/*` with `docker exec`, matching the existing Rust daemon artifact upload pattern.
- Remaining risk or next iteration target: Implement daemon `workspace_snapshot_refresh` remount/restart, automatic `WRITE_ALLOWED` overlay execution, package adapter harnesses, Pyright/LSP parity, and AV-10.

## Iteration 10 - 2026-06-02 00:53 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output and the refreshed `.omc/results/plugin-refresh-strategies-*` artifact emitted by the test.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 12.41s`.
- Findings summary: The benchmark-backed live plugin refresh case remains green after wiring the daemon-side OCC callback body. Focused Rust coverage now proves a connected self-managed `plugin.*` route can receive a `daemon.occ.apply_changeset` callback, publish through the daemon OCC path, and then return the plugin's final reply.
- Fix applied: Added `plugin/occ_callbacks.rs` for generic callback changeset parsing, layer-stack-root validation, and `dispatcher::apply_occ_changeset` publishing. Connected self-managed routes now use `round_trip_with_callbacks` and service OCC callbacks on the same PPC stream.
- Remaining risk or next iteration target: Add a live Rust-runtime generic harness case, then implement `workspace_snapshot_refresh` remount/restart and WRITE_ALLOWED overlay execution.

## Iteration 9 - 2026-06-02 00:44 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output and the refreshed `.omc/results/plugin-refresh-strategies-*` artifact emitted by the test.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 12.30s`.
- Findings summary: The benchmark-backed live plugin refresh case remains green after adding daemon PPC callback-frame servicing. Focused Rust coverage now proves a plugin service can send a daemon callback request on the same PPC stream before the final operation reply and that the daemon rejects callback replies with a mismatched message id.
- Fix applied: Added `PpcClient::round_trip_with_callbacks`, which loops on PPC frames until the final reply for the original request arrives, services plugin-originated callback request frames through a caller-provided handler, validates reply direction/message id, and keeps the existing `round_trip` path strict by failing unexpected callbacks.
- Remaining risk or next iteration target: Wire the callback payload to the same per-root OCC writer for self-managed plugin publish, then add a live Rust-runtime generic harness case before moving to `workspace_snapshot_refresh` remount/restart and WRITE_ALLOWED execution.

## Iteration 8 - 2026-06-02 00:33 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output and the refreshed `.omc/results/plugin-refresh-strategies-*` artifact emitted by the test.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 12.23s`.
- Findings summary: The benchmark-backed live plugin refresh case remains green after changing the Rust daemon PPC registry to keep a shared per-service client handle. Focused Rust coverage now proves a second concurrent read-only `plugin.*` dispatch waits for the same service client instead of seeing a missing client and returning deferred.
- Fix applied: Replaced take/remove/putback PPC client dispatch with a cloned `Arc<Mutex<PpcClient>>` per service instance, keeping the daemon registry lock out of AF_UNIX I/O while serializing same-service requests on the client connection. Failed PPC I/O now removes the broken client from connected-route status.
- Remaining risk or next iteration target: Implement PPC callback servicing through the same OCC writer and add a live Rust-runtime generic harness case before moving to `workspace_snapshot_refresh` remount/restart and WRITE_ALLOWED execution.

## Iteration 7 - 2026-06-02 00:16 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output and the refreshed `.omc/results/plugin-refresh-strategies-*` artifact emitted by the test.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 12.77s`.
- Findings summary: The benchmark-backed live plugin refresh case remains green after the daemon-side Rust service PPC accept/connect handoff. The Rust unit coverage now proves `api.plugin.ensure start_services=true` can bind the service socket, accept a harness connection, and dispatch a registered read-only `plugin.*` request over the accepted PPC stream.
- Fix applied: Fixed the daemon PPC accept path by restoring accepted Unix streams to blocking mode before `PpcClient` request/reply I/O, because streams accepted from the nonblocking listener could otherwise return `WouldBlock` before the harness reply was available. Shortened test-only socket roots to avoid Unix socket path-length failures.
- Remaining risk or next iteration target: Add a live Rust-runtime plugin case with a small generic harness process, then extend toward concurrent PPC multiplexing, `workspace_snapshot_refresh` remount/restart, and self-managed OCC callbacks.

## Iteration 6 - 2026-06-01 23:59 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output and the refreshed `.omc/results/plugin-refresh-strategies-*` artifact emitted by the test.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 12.38s`.
- Findings summary: The benchmark-backed live plugin refresh case remains green after adding daemon-owned service process lifecycle behind `api.plugin.ensure start_services=true`. The live case still uses the Python refresh-strategy benchmark until the Rust harness accept/connect path is wired.
- Fix applied: None; this was a verification rerun after the Rust service lifecycle changes.
- Remaining risk or next iteration target: Wire the real plugin harness accept/connect path so a live Rust plugin case can start a service process, connect PPC, issue a read-only `plugin.*` op, and then exercise refresh.

## Iteration 5 - 2026-06-01 23:47 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output and the refreshed `.omc/results/plugin-refresh-strategies-*` artifact emitted by the test.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 12.30s`.
- Findings summary: The existing Docker fixture plus benchmark-script path remains stable after the Rust daemon PPC route slice. The live evidence still covers `workspace_snapshot_refresh`, stale raw workspace watch behavior, commit-to-workspace timer unsuitability, auto-squash/post-drain materialization, and final cleanup.
- Fix applied: None; this was a verification rerun after the Rust PPC route changes.
- Remaining risk or next iteration target: Add a Rust-runtime live plugin case after daemon service spawn/reap is wired to real plugin harness processes.

## Iteration 4 - 2026-06-01 23:32 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest output and the refreshed `.omc/results/plugin-refresh-strategies-*` artifact emitted by the test.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 12.55s`.
- Findings summary: The pytest wrapper still reuses the Docker sandbox fixture and benchmark script successfully after the Rust registered-route documentation update. No new fixture setup, watcher visibility, auto-squash, or final cleanup regression appeared.
- Fix applied: None; this was a verification rerun.
- Remaining risk or next iteration target: Add the Rust-runtime live variant when process-backed PPC can execute behind the registered `plugin.*` routes.

## Iteration 3 - 2026-06-01 23:20 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: `.omc/results/plugin-refresh-strategies-20260601T152044Z-6474.json`, `.omc/results/plugin-refresh-strategies-20260601T152044Z-6474.md`, `bench/plugin-refresh-strategies-20260601.json`, and `bench/plugin-refresh-strategies-20260601.md`.
- Pass/fail/skip status: passed; focused plugin live test `1 passed in 12.19s`; refreshed benchmark run with 3 samples also passed and wrote the `bench/` artifacts.
- Findings summary: The live suite now exercises the fast existing-container benchmark path from pytest instead of requiring a separate manual setup. The coverage proves `workspace_snapshot_refresh` keeps daemon reads current without raw workspace materialization, raw workspace watchers stay stale without materialization, `commit_to_workspace` is not a safe timer primitive, and auto-squash plus post-drain materialization preserves final bytes with zero orphan/missing layers.
- Verification result after the fix: Latest durable benchmark artifact recommends `workspace_snapshot_refresh`; p95 refresh was `5.747 ms` versus `commit_to_workspace` p95 `11.419 ms`; `workspace_snapshot_refresh.all_samples_ok=true`; `fs_watch_without_materialization.raw_workspace_stale=true`; `auto_squash_then_commit.gate_pass=true`; final active leases, orphan layers, and missing layers were all `0`.
- Remaining risk or next iteration target: The test still compares refresh strategies against the Python daemon path. Once Rust process-backed PPC lands, add a Rust-runtime variant that invokes `api.plugin.ensure`, one exact dynamic `plugin.*` read op, and one self-managed write callback.

## Iteration 2 - 2026-06-01 23:19 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest failure output; no benchmark JSON was produced because setup failed before watcher startup completed.
- Pass/fail/skip status: failed in `8.96s`.
- Findings summary: Recreating `/eos/plugin` immediately before `docker cp` was insufficient for fixture-created Docker containers. `docker exec` could create and see `/eos/plugin`, but `docker cp` still failed with `Could not find the file /eos/plugin in container`.
- Issue found: `docker cp` observes the container root namespace differently from the daemon/exec path for this fixture-created `/eos` runtime layout.
- Fix applied: Removed the watcher `docker cp` dependency and installed the watcher script through `docker exec` using Python/base64, so all setup happens through the same visibility path as the rest of the benchmark.

## Iteration 1 - 2026-06-01 23:18 CST

- Exact command run: `EOS_SANDBOX_PROVIDER=docker EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_PLUGIN_REFRESH_SAMPLES=1 EOS_PLUGIN_REFRESH_AUTO_SQUASH_WRITES=104 uv run pytest -q -x -rs --tb=short --durations=10 backend/tests/live_e2e_test/sandbox/plugin/test_plugin_refresh_strategies.py`.
- Artifact paths inspected: pytest failure output; no benchmark JSON was produced because setup failed before watcher startup completed.
- Pass/fail/skip status: failed in `8.55s`.
- Findings summary: The standalone benchmark passed on an older existing container, but the pytest fixture-created container failed installing the watcher script.
- Issue found: `docker cp local container:/eos/plugin/watch.py` failed because `/eos/plugin` was not visible to Docker copy even though the benchmark had just reset the experiment workspace.
- Fix applied: Added an idempotent watcher-parent directory creation step before copying. Iteration 2 proved that the issue was not directory creation alone but the `docker cp` path itself.
