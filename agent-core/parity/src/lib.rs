//! eos-parity — Phase-0 parity harness.
//! Fixtures (schemas, sse, prompt-report, sqlite schema) and the guard tests
//! that freeze current Python behavior live in `tests/`. This crate carries no
//! public api; see impl-workspace.md §3-§4.
#![forbid(unsafe_code)]
