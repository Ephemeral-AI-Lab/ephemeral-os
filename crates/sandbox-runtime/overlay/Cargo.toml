[package]
name = "sandbox-runtime-overlay"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
rustix.workspace = true
thiserror.workspace = true

[features]
# Test-only seam for package tests that exercise overlay-backed code on hosts
# without the production `/eos/scratch/overlay` tree.
test-root-override = []

[lints]
workspace = true
