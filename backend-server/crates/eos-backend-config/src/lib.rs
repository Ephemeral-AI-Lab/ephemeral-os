//! `eos-backend-config` — backend deployment and sandbox-lifecycle config
//! (`ServerConfig`, `AgentCoreConfigSource`, `SandboxConfig`, `ObsConfig`) with
//! `backend.yml < local.yml` loading.
//!
//! Scaffolded in Phase 1 (workspace shape); the config types land in Phase 3.
