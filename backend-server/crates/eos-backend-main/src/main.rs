//! `eos-backend-main` — the backend-server binary: composition root that wires
//! backend config, `backend.db`, the `SandboxManager` gateway, the agent-core
//! runtime, the event bus, observability, and the axum API.
//!
//! Scaffolded in Phase 1 (workspace shape); the app wiring lands in later phases.
#![forbid(unsafe_code)]

fn main() {}
