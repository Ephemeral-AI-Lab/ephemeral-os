# e2e test run report - 2026-06-12

## Run 1
- Command: `cargo test -p eos-e2e-test -- --list`
- Result: FAIL
- Issue: Compile failed before listing tests. `sandbox/crates/eos-e2e-test/tests/eos-layerstack/test_eos_layerstack_lease_and_squash_pinning.rs:221` referenced `std::catalog::Range<usize>`, but `std::catalog` does not exist.
- Finding: The failure is a local typo in the layerstack test helper signature, not a live Docker/runtime issue.
- Fix for next iteration: Replace the type path with `std::ops::Range<usize>`.

## Run 2
- Command: `cargo test -p eos-e2e-test -- --list`
- Result: PASS
- Issue: None in the compile/list gate after the `std::ops::Range<usize>` fix.
- Finding: Cargo listed the `eos-e2e-test` crate unit test plus all nine integration test binaries: `core`, `daemon`, `eos-layerstack`, `ephemeral_workspace`, `plugin`, `pressure`, `workspace-publish-gate`, `workspace-runtime-command-session`, and `workspace-runtime-isolated`.
- Fix for next iteration: Proceed to the serial Docker-backed live run with `--features e2e`.

## Run 3
- Command: `EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_E2E_PLATFORM=linux/amd64 cargo test -p eos-e2e-test --features e2e -- --test-threads=1 --nocapture`
- Result: FAIL
- Issue: The live run stopped in `--test core`. `test_core_direct_file_contracts::read_max_bytes_guard` and `test_core_direct_file_contracts::write_max_file_bytes_guard` failed because the daemon returned `error.code = "invalid_envelope"` while the tests expected `"invalid_request"`.
- Finding: The failure is deterministic and limited to oversize guard expectations in the core direct-file contract tests; 29 other core tests passed before Cargo stopped.
- Fix for next iteration: Trace the envelope and request-size guards to determine whether the live contract changed at the wire layer or the tests are constructing requests larger than the envelope limit.

## Preparation after Run 3
- Command: `cargo run -p xtask -- package`
- Result: PASS after one source fix.
- Issue: The first packaging attempt failed for the Linux target in `eos-command-session`: `open_pty_pair` returned an `OwnedFd` from `ioctl_tiocgptpeer` where the helper promised `File`.
- Finding: Current source no longer contains `invalid_envelope`, so Run 3 was exercising the stale packaged `dist/eosd-linux-amd64` artifact rather than the current daemon contract.
- Fix applied: Convert the Linux PTY slave fd with `File::from(...)` and rebuild `dist/eosd-linux-amd64`; new package SHA is `ac8d8014d4bddf3190f6f92fc6f247cc15421680d1d1bc58239365fccd56450d`.

## Run 4
- Command: `EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_E2E_PLATFORM=linux/amd64 cargo test -p eos-e2e-test --features e2e -- --test-threads=1 --nocapture`
- Result: FAIL
- Issue: The rebuilt-daemon live run passed crate unit tests plus `core`, `daemon`, `eos-layerstack`, `ephemeral_workspace`, `plugin`, and `pressure`, then stopped in `workspace-publish-gate`.
- Finding: The prior `invalid_envelope` failures disappeared after rebuilding `dist/eosd-linux-amd64`. The remaining failure is `workspace_publish_merge_conflicts::edit_anchor_errors_do_not_publish_or_advance_manifest`, where the response for an anchor-not-found edit conflict omitted `applied_edits`; the payload was otherwise a structured unpublished `status = "aborted_overlap"` mutation outcome.
- Fix for next iteration: Trace the edit conflict output contract and either restore `applied_edits = 0` for aborted edits or update the test if the field is intentionally absent.

## Run 5
- Command: `EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_E2E_PLATFORM=linux/amd64 cargo test -p eos-e2e-test --features e2e --test workspace-publish-gate -- --test-threads=1 --nocapture`
- Result: PASS
- Issue: None in the focused rerun.
- Finding: Restoring `applied_edits = 0` for edit conflict outcomes fixed the publish-gate failure; all 14 `workspace-publish-gate` tests passed against the refreshed packaged daemon (`dist/eosd-linux-amd64` SHA `422ab343aa1d79a5e7e7ba366230668c426c823661a7009c00fdf9d95317568c`).
- Fix for next iteration: Run the full serial live suite again to prove no later suite failures remain.

## Run 6
- Command: `cargo run -p eos-e2e-test --bin e2e-reap && EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_E2E_PLATFORM=linux/amd64 cargo test -p eos-e2e-test --features e2e -- --test-threads=1 --nocapture`
- Result: FAIL
- Issue: The full live run passed crate unit tests plus `core`, `daemon`, `eos-layerstack`, `ephemeral_workspace`, `plugin`, `pressure`, and `workspace-publish-gate`, then stopped in `workspace-runtime-command-session`.
- Finding: `command_session_lifecycle::write_stdin_echo` failed because `sandbox.command.stdin` returned `status = "running"` after writing `hello\n`, while the test expected terminal `status = "ok"` immediately. The payload had a valid `command_session_id`, so the command had not yet naturally exited within the response window.
- Fix for next iteration: Inspect `write_stdin_echo` and use the existing command-settle/poll pattern if this is only a timing assumption under linux/amd64 emulation.

## Run 7
- Command: `cargo run -p eos-e2e-test --bin e2e-reap && EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_E2E_PLATFORM=linux/amd64 cargo test -p eos-e2e-test --features e2e --test workspace-runtime-command-session write_stdin_echo -- --test-threads=1 --nocapture`
- Result: PASS
- Issue: None in the focused rerun.
- Finding: Settling the `sandbox.command.stdin` response through the existing poll helper fixed the linux/amd64 timing assumption; `command_session_lifecycle::write_stdin_echo` passed.
- Fix for next iteration: Run the full serial live suite again to verify the command-session suite and final isolated-workspace suite together.

## Run 8
- Command: `cargo run -p eos-e2e-test --bin e2e-reap && EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_E2E_PLATFORM=linux/amd64 cargo test -p eos-e2e-test --features e2e -- --test-threads=1 --nocapture`
- Result: PASS
- Issue: None.
- Finding: The full Docker-backed live suite passed end to end against `dist/eosd-linux-amd64` SHA `422ab343aa1d79a5e7e7ba366230668c426c823661a7009c00fdf9d95317568c`: crate unit test, `core` 31, `daemon` 12, `eos-layerstack` 20, `ephemeral_workspace` 11, `plugin` 14, `pressure` 23, `workspace-publish-gate` 14, `workspace-runtime-command-session` 66, `workspace-runtime-isolated` 20, and doc tests all passed.
- Fix for next iteration: No further E2E fixes needed for this run; keep the rebuilt daemon artifact with the source changes.

## Run 9
- Command: `cargo run -p eos-e2e-test --bin e2e-reap && EOS_LIVE_E2E_IMAGE=xingyaoww/sweb.eval.x86_64.dask_s_dask-10042:latest EOS_E2E_PLATFORM=linux/amd64 cargo test -p eos-e2e-test --features e2e -- --test-threads=1 --nocapture`
- Result: PASS
- Issue: None.
- Finding: Reaped 4 stale `eos-e2e` containers, then the current checkout passed the full Docker-backed live suite end to end: crate unit test, `core` 31, `daemon` 12, `eos-layerstack` 20, `ephemeral_workspace` 11, `plugin` 14, `pressure` 23, `workspace-publish-gate` 14, `workspace-runtime-command-session` 66, `workspace-runtime-isolated` 20, and doc tests all passed.
- Fix for next iteration: No further fixes are needed for `sandbox/crates/eos-e2e-test/tests` in this verified state.
