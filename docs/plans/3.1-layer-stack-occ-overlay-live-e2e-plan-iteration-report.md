# 3.1 Layer Stack/OCC/Overlay Live E2E Iteration Report

## Iteration 1 - 2026-05-24 18:47:31 CST

- Checkout summary: `main` at `ac57206d6`, ahead of `origin/main` by 16 commits. The worktree also contains unrelated overlay/plugin/LSP edits outside this plan's test files; they are left intact and treated as active runtime state for verification.
- Plan path: `docs/plans/3.1-layer-stack-occ-overlay-live-e2e-plan.md`.
- Target files:
  - `backend/src/task_center_runner/tests/mock/_layer_stack_occ_overlay_assertions.py`
  - `backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py`
  - `backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_heavy_io_zoned_concurrent.py`
  - `backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_auto_squash_commit_resume.py`
- Dependency pivot: none; this iteration stays on the 3.1 layer-stack/OCC/overlay plan.
- Coverage gaps found against `3.1.x Complex Cases To Add`:
  - High-concurrency already covered worker fanout, conflicts, squash, and broad performance-report shape, but did not assert `workspace_tree_bytes`, `workspace_tree_exists`, write/edit p95 budget, or OCC queue wait as a distinct timing key from mount time.
  - Heavy I/O already covered lowerdir O(1) and outside-zone changed-path leakage, but did not assert payload-bounded `upperdir_tree_bytes` or that gitignored `build/` writes are filtered from OCC changed paths.
  - Auto-squash already covered depth threshold, commit-resume wait, typed conflict, and final readback, but did not assert no materialized workspace tree appears in resource snapshots or performance-report resource keys.
- Tests, probes, or audit fields added:
  - Added shared 3.1 artifact assertion helpers for performance reports, resource keys, timing keys, JSONL rows, and O(1) workspace resource snapshots.
  - Tightened high-concurrency assertions for `workspace_tree_bytes == 0`, `workspace_tree_exists == 0`, write/edit p95 <= 1000ms, `occ.apply.commit_queue_wait_s`, `occ.apply.commit_resume_wait_s`, `command_exec.mount_workspace_s`, `command_exec.run_command_s`, `command_exec.capture_upperdir_s`, `layer_stack.prepare_workspace_snapshot.total_s`, `occ.apply.total_s`, and `api.shell.total_s`.
  - Tightened high-concurrency conflict checks so synthetic conflict errors cannot be reported as `internal_error` and must carry `conflict_reason`.
  - Tightened heavy I/O assertions for tracked payload upperdir bounds, gitignored `build/perf_load_*` filtering, outside `/tmp` filtering, and max changed-path count.
  - Tightened auto-squash assertions for O(1) workspace snapshots plus performance-report resource/timing keys.
- Exact commands run so far:
  - `sed -n '1,260p' docs/plans/3.1-layer-stack-occ-overlay-live-e2e-plan.md`
  - `if [ -f docs/plans/3.1-layer-stack-occ-overlay-live-e2e-plan-iteration-report.md ]; then sed -n '1,260p' docs/plans/3.1-layer-stack-occ-overlay-live-e2e-plan-iteration-report.md; else printf 'NO_REPORT\n'; fi`
  - `rg -n "test_high_concurrency_o1_disk_and_latency_budget|test_heavy_io_upperdir_scales_with_payload_not_repo|test_auto_squash_does_not_materialize_workspace_tree|high_concurrency_layerstack_overlay_occ|heavy_io_zoned|auto_squash|workspace_tree_bytes|workspace_tree_exists" backend/src/task_center_runner backend/tests docs/plans`
  - `git status --short --branch --untracked-files=all`
- Fresh artifacts inspected so far: none from this iteration; historical artifacts were used only to calibrate assertion shape, not for a pass verdict.
- First failure/stop signal: none yet.
- Root-cause hypothesis and evidence: the missing coverage was assertion-level, not a demonstrated runtime failure. Existing plan-surface tests already emitted the necessary `performance_report.json` and `sandbox_events.jsonl` fields.
- Code/test/audit changes made: test-only assertion additions plus a shared assertion helper; no runtime audit fields added in this iteration because existing artifacts already expose the required resource and latency keys.
- Correctness result: pending collect-only and live verification.
- Performance result: pending fresh 3.1 live artifacts.
- Next iteration entry point: run collect-only for `backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/`, then run the narrow 3.1 tests and the plan verification command.

### Iteration 1.1 - auto-squash assertion correction

- Timestamp: 2026-05-24 18:55 CST.
- Exact command run:
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/`
- Fresh artifact path inspected:
  - `.sweevo_runs/scenario_logs/sandbox.auto_squash_commit_resume/20260524T104857Z_e57581e9d6e5`
- First failure/stop signal:
  - `test_auto_squash_commit_resume_crosses_depth_threshold` failed because the new assertion required `layer_stack.auto_squash.depth_before` and `layer_stack.prepare_workspace_snapshot.total_s` under `performance_report.json -> sandbox.timing_keys`.
- Root-cause hypothesis and evidence:
  - `layer_stack.auto_squash.depth_before` is intentionally a non-duration observation, not a timing key. Fresh `performance_report.json` recorded it under `sandbox.non_duration_observations` with `max=101.0`.
  - This auto-squash write/read path emitted `workspace.mount_s` but not `layer_stack.prepare_workspace_snapshot.total_s`; that key is still enforced by the high-concurrency shell path where command-exec attribution is required.
- Code/test changes made:
  - Moved the auto-squash depth assertion to `sandbox.non_duration_observations`.
  - Removed `layer_stack.prepare_workspace_snapshot.total_s` from the auto-squash-specific required timing set.
  - Added `occ.apply.commit_queue_wait_s` to the auto-squash timing set.
- Correctness result:
  - Runtime completed the scenario, but pytest failed on the assertion mismatch. Rerun pending.
- Performance result from inspected failed-run artifacts:
  - O(1) lowerdir held in the failed-run artifacts: `resource.command_exec.workspace_tree_bytes max=0`, `workspace_tree_exists max=0`.
  - Auto-squash crossed threshold: `layer_stack.auto_squash.depth_before max=101.0`.
  - Commit-resume timing present: `occ.apply.commit_resume_wait_s max=0.0005350830033421516`.
- Next iteration entry point:
  - Rerun collect-only, then rerun the auto-squash test before returning to the full 3.1 command.

### Iteration 1.2 - auto-squash rerun

- Timestamp: 2026-05-24 19:00 CST.
- Exact commands run:
  - `uv run pytest --collect-only -q backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_auto_squash_commit_resume.py`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_auto_squash_commit_resume.py`
- Fresh artifact path inspected:
  - `.sweevo_runs/scenario_logs/sandbox.auto_squash_commit_resume/20260524T105248Z_6a897d7beaf0`
- First failure/stop signal:
  - none.
- Correctness result:
  - Auto-squash test passed: `1 passed in 180.17s`.
- Performance result:
  - `sandbox_events.jsonl` contained 113 resource snapshots, 1 `sandbox_layer_stack_layers_squashed` event, and 2 typed conflict events.
  - O(1) lowerdir held: `resource.command_exec.workspace_tree_bytes max=0`, `workspace_tree_exists max=0`.
  - Auto-squash threshold crossed: `layer_stack.auto_squash.depth_before max=101.0`.
  - Commit-resume wait present: `occ.apply.commit_resume_wait_s max=0.00024008398759178817`.
- Next iteration entry point:
  - Run the full 3.1 verification command again and inspect fresh artifacts for all four collected tests.

### Iteration 1.3 - heavy I/O gitignore route correction

- Timestamp: 2026-05-24 19:05 CST.
- Exact command run:
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/`
- Fresh artifact paths inspected:
  - `.sweevo_runs/scenario_logs/sandbox.auto_squash_commit_resume/20260524T105619Z_f79c3a02efd7`
  - `.sweevo_runs/scenario_logs/sandbox.occ_concurrent_conflicts/20260524T105909Z_7c93573ee9c1`
  - `.sweevo_runs/scenario_logs/sandbox.heavy_io_zoned_concurrent/20260524T105921Z_cfd153ed8702`
- First failure/stop signal:
  - `test_heavy_io_zoned_concurrent` failed because the new assertion expected gitignored `build/perf_load_*` writes to produce zero workspace changed paths, but the fresh summary reported `gitignored.workspace_changed_paths=55`.
- Root-cause hypothesis and evidence:
  - The assertion was stricter than the shipped OCC contract. Existing OCC policy routes gitignored paths through the direct path, not the gated path, so they are still workspace changed paths.
  - Fresh `performance_report.json` samples show tracked zone writes with `occ.commit.gated_path_count=11` and `direct_path_count=0`, while gitignored zone writes show `direct_path_count=11` and `gated_path_count=0`.
  - Outside `/tmp` writes still produced zero workspace changed paths, which is the plan's hard no-leak requirement.
- Code/test changes made:
  - Replaced the zero-gitignored-changed-path assertion with a route-filtering assertion: tracked shell writes must be gated; gitignored shell writes must be direct; outside shell writes must not appear in changed paths.
- Correctness result:
  - Full gate stopped after `1 failed, 2 passed in 316.63s`; heavy I/O runtime completed, but assertion semantics were corrected. Rerun pending.
- Performance result from inspected failed-run artifacts:
  - Heavy I/O O(1) lowerdir held: `workspace_tree_bytes max=0`, `workspace_tree_exists max=0`.
  - Payload upperdir bound observed: `upperdir_tree_bytes max=34603008`, matching 11 * 3 MiB.
  - Max changed path count observed: `changed_path_count max=11`.
- Next iteration entry point:
  - Rerun collect-only for the heavy I/O test, rerun the heavy I/O test, then return to the full 3.1 gate.

### Iteration 1.4 - heavy I/O rerun

- Timestamp: 2026-05-24 19:08 CST.
- Exact commands run:
  - `uv run pytest --collect-only -q backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_heavy_io_zoned_concurrent.py`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_heavy_io_zoned_concurrent.py`
- Fresh artifact path inspected:
  - `.sweevo_runs/scenario_logs/sandbox.heavy_io_zoned_concurrent/20260524T110257Z_641b00d986f0`
- First failure/stop signal:
  - none.
- Correctness result:
  - Heavy I/O test passed: `1 passed in 134.14s`.
- Performance result:
  - O(1) lowerdir held during the run: `workspace_tree_bytes max=0`, `workspace_tree_exists max=0`.
  - Upperdir max matched payload scale: `upperdir_tree_bytes max=34603008`.
  - Max per-call changed paths remained bounded by the chunk count: `changed_path_count max=11`.
  - Tracked writes asserted as gated and gitignored writes asserted as direct-route OCC changes.
- Next iteration entry point:
  - Run the high-concurrency test directly, then run the full 3.1 verification command.

### Iteration 1.5 - high-concurrency timing audit field correction

- Timestamp: 2026-05-24 19:17 CST.
- Exact command run:
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py`
- Fresh artifact path inspected:
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T110536Z_af75326d8e80`
- First failure/stop signal:
  - `test_high_concurrency_layerstack_overlay_occ_capacity` failed because the fresh `performance_report.json` lacked plan-facing timing keys: `command_exec.mount_workspace_s`, `command_exec.run_command_s`, `command_exec.capture_upperdir_s`, `layer_stack.prepare_workspace_snapshot.total_s`, and `api.shell.total_s`.
- Root-cause hypothesis and evidence:
  - The runtime completed the scenario and emitted the current namespace timing vocabulary (`workspace.mount_s`, `workspace.tool_s`, `api.shell.dispatch_total_s`, and `occ.apply.*`), but the daemon overlay boundary no longer carried the plan-required `command_exec.*`, `layer_stack.prepare_workspace_snapshot.total_s`, or `api.shell.total_s` aliases into the sandbox audit artifact.
  - The same artifact already showed O(1) lowerdir behavior: `resource.command_exec.workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, and `upperdir_tree_bytes max=4096`.
- Code/runtime changes made:
  - Added `snapshot_timings` to `OverlayHandle` and populated it from `prepare_workspace_snapshot(...)` in `sandbox.overlay.lifecycle.acquire`.
  - Added daemon overlay timing aliases in `EphemeralPipeline.run_tool_call`: `workspace.mount_s -> command_exec.mount_workspace_s`, `workspace.tool_s -> command_exec.run_command_s`, measured `command_exec.capture_upperdir_s`, carried `layer_stack.prepare_workspace_snapshot.total_s`, and added `api.shell.total_s` for shell calls.
- Correctness result:
  - Post-fix collect-only passed for `test_high_concurrency_layerstack_overlay_occ.py`.
  - Targeted runtime unit checks passed: `15 passed in 0.17s`.
- Performance result:
  - Pending rerun with the new audit fields.
- Next iteration entry point:
  - Rerun the high-concurrency live test and inspect both timing-key presence and the write/edit p95 budget.

### Iteration 1.6 - file-verb mount bypass for write/edit latency

- Timestamp: 2026-05-24 19:31 CST.
- Exact command run:
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py`
- Fresh artifact path inspected:
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T111400Z_89ce1f49278d`
- First failure/stop signal:
  - Timing-key coverage passed, then `test_high_concurrency_layerstack_overlay_occ_capacity` failed on the write/edit p95 budget: `write_file p95 12572.650ms exceeds 1000ms budget`.
- Root-cause hypothesis and evidence:
  - O(1) disk still held: `workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=4096`.
  - The slow path was not OCC or capture: `occ.apply.total_s p95=0.044s`, `command_exec.capture_upperdir_s p95=0.002s`, and `workspace.tool_s p95=0.009s`.
  - The expensive part was unnecessary per-call namespace/overlay mounting for file-only verbs under high manifest depth: `workspace.mount_s p95=4.685s`, `command_exec.total_s p95=12.340s`, and mount p95 rose with `resource.layer_stack.manifest_depth` (`75..101` depth bucket p95 mount `4.964s`).
- Code/runtime changes made:
  - Added a direct layer-stack/OCC fast path for ephemeral `read_file`, `write_file`, and `edit_file` daemon dispatch.
  - The fast path preserves isolated-workspace routing, path binding validation, OCC/gitignore routing, edit conflicts, and public result shape, while leaving shell on the overlay namespace path.
  - Added `test_ephemeral_file_verbs_use_direct_occ_path` so file verbs fail if they mount an ephemeral overlay again.
- Correctness result:
  - Targeted unit checks passed: `22 passed in 0.24s`.
  - Ruff passed for touched runtime and test files.
  - High-concurrency collect-only still passes.
- Performance result:
  - Pending rerun with direct file verbs.
- Next iteration entry point:
  - Rerun the high-concurrency live test and inspect the updated write/edit p95 plus shell-only O(1) resource snapshots.

### Iteration 1.7 - high-concurrency direct file rerun

- Timestamp: 2026-05-24 19:31 CST.
- Exact commands run:
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py`
  - `uv run pytest -q backend/tests/unit_test/test_sandbox/test_daemon/test_file_dispatch.py backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_workspace_unification_phase2.py backend/tests/unit_test/test_sandbox/test_api/test_read.py backend/tests/unit_test/test_sandbox/test_api/test_write.py backend/tests/unit_test/test_sandbox/test_api/test_edit.py`
- Fresh artifact path inspected:
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T112923Z_338488c2a915`
- First failure/stop signal:
  - none.
- Correctness result:
  - High-concurrency live test passed: `1 passed in 38.72s`.
  - Targeted unit checks passed: `22 passed in 0.24s`.
- Performance result:
  - Write/edit p95 is under the 1000ms budget: `write_file p95_ms=468.474`, `edit_file p95_ms=482.365`.
  - Direct read is no longer mount-bound: `api.read.total_s p95=0.003787s`, `read_file p95_ms=130.012`.
  - O(1) lowerdir held across direct file and shell resource samples: `resource.command_exec.workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=4096`, `changed_path_count max=1`.
  - OCC queue wait is reported separately from mount time: `occ.apply.commit_queue_wait_s p95=0.442658s`; `workspace.mount_s` remains only on shell/overlay calls with p95 `3.973840s`.
- Remaining risk:
  - Shell overlay calls still have high wall-clock latency (`shell p95_ms=11912.836`) because they still require per-call namespace/overlay mounting at high manifest depth. That is outside the write/edit p95 budget fixed in this iteration, but it is visible in the artifact.
- Next iteration entry point:
  - Run the full 3.1 directory verification command.

### Iteration 1.8 - full 3.1 verification

- Timestamp: 2026-05-24 19:35 CST.
- Exact command run:
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/`
- Fresh artifact paths inspected:
  - `.sweevo_runs/scenario_logs/sandbox.auto_squash_commit_resume/20260524T113033Z_a8cf143af564`
  - `.sweevo_runs/scenario_logs/sandbox.occ_concurrent_conflicts/20260524T113044Z_e4e84c10bd00`
  - `.sweevo_runs/scenario_logs/sandbox.heavy_io_zoned_concurrent/20260524T113054Z_fab435416369`
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T113255Z_067ba632fd1c`
- First failure/stop signal:
  - none.
- Correctness result:
  - Full 3.1 gate passed: `4 passed in 179.18s`.
- Performance and resource result:
  - Auto-squash O(1) lowerdir held: `workspace_tree_bytes max=0`, `workspace_tree_exists max=0`; `layer_stack.auto_squash.depth_before max=101`.
  - Focused OCC O(1) lowerdir held: `workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=4096`.
  - Heavy I/O upperdir scales with payload, not repo: `workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=34603008`, `changed_path_count max=11`.
  - High-concurrency write/edit stayed below budget: `write_file p95_ms=412.474`, `edit_file p95_ms=384.092`.
  - High-concurrency direct reads stayed cheap: `api.read.total_s p95=0.005174s`.
  - High-concurrency O(1) lowerdir held over 569 resource samples: `workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=4096`.
  - OCC queue wait is separated from mount time in the passing high-concurrency artifact: `occ.apply.commit_queue_wait_s p95=0.365611s`, `occ.apply.total_s p95=0.395809s`, while shell-only `workspace.mount_s p95=3.944329s`.
- Remaining risk:
  - Shell is still the slow family under high manifest depth: high-concurrency `shell p95_ms=12248.191` and `api.shell.total_s p95=12.222627s`. The direct file-verb fix removes this cost from write/edit/read but does not optimize shell overlay mount latency.
- Final status:
  - Plan 3.1 is green for the required full directory command, with shell latency called out as a separate remaining performance risk.

## Iteration 2 - 2026-05-24 19:50:12 CST

- Checkout summary: `main` ahead of `origin/main` by 18 commits. This iteration continues from the Iteration 1 shell-latency risk and leaves unrelated worktree state untouched.
- Plan path: `docs/plans/3.1-layer-stack-occ-overlay-live-e2e-plan.md`.
- Target files:
  - `backend/src/sandbox/overlay/namespace_runner.py`
  - `backend/src/sandbox/ephemeral_workspace/pipeline.py`
  - `backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py`
- Dependency pivot: none; this stays on 3.1 layer-stack/OCC/overlay.
- Coverage gaps found against `3.1.x Complex Cases To Add`:
  - No new coverage gap in the 3.1 plan assertions. The remaining problem was the already-recorded shell overlay latency risk in the high-concurrency artifact.
- Tests, probes, or audit fields added:
  - Added async fresh-namespace child execution for shell overlay calls so the daemon event loop no longer spends shell concurrency on `asyncio.to_thread`.
  - Added `test_run_in_namespace_does_not_use_default_threadpool` to prevent reverting fresh namespace execution to the default executor.
  - Added shell pre-mount layer-stack maintenance with timing fields:
    - `layer_stack.shell_pre_mount_squash.total_s`
    - `layer_stack.shell_pre_mount_squash.max_depth`
    - `layer_stack.shell_pre_mount_squash.depth_before`
    - `layer_stack.shell_pre_mount_squash.depth_after`
    - `layer_stack.shell_pre_mount_squash.raced`
  - Added `test_shell_pre_mount_squashes_deep_manifest` to verify shell mounts trigger the narrower pre-mount squash path without changing the global `AUTO_SQUASH_MAX_DEPTH=100` OCC policy.
- Exact commands run:
  - `jq '.tools.per_tool.shell | {count, min_ms, mean_ms, p50_ms, p75_ms, p90_ms, p95_ms, p99_ms, max_ms}' .sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T113255Z_067ba632fd1c/performance_report.json`
  - `jq '[.tools.per_tool.shell.samples[] | {dur_ms:.duration_ms, depth:.timings_s["resource.layer_stack.manifest_depth"], total:.timings_s["command_exec.total_s"], mount:.timings_s["command_exec.mount_workspace_s"], run:.timings_s["command_exec.run_command_s"], occ:.timings_s["occ.apply.total_s"], dispatch:.timings_s["runtime.dispatch_s"], lock_wait:.timings_s["layer_stack.transaction.lock_wait_s"], queue:.timings_s["occ.apply.commit_queue_wait_s"], gap:(.timings_s["command_exec.total_s"] - ((.timings_s["command_exec.mount_workspace_s"] // 0) + (.timings_s["command_exec.run_command_s"] // 0) + (.timings_s["command_exec.capture_upperdir_s"] // 0) + (.timings_s["occ.apply.total_s"] // 0) + (.timings_s["layer_stack.prepare_workspace_snapshot.total_s"] // 0)))}] | sort_by(.dur_ms) | .[-8:]' .sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T113255Z_067ba632fd1c/performance_report.json`
  - `uv run pytest -q backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run ruff check backend/src/sandbox/overlay/namespace_runner.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run python -m py_compile backend/src/sandbox/overlay/namespace_runner.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run pytest -q backend/tests/unit_test/test_sandbox/test_daemon/test_file_dispatch.py backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_workspace_unification_phase2.py backend/tests/unit_test/test_sandbox/test_api/test_shell.py backend/tests/unit_test/test_sandbox/test_execution/test_subprocess_runner_cancel.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run pytest --collect-only -q backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py`
  - `uv run ruff check backend/src/sandbox/ephemeral_workspace/pipeline.py backend/src/sandbox/overlay/namespace_runner.py backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run pytest -q backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run python -m py_compile backend/src/sandbox/ephemeral_workspace/pipeline.py backend/src/sandbox/overlay/namespace_runner.py backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run pytest -q backend/tests/unit_test/test_sandbox/test_daemon/test_file_dispatch.py backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_workspace_unification_phase2.py backend/tests/unit_test/test_sandbox/test_api/test_shell.py backend/tests/unit_test/test_sandbox/test_execution/test_subprocess_runner_cancel.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py backend/tests/unit_test/test_sandbox/test_occ/test_auto_squash.py backend/tests/unit_test/test_sandbox/test_layer_stack/test_squash.py`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/`
- Fresh artifact paths inspected:
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T113255Z_067ba632fd1c`
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T114151Z_60c26067fd9e`
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T114602Z_96400d196029`
  - `.sweevo_runs/scenario_logs/sandbox.auto_squash_commit_resume/20260524T114700Z_f75a3b3e1c13`
  - `.sweevo_runs/scenario_logs/sandbox.occ_concurrent_conflicts/20260524T114712Z_fe2d3717ba34`
  - `.sweevo_runs/scenario_logs/sandbox.heavy_io_zoned_concurrent/20260524T114721Z_38f3ea06c206`
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T114922Z_5b01b6f0f723`
- First failure/stop signal:
  - No correctness failure. The performance stop signal was the previously green high-concurrency run still showing shell p95 `12248.191ms` and `api.shell.total_s p95=12.222627s`.
- Root-cause hypothesis and evidence:
  - Flock was not the bottleneck. In the final full-gate high-concurrency artifact, `layer_stack.transaction.lock_wait_s p95=0.000005958s`; in the pre-fix artifact, slow shell samples also had lock wait around microseconds.
  - The first hidden cost was default-executor admission: in `20260524T113255Z_067ba632fd1c`, slow shell samples had 5s to 9.5s of `command_exec.total_s` not explained by `mount_workspace_s`, `run_command_s`, capture, OCC, or snapshot prep.
  - Removing the threadpool hop exposed the real bottleneck: in `20260524T114151Z_60c26067fd9e`, the unexplained gap shrank, but `command_exec.mount_workspace_s p95` rose to `9.522299s` under unconstrained concurrent deep overlay mounts.
  - The durable bottleneck is kernel overlay mount time with many lowerdirs under shell concurrency, not OCC, capture, shell body time, or flock.
- Code/test/audit changes made:
  - Replaced fresh namespace `asyncio.to_thread(_run_namespace_entrypoint, ...)` with `asyncio.create_subprocess_exec(...)` plus async timeout/cancellation handling and process-group SIGTERM/SIGKILL escalation.
  - Added shell pre-mount layer-stack squash guarded by an `asyncio.Lock`, with default `EOS_SHELL_MOUNT_SQUASH_MAX_DEPTH=64` and `0` as an opt-out.
  - Kept global OCC auto-squash policy unchanged at `AUTO_SQUASH_MAX_DEPTH=100`.
- Correctness result:
  - Focused namespace tests passed: `3 passed in 0.15s`.
  - Focused pipeline/namespace tests passed: `9 passed in 0.16s`.
  - Broader daemon/pipeline/OCC/squash unit slice passed: `39 passed in 1.47s`.
  - Collect-only passed for the 3.1 folder: `4 tests collected in 0.08s`.
  - Targeted high-concurrency live test passed after pre-mount squash: `1 passed in 37.81s`.
  - Full 3.1 verification passed: `4 passed in 175.59s`.
- Performance result:
  - Final full-gate high-concurrency shell improved to `shell p95_ms=6823.677`, down from the earlier full-gate `12248.191ms`.
  - Final full-gate `api.shell.total_s p95=6.816633s`; `command_exec.mount_workspace_s p95=5.967819s`; `command_exec.run_command_s p95=1.740880s`; `command_exec.capture_upperdir_s p95=0.001649s`.
  - Shell pre-mount squash fired once in the full-gate high-concurrency run, reported as repeated audit observations: `depth_before=66`, `depth_after=8`, `max_depth=64`, `total_s=0.196020`.
  - High-concurrency direct file verbs remained under the 1000ms budget: `write_file p95_ms=521.209`, `edit_file p95_ms=422.845`, `read_file p95_ms=37.770`.
  - OCC queue wait remains separate from mount time: final high-concurrency `occ.apply.commit_queue_wait_s p95=0.455929s`.
  - O(1) lowerdir still held: final high-concurrency `resource.command_exec.workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=4096`.
  - Heavy I/O still scales with payload rather than repo size: final heavy run `workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=34603008`, `changed_path_count max=11`.
  - Auto-squash still crossed the global threshold: final auto-squash run `layer_stack.auto_squash.depth_before max=101`, `depth_after max=1`, `max_depth=100`.
- Remaining risk:
  - Shell overlay is materially faster but still mount-bound at p95 around 6s to 7s under high concurrency. A further reduction likely needs either a persistent shell namespace, deeper shell batching, or a lower shell-specific squash threshold after measuring checkpoint cost on larger workspaces.
- Next iteration entry point:
  - If shell p95 must drop below 5s, run a targeted matrix over `EOS_SHELL_MOUNT_SQUASH_MAX_DEPTH` values such as `32`, `48`, and `64` against `test_high_concurrency_layerstack_overlay_occ.py`, then decide whether the extra squash work is worth the mount-time reduction.

## Iteration 3 - 2026-05-24 20:23:21 CST

- User correction:
  - The previous fanout diagnosis confused audit-event duplication with real shell concurrency. The fix in this iteration does not add a daemon semaphore.
  - The high-concurrency scenario now keeps the workload to 20 workers and asserts the measured max overlapping sandbox tool calls is `<= 20`.
  - Worker shell calls were removed from the high-concurrency pressure path; shell is measured separately with an explicit 1/5/10 diagnostic matrix.
- Exact commands run:
  - `uv run ruff check backend/src/sandbox/audit/translation.py backend/src/sandbox/ephemeral_workspace/pipeline.py backend/src/sandbox/overlay/namespace_runner.py backend/src/task_center_runner/agent/mock/high_concurrency_probe.py backend/src/task_center_runner/scenarios/sandbox/high_concurrency_layerstack_overlay_occ.py backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_shell_concurrency_latency_matrix_diagnostic.py backend/tests/unit_test/test_sandbox/test_audit/test_operation.py backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run pytest -q backend/tests/unit_test/test_sandbox/test_audit/test_operation.py backend/tests/unit_test/test_sandbox/test_timing.py backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_overlay/test_namespace_runner_cancellation.py`
  - `uv run pytest --collect-only -q backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py`
  - `EOS_RUN_SHELL_LATENCY_MATRIX=1 EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -s -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_shell_concurrency_latency_matrix_diagnostic.py`
- Fresh artifact paths inspected:
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T121743Z_5815d20041ca`
  - `.sweevo_runs/manual_diagnostics/shell_concurrency_latency/20260524T122119Z.json`
- Test and audit changes:
  - Filtered subsystem audit event timing payloads by signal so `api.shell.total_s` is counted on overlay execution events, not repeated across every derived sandbox event.
  - Changed the high-concurrency test to compute overlap from tool sample start/end timestamps and assert `max_active <= WORKER_COUNT`.
  - Reduced the high-concurrency worker workload to direct file/OCC pressure and moved shell latency into the opt-in `test_shell_concurrency_latency_matrix_diagnostic.py` diagnostic.
  - Replaced the shell-based reconcile step with Python aggregation plus `read_file`/`write_file`, so the high-concurrency test no longer uses late shell calls at deep manifest depth.
- Correctness result:
  - Focused audit/pipeline/namespace unit slice passed: `19 passed in 0.18s`.
  - Targeted high-concurrency live test passed: `1 passed in 18.94s`.
  - Shell diagnostic matrix passed: `1 passed in 22.06s`.
  - The full 3.1 gate was intentionally stopped after user redirection; this iteration verifies the targeted high-concurrency scenario and shell latency diagnostic instead.
- High-concurrency result:
  - `max_overlapping_sandbox_tool_calls = 20`.
  - `shell count = 1`; the remaining shell call is the serial seed-directory setup.
  - `api.shell.total_s count=1`, `p95=0.824939s`.
  - Direct file verbs stayed under budget: `write_file p95_ms=154.573`, `edit_file p95_ms=170.718`, `read_file p95_ms=4.712`.
  - Flock is not the bottleneck: `layer_stack.transaction.lock_wait_s p95=0.000002125s`.
  - O(1) lowerdir still held: `resource.command_exec.workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=0`.
- Shell 1/5/10 diagnostic result:
  - Concurrency 1: `api.shell.total_s p95=0.835987s`, `mount_workspace_s p95=0.166376s`, `run_command_s p95=0.381419s`, `occ.apply.total_s p95=0.019884s`, residual timing gap about `0.267719s`.
  - Concurrency 5: `api.shell.total_s p95=1.145027s`, `mount_workspace_s p95=0.285457s`, `run_command_s p95=0.483031s`, `occ.apply.total_s p95=0.019753s`, residual timing gap about `0.355588s`.
  - Concurrency 10: `api.shell.total_s p95=2.415451s`, `mount_workspace_s p95=0.881401s`, `run_command_s p95=1.105861s`, `occ.apply.total_s p95=0.046626s`, residual timing gap about `0.377332s`.
  - OCC queue and layer-stack locks remain small even at concurrency 10: `occ.apply.commit_queue_wait_s p95=0.031982s`, `layer_stack.transaction.lock_wait_s p95=0.000208s`.
- Current bottleneck assessment:
  - Shell at concurrency 1 and 5 is inside the user's acceptable 1s to 2s range.
  - Concurrency 10 exceeds the acceptable range because both overlay mount time and in-namespace command runtime grow under the burst. OCC, commit queueing, capture, snapshot prep, and flock are not the material source.
  - The remaining residual gap is roughly 0.27s to 0.38s per call and should be instrumented separately only if the 10-way shell path must be optimized below 2s.

## Iteration 4 - 2026-05-24 20:29:21 CST

- User correction:
  - The high-concurrency live test should be bounded to 5 active sandbox tool calls.
  - This remains a test/scenario bound, not a daemon-wide semaphore.
- Exact commands run:
  - `uv run ruff check backend/src/task_center_runner/scenarios/sandbox/high_concurrency_layerstack_overlay_occ.py backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py backend/src/task_center_runner/tests/mock/contracts/test_context_message_scenarios.py`
  - `uv run pytest -q backend/src/task_center_runner/tests/mock/contracts/test_context_message_scenarios.py`
  - `uv run pytest --collect-only -q backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q -x --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/layer_stack_occ_overlay/test_high_concurrency_layerstack_overlay_occ.py`
  - `git diff --check`
- Fresh artifact path inspected:
  - `.sweevo_runs/scenario_logs/sandbox.high_concurrency_layerstack_overlay_occ/20260524T122851Z_bb4e91827906`
- Skill/test changes:
  - Updated `.agents/skills/live-e2e-plan-executor/SKILL.md` to require the 3.1 high-concurrency scenario to distinguish total workload size from active fanout and keep recurring high-concurrency runs bounded to 5 active worker lanes unless the user explicitly asks for more.
  - Added `MAX_CONCURRENT_WORKERS = 5` to the high-concurrency scenario.
  - Changed the scenario DAG from 20 workers all depending on the seed to 20 workers arranged in five dependency lanes: workers `0..4` depend on seed; worker `N` for `N>=5` depends on worker `N-5`.
  - Updated the live test assertion to require observed max overlapping sandbox tool calls `<= MAX_CONCURRENT_WORKERS`.
  - Added a contract test for the five-lane DAG shape.
- Correctness result:
  - Contract tests passed: `7 passed in 0.08s`.
  - Collect-only passed for the 3.1 folder: `5 tests collected in 0.12s`.
  - Targeted high-concurrency live test passed: `1 passed in 20.18s`.
- Performance/resource result:
  - Computed max overlapping sandbox tool calls from `performance_report.json` samples: `5`.
  - Tool p95 stayed well under the 1000ms write/edit budget: `write_file p95_ms=50.541`, `edit_file p95_ms=54.055`, `read_file p95_ms=6.309`.
  - The single serial shell setup stayed inside the acceptable shell range: `shell p95_ms=931.726`, `api.shell.total_s p95=0.926040s`.
  - Flock remains non-material: `layer_stack.transaction.lock_wait_s p95=0.000002916s`.
  - O(1) lowerdir held: `resource.command_exec.workspace_tree_bytes max=0`, `workspace_tree_exists max=0`, `upperdir_tree_bytes max=0`.
- Remaining risk:
  - The recurring high-concurrency test is now bounded to 5 active worker lanes. The separate opt-in shell diagnostic can still exercise 10-way shell bursts when explicitly requested, but those numbers should not be treated as the recurring high-concurrency gate.
