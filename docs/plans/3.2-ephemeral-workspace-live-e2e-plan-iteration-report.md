# 3.2 Ephemeral Workspace Live E2E Iteration Report

## Iteration 1 - 2026-05-24 20:44:49 CST

- Checkout summary: `bc2927a01`; worktree already contained no 3.2 iteration report. A pre-existing 3.1 report modification was observed earlier and left untouched.
- Plan path: `docs/plans/3.2-ephemeral-workspace-live-e2e-plan.md`.
- Target files:
  - `backend/src/task_center_runner/agent/mock/ephemeral_workspace_probe.py`
  - `backend/src/task_center_runner/scenarios/sandbox/ephemeral_workspace.py`
  - `backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/`
  - `backend/src/sandbox/daemon/dispatch.py`
  - result/audit metadata surfaces under `backend/src/sandbox`, `backend/src/tools/sandbox`, and `backend/src/task_center_runner/audit`
- Dependency pivot: none so far. 3.1 evidence was consulted only for local scenario/audit patterns.
- Coverage gaps found against `3.2.x Complex Cases To Add`:
  - No existing 3.2 `ephemeral_workspace` mock scenario folder.
  - No `ephemeral_workspace_probe.py`.
  - No scenario registry entries for the required ephemeral workspace modes.
  - Current daemon dispatch still shortcut `read_file`, `write_file`, and `edit_file` through direct layer-stack/OCC code instead of the per-call workspace pipeline required by the plan.
  - Existing sandbox event translation did not include `grep` / `glob` or mutation-source / changed-path-kind evidence.
- Tests, probes, or audit fields added:
  - Added probe modes for all verbs, concurrent disjoint writes, same-path conflict/retry, outside-workspace policy, cancellation, and 100-call O(1) disk pressure.
  - Added scenario registry entries for the five required plan names plus `sandbox.ephemeral_workspace_same_path_conflict` to cover 3.2.3 explicitly.
  - Added live tests for all six complex cases.
  - Added `mutation_source`, `changed_path_kinds`, typed `error`, and `sandbox_tool_cancelled` artifact evidence.
- Commands run so far:
  - `sed -n '1,260p' docs/plans/3.2-ephemeral-workspace-live-e2e-plan.md`
  - `rg -n "ephemeral_workspace|ephemeral workspace|ephemeral_workspace_all_verbs|workspace_mode|summary\\.json" backend/src/task_center_runner backend/tests docs/plans`
  - `rg --files backend/src/task_center_runner | rg 'tests/mock/sandbox|agent/mock|scenarios|SCENARIO|registry|sandbox/.*/.*probe|_probe\\.py'`
  - targeted `sed`/`rg` reads across sandbox dispatch, pipeline, probes, scenarios, and test helpers
- Fresh artifact or isolated-audit paths inspected: none yet; live execution has not started.
- First failure/stop signal: none yet.
- Root-cause hypothesis and evidence: The 3.2 plan requires the same fresh overlay pipeline for all default foreground verbs. `backend/src/sandbox/daemon/dispatch.py` routed only shell/grep/glob through `resolve_pipeline`, while read/write/edit used direct layer-stack/OCC helpers, so file verbs could not produce per-call overlay cleanup or mutation-source evidence.
- Code/test/audit changes made:
  - Routed all foreground verbs through `resolve_pipeline(...).run_tool_call(...)`.
  - Added result and audit metadata for mutation source, changed path kinds, and typed daemon errors.
  - Added cancellation event publication from the mock runner when it cancels an invocation-keyed sandbox request.
  - Added 3.2 probe, scenario classes, registry entries, runner dispatch, and live tests.
- Correctness result: pending verification.
- Performance result: pending verification; no O(1) or latency verdict yet.
- Next iteration entry point: run collect-only for `backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/`, then targeted unit checks for daemon dispatch/pipeline metadata, then the plan verification command while monitoring `.sweevo_runs/scenario_logs`.

## Iteration 2 - 2026-05-24 21:17:16 CST

- Checkout summary: `bc2927a01`; same working tree, continuing the 3.2 implementation from Iteration 1.
- Plan path: `docs/plans/3.2-ephemeral-workspace-live-e2e-plan.md`.
- Target files:
  - `backend/src/task_center_runner/agent/mock/ephemeral_workspace_probe.py`
  - `backend/src/task_center_runner/scenarios/sandbox/ephemeral_workspace.py`
  - `backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/`
  - `backend/src/sandbox/daemon/dispatch.py`
  - `backend/src/sandbox/ephemeral_workspace/pipeline.py`
  - `backend/src/sandbox/overlay/namespace_entrypoint.py`
  - `backend/src/sandbox/host/daemon_client.py`
  - `backend/src/sandbox/occ/service.py`
  - sandbox tool/audit metadata adapters under `backend/src/tools/sandbox`, `backend/src/sandbox/audit`, and `backend/src/task_center_runner/audit`
- Dependency pivot: none.
- Coverage gaps found against `3.2.x Complex Cases To Add`: Iteration 1 added the missing coverage. Iteration 2 found assertion/evidence gaps only: per-call cleanup counted warm LSP overlay directories, typed daemon policy errors were lost by host transport exception mapping, and denied host-path writes short-circuited before preserving mount timing evidence.
- Tests, probes, or audit fields added:
  - Added direct per-call routing and live coverage for read/write/edit/grep/glob/shell, concurrent disjoint writes, same-path conflict/retry, outside-workspace policy, cancellation, and 100-call O(1) disk pressure.
  - Added `changed_path_kinds`, `mutation_source`, typed `error.kind`, `sandbox_tool_cancelled`, and API timing aliases such as `api.read.total_s`, `api.write.total_s`, `api.edit.total_s`, `api.grep.total_s`, and `api.glob.total_s`.
  - Added daemon-client regression coverage for guarded error responses with `status`.
- Commands run:
  - `uv run pytest --collect-only backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace -q`
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_daemon/test_file_dispatch.py -q`
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_workspace_unification_phase2.py -q`
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_api/test_write.py backend/tests/unit_test/test_sandbox/test_api/test_shell.py backend/tests/unit_test/test_sandbox/test_daemon/test_search_handler.py -q`
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_occ/test_auto_squash.py -q`
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_api/test_daemon_client.py backend/tests/unit_test/test_sandbox/test_api/test_write.py -q`
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_workspace_unification_phase2.py backend/tests/unit_test/test_sandbox/test_command_exec/test_out_of_workspace_passthrough.py backend/tests/unit_test/test_sandbox/test_api/test_daemon_client.py -q`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q --tb=short -x -s backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/test_ephemeral_outside_workspace_policy.py`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/`
  - `uv run ruff check $(git diff --name-only --diff-filter=ACM | tr '\n' ' ')`
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_api/test_daemon_client.py backend/tests/unit_test/test_sandbox/test_api/test_write.py backend/tests/unit_test/test_sandbox/test_api/test_shell.py backend/tests/unit_test/test_sandbox/test_daemon/test_file_dispatch.py backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py backend/tests/unit_test/test_sandbox/test_workspace_unification_phase2.py backend/tests/unit_test/test_sandbox/test_command_exec/test_out_of_workspace_passthrough.py backend/tests/unit_test/test_sandbox/test_occ/test_auto_squash.py -q`
- Fresh artifact paths inspected:
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_all_verbs/20260524T131431Z_9f1a803d4583`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_cancellation/20260524T131450Z_cbc18509f3cb`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_concurrent_writes/20260524T131501Z_530ea4a19b81`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T131517Z_3f82c5d112ee`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_policy/20260524T131622Z_ca2cd0f363f7`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_same_path_conflict/20260524T131633Z_3e548dcd32ae`
  - Earlier failure artifacts inspected during the stop/fix loop:
    - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_all_verbs/20260524T124827Z_70d00f18f02b`
    - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T125847Z_b57715c4ff7e`
    - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T130237Z_c5e3754f049a`
    - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_policy/20260524T130847Z_a1e05a6c390e`
- First failure/stop signals:
  - `all_verbs`: cleanup assertion falsely counted warm `lsp-session-*` overlay directories as command overlay leaks.
  - `all_verbs`: shell payload attempted blocked `rm -rf /testbed...`, then kernel-level lowerdir directory removal produced `EIO`; symlink changes in tracked paths also failed OCC policy.
  - `all_verbs`: `performance_report.json` lacked API timing aliases for read/write/edit/grep/glob.
  - `o1_disk`: warm `read_file` p95 reached about `1752 ms` after manifest depth grew to about `68`.
  - `o1_disk`: after auto-squash, the probe still assumed one manifest version per mutation and failed on squash checkpoints.
  - `policy`: daemon correctly denied system writes, but `_DaemonDispatchError` stripped `forbidden_host_path` from tool metadata; after that fix, denied writes still missed mount timing because the namespace denylist returned before the shared timing attachment.
- Root-cause hypothesis and evidence:
  - The 3.2 plan requires all default foreground verbs to traverse the same ephemeral per-call overlay lifecycle. Direct read/write/edit dispatch and exception-level daemon error handling hid the behavior that live artifacts must assert.
  - O(1) disk was correct (`workspace_tree_bytes` remained zero), but manifest depth was not O(1) enough for the latency budget until auto-squash was made aggressive for this path.
- Code/test/audit changes made:
  - Routed foreground read/write/edit through `resolve_pipeline(...).run_tool_call(...)` like shell/grep/glob.
  - Preserved `changed_path_kinds`, `mutation_source`, and typed errors through daemon response parsing, tool result payloads, and sandbox event translation.
  - Added API timing alias projection to performance reports for all foreground verbs.
  - Added cancellation event publication from mock runner cancellation cleanup.
  - Changed O1 pressure expectations to account for auto-squash checkpoint versions and lowered `AUTO_SQUASH_MAX_DEPTH` to `4`.
  - Changed all-verbs fixtures to use ignored scratch paths for direct-route symlink/opaque evidence and to assert command overlay run-dir cleanup separately from warm LSP runtime directories.
  - Changed namespace denylist responses to receive the same post-processing timing fields as normal verb bodies.
  - Changed daemon client response handling so guarded handler-level errors with a result `status` return as data, while transport/daemon dispatcher errors still raise.
- Correctness result: passed. The exact full 3.2 verification command completed `6 passed in 144.64s`.
- Performance result:
  - O(1) lowerdir verdict: passed. In `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T131517Z_3f82c5d112ee`, max `resource.command_exec.workspace_tree_exists` was `0.0` and max `resource.command_exec.workspace_tree_bytes` was `0.0`.
  - Manifest-depth verdict: passed after auto-squash. Max `resource.layer_stack.manifest_depth` was `4.0`.
  - Upper/run-dir verdict: passed for changed-data scale. Max `upperdir_tree_bytes` was `16384.0`; max `run_dir_tree_bytes` was `45056.0`.
  - Latency verdict: passed for warm read budget; `read_file` p95 was `488.289 ms`. `edit_file` p95 was `500.385 ms`; `write_file` p95 was `610.782 ms`, which is over 500 ms but not the plan's previously failing warm read/grep/glob gate. The full suite still passed its explicit performance assertions.
- Next iteration entry point: proceed to 3.3 only after reviewing whether `AUTO_SQUASH_MAX_DEPTH = 4` should remain global or become policy-configured for future heavier write/edit throughput scenarios.

## Iteration 3 - 2026-05-25 02:21:46 CST

- Checkout summary: `bc2927a01`; same working tree, continuing after the user challenged the `AUTO_SQUASH_MAX_DEPTH = 4` workaround.
- Plan path: `docs/plans/3.2-ephemeral-workspace-live-e2e-plan.md`.
- Target files:
  - `backend/src/sandbox/overlay/mount_syscalls.py`
  - `backend/tests/unit_test/test_sandbox/test_execution/test_overlay/test_mount_syscalls.py`
  - `backend/src/sandbox/occ/service.py`
  - `backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/test_ephemeral_lowerdir_disk_is_o1_under_100_calls.py`
- Dependency pivot: none.
- Coverage gaps found against `3.2.x Complex Cases To Add`: no new scenario gap. The gap was diagnostic: Iteration 2 treated depth reduction as the fix before isolating the mount syscall wrapper cost.
- First failure/stop signal:
  - The failing O1 artifact `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T125847Z_b57715c4ff7e` had max `resource.layer_stack.manifest_depth = 69.0`, `command_exec.mount_workspace_s` p95 `1.491 s`, max `1.632 s`, and tool p95s around `1.70-1.75 s`.
  - The low-depth workaround artifact `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T131517Z_3f82c5d112ee` lowered max depth to `4.0` and mount p95 to `0.227 s`, but that did not explain why pre-migration 100-layer mounting had been faster.
- Root-cause hypothesis and evidence:
  - `kernel_mount.mount_overlay()` calls `fsconfig_string(..., "lowerdir+", ...)` once per lower layer.
  - After the mount(2) migration, each syscall wrapper called `_get_libc()`, and `_get_libc()` re-ran `ctypes.util.find_library("c")` plus `ctypes.CDLL(...)` on every lowerdir append.
  - A Docker runtime microbench reproduced the production-wrapper slope before the fix: depth 1 averaged about `0.159 s`, depth 50 about `1.233 s`, depth 70 about `1.660 s`, and depth 100 about `2.328 s`.
  - The same benchmark with `_get_libc` cached reduced depth 100 to about `0.00044 s` for the syscall wrapper loop. Raw cached syscall mounting was also sub-millisecond at 100 lowerdirs, so the kernel layerstack algorithm was not the slow path.
- Code/test/audit changes made:
  - Added `@cache` to `sandbox.overlay.mount_syscalls._get_libc`.
  - Added regression coverage proving repeated syscall wrappers reuse one libc lookup while still issuing each syscall.
  - Restored `AUTO_SQUASH_MAX_DEPTH` to `100`.
  - Changed the O1 disk test to keep `auto_squash_count` as telemetry instead of requiring auto-squash to happen during the 100-call run.
- Commands run:
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_execution/test_overlay/test_mount_syscalls.py backend/tests/unit_test/test_sandbox/test_execution/test_overlay/test_kernel_mount.py -q`
  - `uv run pytest backend/tests/unit_test/test_sandbox/test_occ/test_auto_squash.py -q`
  - `uv run ruff check backend/src/sandbox/overlay/mount_syscalls.py backend/src/sandbox/occ/service.py backend/tests/unit_test/test_sandbox/test_execution/test_overlay/test_mount_syscalls.py backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/test_ephemeral_lowerdir_disk_is_o1_under_100_calls.py`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q --tb=short -x -s backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/test_ephemeral_lowerdir_disk_is_o1_under_100_calls.py`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/`
- Fresh artifact paths inspected:
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T182005Z_1511d4132d4b`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_all_verbs/20260524T182213Z_52e574d499bb`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_cancellation/20260524T182229Z_c49c17d27cd2`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_concurrent_writes/20260524T182240Z_c7363470e658`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T182254Z_62924709509e`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_policy/20260524T182342Z_022ac215f768`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_same_path_conflict/20260524T182356Z_ad3e0534b04e`
- Correctness result:
  - Overlay syscall/kernel mount unit checks: `30 passed, 1 skipped in 0.11s`.
  - OCC auto-squash checks: `6 passed in 0.31s`.
  - Ruff on edited files: passed.
  - Focused O1 live scenario: `1 passed in 57.80s`.
  - Full 3.2 ephemeral-workspace folder: `6 passed in 124.81s`.
- Performance result:
  - Focused O1 artifact `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T182005Z_1511d4132d4b`: max `manifest_depth = 69.0`, `command_exec.mount_workspace_s` p95 `0.026 s`, max `0.028 s`, and `command_exec.total_s` p95 `0.298 s`.
  - Full-suite O1 artifact `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T182254Z_62924709509e`: max `manifest_depth = 69.0`, `command_exec.mount_workspace_s` p95 `0.032 s`, max `0.041 s`, and tool p95s `read_file = 367.756 ms`, `write_file = 383.226 ms`, `edit_file = 365.842 ms`.
  - O(1) disk verdict remains passed: `workspace_tree_bytes` stayed `0.0`, no command overlay run-dir leaks were observed, and the manifest grew without requiring global squash at depth `4`.
- Next iteration entry point: keep `AUTO_SQUASH_MAX_DEPTH = 100` unless a future scenario proves an independent correctness or resource ceiling. If latency regresses again, inspect wrapper/setup overhead before lowering layer depth.

## Iteration 4 - 2026-05-25 03:41:51 CST

- Checkout summary: `bc2927a01`; same working tree, cleanup after the user chose the direct fast path for in-workspace file verbs.
- Plan update:
  - Updated `docs/plans/3.2-ephemeral-workspace-live-e2e-plan.md` so the current contract is explicit: in-workspace `read_file`, `write_file`, and `edit_file` use the direct layer-stack/OCC fast path; shell/search and outside-workspace file operations use the per-call overlay path.
- Review findings fixed:
  - The 3.2 helper still treated direct file verbs as if they always produced `command_exec.total_s`; O1 warm p95 now reads the tool-specific `api.read.total_s`, `api.write.total_s`, or `api.edit.total_s` when present.
  - Direct-only scenarios no longer require overlay timing keys in `performance_report.json`.
  - `run_ephemeral_scenario()` now awaits `report.performance_report_task` before reading `performance_report.json`, removing a race that cancelled the async report writer during the policy test.
  - The outside-workspace policy case remains overlay-backed by dispatch fallback and still verifies typed `forbidden_host_path` errors.
- Exact commands run:
  - `uv run ruff check $(git diff --name-only --diff-filter=ACM -- '*.py') $(git ls-files --others --exclude-standard -- '*.py')`
  - `uv run pytest --collect-only -q backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q --tb=short -x -s backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/test_ephemeral_outside_workspace_policy.py`
  - `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0 EOS_SANDBOX_PROVIDER=docker EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest -q --tb=short --durations=20 backend/src/task_center_runner/tests/mock/sandbox/ephemeral_workspace/`
  - `git diff --check`
- Fresh artifact paths inspected:
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_all_verbs/20260524T194033Z_0e0dfe21bca3`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_cancellation/20260524T194047Z_7b51a7cc1ca6`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_concurrent_writes/20260524T194056Z_8547191f77c1`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T194105Z_ef764d8aae59`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_policy/20260524T194119Z_c32cc9d457b3`
  - `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_same_path_conflict/20260524T194129Z_31e32989b6ee`
- Correctness result:
  - Focused policy live scenario passed after the report-task await fix: `1 passed in 19.10s`.
  - Full 3.2 ephemeral-workspace folder passed: `6 passed in 72.98s`.
  - Ruff, collect-only, and `git diff --check` passed.
- Performance result:
  - Full-suite O1 artifact `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_o1_disk/20260524T194105Z_ef764d8aae59`: `read_file p95=4.861ms`, `write_file p95=19.369ms`, `edit_file p95=16.281ms`.
  - O1 disk verdict remains passed: `workspace_tree_bytes max=0.0`, `workspace_tree_exists max=0.0`, and `upperdir_tree_exists max=0.0` for direct file calls.
  - Full-suite policy artifact `.sweevo_runs/scenario_logs/sandbox.ephemeral_workspace_policy/20260524T194119Z_c32cc9d457b3`: `/etc` read and `/tmp`/denylisted writes stayed overlay-backed, with `command_exec.mount_workspace_s p95=0.025s`, `read_file p95=274.128ms`, and `write_file p95=294.440ms` including four expected denied-write errors.
- Next iteration entry point: the 3.2 suite now reflects the hybrid contract. If future work needs direct API timing keys in sandbox subsystem reports, add a first-class direct-file audit signal instead of reusing overlay timing assertions.
