# 3.7 Full Stack Live E2E Plan Iteration Report

## 2026-05-25T10:41:41+08:00

- Checkout summary: `main` at `20667d430`; dirty files for this iteration were `backend/src/task_center_runner/agent/mock/full_stack_tool_scripts.py`, `backend/src/task_center_runner/audit/performance_report.py`, `backend/src/task_center_runner/scenarios/full_stack_adversarial.py`, `backend/src/task_center_runner/tests/mock/sandbox/full_stack/test_full_stack_adversarial.py`, and `backend/tests/unit_test/test_benchmarks/test_sweevo_audit_recorder.py`. An unrelated untracked project-build test file was present and left untouched.
- Plan path: `docs/plans/3.7-full-stack-live-e2e-plan.md`.
- Target files: `backend/src/task_center_runner/tests/mock/sandbox/full_stack/test_full_stack_adversarial.py`, `backend/src/task_center_runner/agent/mock/full_stack_tool_scripts.py`, `backend/src/task_center_runner/scenarios/full_stack_adversarial.py`, `backend/src/task_center_runner/audit/performance_report.py`, `backend/tests/unit_test/test_benchmarks/test_sweevo_audit_recorder.py`.
- Dependency pivot: none. The 3.7 full-stack scenario ran through the live SWE-EVO path; failures were assertion/report-location issues in the new 3.7 checks.
- Coverage gaps found against 3.7 complex cases:
  - 3.7.1 was missing explicit `performance_report.json` existence/schema checks, per-family sample checks, O(1) lowerdir resource assertions, and forbidden sandbox failure signature scans.
  - 3.7.2 was missing focused checks that recursive tasks used sandbox reads/writes, that foreground sandbox p95 stayed under the post-unify baseline, and that the final verifier phase read committed workspace state.
  - The scenario had READ_ONLY LSP calls but no WRITE_ALLOWED plugin call sample in the full-stack report.
- Tests, probes, and audit fields added:
  - Added `lsp.apply_workspace_edit` to the full-stack LSP tool script and matrix cell list, followed by a normal `read_file` readback.
  - Added full-stack performance assertions for required tool samples, sample-level direct API timings, daemon-event overlay/OCC/layer-stack timings, O(1) `workspace_tree_bytes`/`workspace_tree_exists`, cgroup run-delta metadata, recursive read/write task usage, final verifier readback, and forbidden sandbox event text.
  - Updated `performance_report.py` so cumulative cgroup CPU and I/O resource counters are reported as `source=run_delta` with `first_lifetime`, `latest_lifetime`, and delta `latest` values.
  - Extended the audit-recorder unit test to cover cgroup run-delta reporting.
- Commands run:
  - `uv run pytest -q backend/tests/unit_test/test_benchmarks/test_sweevo_audit_recorder.py::test_write_performance_reports_produces_detailed_report` -> passed.
  - `EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db" uv run pytest --collect-only -q backend/src/task_center_runner/tests/mock/sandbox/full_stack/` -> passed, 2 tests collected.
  - `uv run ruff check backend/src/task_center_runner/agent/mock/full_stack_tool_scripts.py backend/src/task_center_runner/scenarios/full_stack_adversarial.py backend/src/task_center_runner/tests/mock/sandbox/full_stack/test_full_stack_adversarial.py backend/src/task_center_runner/audit/performance_report.py backend/tests/unit_test/test_benchmarks/test_sweevo_audit_recorder.py` -> passed after each assertion update.
  - Plan verification command with `EOS_SWEEVO_INSTANCE=dask__dask_2023.3.2_2023.4.0`, `EOS_SANDBOX_PROVIDER=docker`, `EOS__RUNNER__LIVE_E2E__HEAVY_ENABLED=true`, `EPHEMERALOS_DATABASE_URL="sqlite:///./.ephemeralos/ephemeralos.db"` over `backend/src/task_center_runner/tests/mock/sandbox/full_stack/` -> first completed live run failed on over-specific `api.write.occ_apply_s` sample assertion.
  - Same plan verification command -> second completed live run failed on over-specific `api.write.total_s` daemon timing-key assertion.
  - Same plan verification command -> third retry was stopped after several minutes with no new full-stack run directory; another live project-build pytest process was active in the workspace, and the retry did not produce fresh artifacts.
  - `uv run python - <<'PY' ... _assert_full_stack_performance_report_complete(Path(".sweevo_runs/scenario_logs/full_stack_adversarial/20260525T023450Z_7e6f9b9847d5")) ... PY` -> passed against the fresh completed live artifact after the assertion fix.
- Fresh artifacts inspected:
  - `.sweevo_runs/scenario_logs/full_stack_adversarial/20260525T023318Z_6f1ce37826ff`
  - `.sweevo_runs/scenario_logs/full_stack_adversarial/20260525T023450Z_7e6f9b9847d5`
  - Inspected `run.json`, `metrics.json`, `performance_report.json`, `sandbox_events.jsonl`, and message logs under the latest run.
- First failure/stop signal:
  - First live failure: `write_file samples missing timing keys: ['api.write.occ_apply_s']`.
  - Second live failure: `missing sandbox timing keys: ['api.write.total_s']`.
  - Third retry stop: no new run directory after several minutes, with an unrelated `project_build` live pytest still active.
- Root-cause hypothesis and evidence:
  - Direct file operations currently expose total direct API latency in per-tool samples and shared OCC timings as `occ.apply.*`; they do not consistently expose old `api.*.occ_apply_s` aliases.
  - Daemon-event `sandbox.timing_keys` is the right source for overlay/OCC/layer-stack timing attribution, but direct API totals such as `api.write.total_s` live in per-tool samples for this full-stack run.
  - Fresh artifact evidence: latest run `20260525T023450Z_7e6f9b9847d5` has `lsp.apply_workspace_edit`, `read_file`, `write_file`, `edit_file`, `shell`, and all required LSP tools in `performance_report.json`; direct file samples include `api.*.total_s`; sandbox timing keys include `command_exec.*`, `occ.apply.*`, and `layer_stack.prepare_workspace_snapshot.total_s`.
- Code/test/audit changes made:
  - Added the full-stack plugin WRITE_ALLOWED step.
  - Added report-completeness and recursive-responsiveness assertions.
  - Added cgroup run-delta resource reporting.
  - Adjusted assertions to current route-specific evidence locations rather than requiring stale aliases.
- Correctness result:
  - Live workflow itself completed in both fresh runs and produced `task_center_status=done`.
  - Latest completed live artifact `20260525T023450Z_7e6f9b9847d5` passed the final 3.7 artifact assertions after the assertion-location fix.
  - The final exact plan pytest command was not green after the last patch because the retry was stopped before it produced a new run directory.
- Performance result:
  - O(1) lowerdir verdict passed on latest artifact: `workspace_tree_bytes.max=0.0`, `workspace_tree_exists.max=0.0`.
  - Foreground sandbox p95 verdict passed on latest artifact: `shell=855.354ms`, `read_file=5.478ms`, `write_file=22.517ms`, `edit_file=22.299ms`, `lsp.apply_workspace_edit=400.236ms`, all under the 1000ms baseline.
  - Latency attribution is present by route: direct file samples carry `api.read.total_s`, `api.write.total_s`, `api.edit.total_s`, and `occ.apply.total_s`; shell/overlay events carry `command_exec.mount_workspace_s`, `command_exec.run_command_s`, `command_exec.capture_upperdir_s`, and `command_exec.total_s`; plugin WRITE_ALLOWED sample carries `command_exec.capture_upperdir_s` and `command_exec.occ_apply_s`.
  - Cgroup CPU/I/O metrics are reported as run deltas in the latest artifact: `resource.cgroup.cpu_usage_usec.source=run_delta`, `latest=27679393.0`; `resource.cgroup.io_wbytes.source=run_delta`, `latest=109191168.0`.
- Next iteration entry point:
  - Re-run the exact plan verification command once the concurrent project-build live pytest is no longer occupying the shared live environment, then close out only if the command itself exits green and the new artifact repeats the O(1), latency, and forbidden-signature results.
