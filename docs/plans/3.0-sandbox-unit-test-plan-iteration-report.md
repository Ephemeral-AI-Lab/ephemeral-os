# 3.0 Sandbox Unit Test Plan Iteration Report

## Iteration 1 - 2026-05-24 18:17:26 CST

- Checkout summary: `main...origin/main [ahead 14]`; worktree was already dirty with plugin/tool intent-related edits before this iteration. Additional concurrent/untracked `backend/tests/contracts/` files appeared during the run and were not edited in this iteration.
- Plan path: `docs/plans/3.0-sandbox-unit-test-plan.md`.
- Target files: `backend/tests/unit_test/test_sandbox/test_overlay/`, `backend/tests/unit_test/test_sandbox/test_execution/test_overlay/`, `backend/tests/unit_test/test_sandbox/test_ephemeral_pipeline_unified_lifecycle.py`, `backend/tests/unit_test/test_sandbox/test_isolated_pipeline_unified_lifecycle.py`, `backend/tests/unit_test/test_sandbox/test_isolated_workspace_no_publish.py`, `backend/tests/unit_test/test_sandbox/test_inflight_invocation_registry.py`, `backend/tests/unit_test/test_sandbox/test_plugin_intent_dispatch.py`, `backend/tests/unit_test/test_plugins/test_lsp_session_overlay_refresh.py`, and existing background/plugin/LSP unit files named by the plan.
- Dependency pivot: none. This was the 3.0 unit/static plan; no later live E2E dependency was entered.
- Coverage gaps found: planned 3.0 test names only existed in the plan; missing unit/static coverage for overlay-handle release, kernel unmount modes, cancellation cleanup, source-tag conversion, ephemeral lifecycle order/conflicts/out-of-workspace routing, isolated lifecycle kinds/no-publish/concurrency/no-freeze, background wire cancel/reaper contracts, plugin intent dispatch, and LSP pipeline subscription/remount-failure behavior.
- Tests/probes/audit fields added: added the planned unit/static tests listed above. No audit fields were added because the 3.0 plan uses pytest output and contract greps as evidence, not live audit artifacts.
- Commands run:
  - `uv run pytest --collect-only -q ...` on touched unit surfaces: 69 collected, pass.
  - Focused changed-slice pytest with `-x --tb=short --durations=20`: first run failed, second run passed with 43 passed.
  - Plan verification command: `uv run pytest -q backend/tests/unit_test/test_sandbox/test_overlay/ backend/tests/unit_test/test_sandbox/test_execution/test_overlay/ backend/tests/unit_test/test_sandbox/test_api/test_shell_background_dispatch.py backend/tests/unit_test/test_sandbox/test_static_background_lifecycle.py backend/tests/unit_test/test_sandbox/test_plugin_overlay_dispatch.py backend/tests/unit_test/test_sandbox/test_plugin_session.py backend/tests/unit_test/test_sandbox/test_plugin_projection.py backend/tests/unit_test/test_plugins/`: final fresh result 119 passed, 1 skipped.
  - Contract greps:
    - `rg -n "ShellJob|shell_launch|shell_reap|shell_poll|shell_cancel|_background_jobs|_session_jobs" backend/src`: no matches.
    - `rg -n "getattr\\(.*event_bus|\\.event_bus" backend/src/plugins backend/src/sandbox/ephemeral_workspace/plugin`: no matches.
    - `rg -n "class OperationOverlayHandle|class OverlayProjectionHandle" backend/src`: no matches.
  - `uv run python -m py_compile backend/src/sandbox/isolated_workspace/helper/lifecycle.py backend/src/sandbox/isolated_workspace/helper/quota.py`: pass.
  - `uv run ruff check ...` on touched runtime/test files: first run found one unused import in a new test; after cleanup, pass.
- Fresh artifacts inspected: pytest/ruff command output only. No `.sweevo_runs/scenario_logs/` directory was inspected because the plan has no live SWE-EVO command.
- First failure/stop signal: focused pytest failed in `test_namespace_runner_cancel_kills_child_and_discards_upperdir` because `EphemeralPipeline.__init__` reads `overlay_writable_root()` from its own imported helper before `overlay_lifecycle.create`.
- Root-cause hypothesis and evidence: the test patched only `sandbox.overlay.lifecycle.overlay_writable_root`; stack trace showed the failure came from `backend/src/sandbox/ephemeral_workspace/pipeline.py:82`. Fix: patch both imported writable-root helpers in affected tests. A second assertion expected the parent `runtime/overlay` directory to be removed; evidence showed only per-call run dirs are cleaned, so the assertion was narrowed to no leaked children.
- Code/test changes made: added the missing 3.0 contract tests; changed isolated lifecycle production error kinds to `already_open`, `not_open`, and `host_ram_pressure` to match the plan.
- Correctness result: pass for the plan's pytest gate and contract greps.
- Performance result: not exercised. This plan is unit/static only; no live lowerdir disk, upperdir growth, run-dir cleanup under live load, FD/memory monotonicity, or per-tool p50/p95/max latency artifacts were produced. Cleanup/resource contracts are covered only by unit assertions.
- Next iteration entry point: proceed to 3.1 layer stack/OCC/overlay live plan only after accepting the 3.0 lifecycle error-kind contract change against any downstream scenario expectations.
