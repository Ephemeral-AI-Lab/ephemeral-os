/goal Implement **Phase 3, Stage 1** of `sandbox-e2e-live-test` — the `eos-e2e` orchestrator, reproducible artifacts, run-scoped cleanup — build-to-green. Implement code; do not redesign. Live code wins on conflict.

## Source of truth
Read **`docs/e2e/sandbox-e2e-live-test-phase-3-spec.md`** (schemas + ledger), `CLAUDE.md`, `README.md`. Build on the live skeleton; `eos-e2e.rs` is a stub, `config.rs` holds only the test-side reader, `report.rs` only `write_exchange`, no `cleanup.rs`.

## Deliverables
1. **`config.rs`** — rename test-side `RunConfig`→`ManifestConfig` (keep `from_env`/`Manifest`/`SUPPORTED_SCHEMA_VERSION=1`; fix consumer `fixtures.rs:7,33`). Add orchestrator `RunConfig`+clap `Args`+enums `TestSelection`/`CleanupPolicy`/`BuildSource` (variants in spec); precedence flag>env>default. `run_id`=`--run-id` verbatim (validate `[A-Za-z0-9._-]`, no `:`) else `r{ts}-{sha256(HEAD‖test_manifest_hash‖salt)[..8]}`; `ts`=manual `format!` of `OffsetDateTime::now_utc()` fields (no `time` fmt feature; see `transcript.rs:42-53`), pin `EOS_E2E_RUN_CLOCK`. No `uuid`.
2. **`report.rs`** (keep `write_exchange`) — add `result.json` (`TestOutcome`); `run-manifest.json` superset (stays readable by `ManifestConfig` — `schema_version=1`, reader tolerates extras); `summary.json` with `timing{build,runner,per_test}`+`cleanup` (all artifacts carry `schema_version`). Build `tests[]` by globbing `reports/*/result.json` (missing⇒`errored`); pass/fail GATE=`cargo test` exit code, NEVER libtest stdout. Omit observability timing (Phase 4).
3. **`fixtures.rs`** — emit `result.json` in `Sandbox::drop`: `status` from `thread::panicking()`, `duration_ms` from an `Instant` set at provision, `test_name` from `thread::current().name()` (fallback slug), `assertions` from a thread-local in `assertion.rs`. Skip path = bare `return`, writes nothing.
4. **`cleanup.rs` (NEW)** — RAII `RunGuard`; survivor sweep keyed on `reports/*/` dir names → `destroy_sandbox` each (idempotent); gateway DETACH only; `remove_dir_all(run_root)` gated by `CleanupPolicy` (default `OnSuccess`; `--keep-artifacts`=keep). Write `summary.cleanup`. `--clean-run {run_id}` repeats teardown. `pub mod cleanup;` in `lib.rs`.
5. **`eos-e2e.rs`** — replace stub: parse `RunConfig`; **preflight** (also `eos-e2e preflight`): Linux → `docker version` → `docker image inspect {image}` → probe=`create_sandbox` on `--gateway-socket` with a scratch temp workspace-root (only create/destroy reach the runtime trait): "runtime is not configured"⇒exit 2, success⇒destroy id, else exit 2. Then write `run-manifest.json`; Phase A skipped (attach-only); Phase B=`await_ready`, export `EOS_E2E_RUN_ROOT`, run `cargo test`; aggregate→`summary.json`; cleanup; exit 0 iff `cargo test`==0 AND all `result.json` passed.

## Pinned & fences
**Single stage-aware line**: `const STAGE1_DEFAULT_TARGET:&[&str]=&["--test","manager"]` — sole Stage 2 touchpoint; no runtime-readiness skip guard (sole skip=`EOS_E2E_RUN_ROOT` unset). `max_parallel`=`--max-parallel`>`EOS_E2E_MAX_PARALLEL`>`available_parallelism().min(8)`→`--test-threads=N`. Add `clap`,`sha2`,`time` only (sync — NO tokio/uuid). Black-box only: drive `sandbox-cli`, read only `{run_root}`; no internal-crate dep / `*_for_test` / injected runtime. Attach-only; no spawn/label-reaper. OUT: Phase 4 observability, Stage 2 runtime leaves R2-R8/N2, `build.rs`/`tests/*` edits.

## Conventions & acceptance
SRP, prefer less; no inline comments / no test code in `src/`; workspace deps via `dep.workspace=true`; clippy clean. Acceptance: `cargo build`+`clippy --all-targets` exit 0; bare `cargo test` skips clean; `eos-e2e preflight` exits 2 off-Linux/no-Docker/no-image/unconfigured-gateway; a green attach run (real-runtime gateway via `--gateway-socket`) writes `{run_root}/{run-manifest.json,summary.json,reports/{id}/{exchange.jsonl,result.json}}`, cleans up per policy; `--keep-artifacts`+`--rerun-failed-from` behave. Report changes + any spec-vs-live conflict.
