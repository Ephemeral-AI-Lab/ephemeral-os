/goal Implement **Phase 3, Stage 1** of `sandbox-e2e-live-test` — the `eos-e2e` orchestrator, reproducible artifacts, run-scoped cleanup — build-to-green. Implement code; do not redesign. Live code wins on conflict.

## Source of truth
Read **`docs/e2e/sandbox-e2e-live-test-phase-3-spec.md`** (pipeline, config schema, field-by-field artifact schemas, cleanup, anchor ledger) + `CLAUDE.md` + `README.md`. Build on the live skeleton; today `eos-e2e.rs:1-9` is a stub, `config.rs` holds only the test-side reader, `report.rs` only `write_exchange`, no `cleanup.rs`.

## Deliverables
1. **`config.rs`** — rename test-side `RunConfig`→`ManifestConfig` (keep `from_env`/`Manifest`/`SUPPORTED_SCHEMA_VERSION=1`; fix consumer `fixtures.rs:7,33`). Add orchestrator `RunConfig`+clap `Args`+enums `TestSelection{All,Names(Vec<String>),RerunFailedFrom(PathBuf)}`,`CleanupPolicy{Always,OnSuccess,Never}`,`BuildSource{Cargo{profile},Prebuilt(PathBuf)}`; flag>env>default. `run_id`=`--run-id` verbatim (validate `[A-Za-z0-9._-]`, no `:`) else `r{ts}-{sha256(HEAD‖test_manifest_hash‖EOS_E2E_RUN_SALT)[..8]}`; `ts`=manual colon-free `format!` over `OffsetDateTime::now_utc()` fields (workspace `time` has no formatting feature — precedent `crates/sandbox-runtime/command/src/transcript.rs:42-53`), pin via `EOS_E2E_RUN_CLOCK`. No `uuid`.
2. **`report.rs`** — add (keep `write_exchange`): `result.json` (`TestOutcome{schema_version,test_name,sandbox_id,status,assertions{total,failed},duration_ms,failure?}`); `run-manifest.json` superset `{schema_version=1,git_head,config,gateway_socket,run_id,image,clock}` (MUST stay readable by `ManifestConfig`; reader tolerates extras); `summary.json` `{…,status,counts,tests[],failed_tests[],artifacts_root,timing{build,runner,per_test},cleanup}`. Build `tests[]` by globbing `reports/*/result.json` (missing⇒`errored`); pass/fail GATE=`cargo test` exit code, NEVER libtest stdout. Omit observability timing (Phase 4).
3. **`fixtures.rs`** — emit `result.json` in `Sandbox::drop`: `status` from `thread::panicking()`, `duration_ms` from an `Instant` set at provision, `test_name` from `thread::current().name()` (fallback slug), `assertions` from a thread-local in `assertion.rs`. Skip path = bare `return`, writes nothing.
4. **`cleanup.rs` (NEW)** — RAII `RunGuard`; survivor sweep keyed on `reports/*/` dir names → `destroy_sandbox` each (idempotent); gateway DETACH only; `remove_dir_all(run_root)` gated by `CleanupPolicy` (default `OnSuccess`; `--keep-artifacts`=keep). Write `summary.cleanup`. `--clean-run {run_id}` repeats teardown. `pub mod cleanup;` in `lib.rs`.
5. **`eos-e2e.rs`** — replace stub: parse `RunConfig`; **preflight** (also `eos-e2e preflight`): Linux → `docker version` → `docker image inspect {image}` → probe=`sandbox-cli manager create_sandbox` on `--gateway-socket` with a scratch temp workspace-root (`std::env::temp_dir()`): "runtime is not configured"⇒exit 2; success⇒destroy id; else exit 2. Write `run-manifest.json`; Phase A skipped (attach-only⇒`timing.build.*_ms=0`); Phase B=`gateway::await_ready`, export `EOS_E2E_RUN_ROOT`, run `cargo test`; aggregate→`summary.json`; cleanup; exit 0 iff `cargo test`==0 AND every `result.json` passed.

## Pinned
**Single stage-aware line**: `const STAGE1_DEFAULT_TARGET:&[&str]=&["--test","manager"]` — sole Stage 2 touchpoint; no runtime-readiness skip guard (sole skip=`EOS_E2E_RUN_ROOT` unset). `max_parallel`: `--max-parallel`>`EOS_E2E_MAX_PARALLEL`>`available_parallelism().min(8)`→`-- --test-threads=N`. `Cargo.toml`: add `clap`,`sha2`,`time` only (sync — NO tokio/uuid).

## Fences
Black-box only: drive `sandbox-cli`, read only `{run_root}` artifacts; no internal-crate dep, no `*_for_test`, no injected runtime. Cleanup destroys only captured ids + `remove_dir_all(run_root)` — provably this-run-only. Attach-only (no spawn, no label reaper). OUT: Phase 4 observability, Stage 2 runtime leaves R2-R8/N2, `build.rs`/`tests/*` edits.

## Conventions & acceptance
SRP, prefer less; no inline comments in `src/`; no test code in `src/`; workspace deps via `dep.workspace=true`; clippy clean. Acceptance: (1) `cargo build`+`cargo clippy -p sandbox-e2e-live-test --all-targets` exit 0; (2) bare `cargo test -p sandbox-e2e-live-test` skips clean; (3) `eos-e2e preflight` exits 2 off-Linux/no-Docker/no-image/unconfigured-gateway with the exact item; (4) green attach run (external real-runtime gateway via `--gateway-socket`) produces `{run_root}/{run-manifest.json,summary.json,reports/{id}/{exchange.jsonl,result.json}}`, cleans up per policy; `--keep-artifacts`+`--rerun-failed-from` behave. Report changes, run output, any spec-vs-live conflict.
