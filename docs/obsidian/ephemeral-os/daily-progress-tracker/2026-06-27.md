---
title: 2026-06-27
date: 2026-06-27
tags:
  - ephemeral-os
  - daily-progress-tracker
status: draft
---

# 2026-06-27

## Task Tracker

### Main Tasks

- [ ] Live e2e test infra #task/main
- [x] Re-organize `prd.config`, and the `/eos` path under it #task/main
- [ ] Optimize observability trace, log, and perf #task/main
- [ ] Observability rework: collapse to one dependency-light NDJSON leaf crate (drop SQLite) — spec: `docs/observability-rework/README.md` #task/main
- [ ] After e2e test infra is ready, verify at least the command tools are working #task/main
- [x] Justify naming convention: `host-compatible` vs `isolated` #task/main
- [ ] Justify naming convention:`runtime` vs `manager` #task/main
- [x] Create doc for `cli`, `gateway`, `runtime`, and `manager` connection #task/main
- [ ] Create doc for `sandbox-runtime`, namespace, overlay, mount, and OCC #task/main
- [ ] Implement `network_profile` arg refactor on `create_workspace_session` (rename `--profile`/`profile` → `--network-profile`/`network_profile`) — spec: `docs/network-profile-refactor/README.md`; recommended scope A (wire-only) #task/main
- [ ] Add cancel command operation #task/main
- [ ] Create a doc for all command operations #task/main

### Subtasks

#### Re-organize `prd.config`, and the `/eos` path under it

- [x] Remove unused/default `prd.yml` config fields #task/subtask
- [x] Revisit and simplify `/eos` path layout #task/subtask

#### Observability Rework

- [x] Write specs: main + layerstack + CLI (`docs/observability-rework/{README,layerstack-observability,cli-observability}.md`) #task/subtask
- [ ] Phase 1 — crate rework: one-record `span` (`ts`+`dur_ms`), `Sink` (`O_APPEND`, one write/line), `Observer`/`SpanGuard`/`AsyncSpan`/`TraceContext`, `Reader` + views, `paths.rs` retarget, move `collect/{cgroup,disk}` #task/subtask
- [ ] Phase 2 — daemon swap: build `Observer`; `NoopObserver`→`ObsExecutionObserver`; `collect()`→`obs.sample`; serve snapshot from live `observability_snapshot()`; generalize `get_observability` op + CLI #task/subtask
- [ ] Phase 3 — replace ~56 in-sandbox `timing::` sites with span guards/events; thread trace id = `Request.request_id` (thread-local at dispatch + capture into watcher/finalize so Case A tail correlates) #task/subtask
- [ ] Phase 4 — layerstack events: `lease.acquired`/`layerstack.publish`/`publish_rejected`/`lease.released` + squash root span (fresh trace id) #task/subtask
- [ ] Phase 5 — removal checklist: 5 in-sandbox `timing.rs`, `runtime_timing_env`, `rusqlite`/`store/**` → scoped greps to zero; repoint boundary test #task/subtask
- [ ] Layerstack observability: `collect/layerstack.rs` reader (manifest + per-layer bytes, sidecar cache) + leased/booked-by/needed/squashable in snapshot + cgroup `io.stat` — spec: `docs/observability-rework/layerstack-observability.md` #task/subtask
- [ ] CLI observability family: `snapshot`/`trace`/`events`/`cgroup`/`layerstack`/`raw` subcommands (`--sandbox-id`) + help catalog — spec: `docs/observability-rework/cli-observability.md` #task/subtask
- [ ] Phase B (follow-up): thread `trace`/`parent` through `NamespaceRunnerRequest` for the cross-process tree (forked namespace-process); autosquash root span #task/subtask

#### Network Profile Refactor

- [ ] Refactor misleading `profile` vocabulary in `sandbox-runtime-workspace/src` → manager/mounted-workspace naming — spec: `docs/workspace-session-rename/README.md`; approved Tier A + B #task/subtask
- [x] Write rename migration spec (resolved: both tiers, `MountedWorkspace`, `open`/`close`, `isolated_network_setup`, migrate obs DB schema) #task/subtask
- [ ] Types: module `profile/`→`session/`, `isolated_setup/`→`isolated_network_setup/`, `WorkspaceProfileManager`→`WorkspaceManager`, `WorkspaceProfileHandle`→`MountedWorkspace`, `WorkspaceProfileFds`→`HolderNsFds`, `WorkspaceProfileError`→`WorkspaceManagerError` #task/subtask
- [ ] Verbs: `enter_with_profile`→`open`, `exit`→`close`; `active_profile_id`→`active_session_id` + locals #task/subtask
- [ ] Tier B field: typed `profile`→`network` (handle/DTO); string surfaces→`network_profile` (coordinate with this item's Option C — wire/DTO/handle together) #task/subtask
- [ ] Bundled cleanups: drop dead `WorkspaceProfileError::kind()`, drop duplicate `record_create_phase_ms`, collapse `capture_changes` double-lookup #task/subtask
- [ ] Obs DB migration: append schema V9 `RENAME COLUMN profile TO network_profile` + record/row/read/write/JSON + `manager.json` key; bump `schema.rs` test `checksums.len()` 8→9 #task/subtask
- [ ] Update cross-crate call sites: `operation`/`daemon`/`observability` + workspace `tests/unit/{service,model}.rs` #task/subtask
- [ ] Verify: `cargo fmt` / `build` / `clippy --all-targets` / `cargo test` (incl. `-p sandbox-observability`) + v8→v9 DB upgrade check #task/subtask

## Artifact

### Create doc for `cli`, `gateway`, `runtime`, and `manager` connection

**Artifact:** [[docs/cli-gateway-manager-runtime|CLI Gateway Manager Runtime]]

**Session:** Created a user-facing Obsidian note for operators that explains how `bin/start-sandbox-gateway`, `bin/sandbox-cli`, `sandbox-gateway serve`, `SandboxManagerRouter`, Docker setup, `sandbox-daemon serve`, and `sandbox-runtime operations` fit together.

**Included:**

- A system map split into User Tools, Gateway Layer, Manager Layer, Docker Runtime Setup, and Sandbox Runtime.
- Smaller diagrams for gateway startup, CLI manager requests, sandbox creation/runtime setup, and runtime command forwarding.
- Minimal operator commands and path-only implementation references.

### Justify naming convention: `host-compatible` vs `isolated`

**Commit:** `10b70de64` — Rename WorkspaceProfile to NetworkProfile and HostCompatible to Shared

**Verdict:** The two profiles differ on exactly one axis — the network boundary.
Both keep a private overlay plus mount/pid/user namespace isolation; only the net
namespace differs. The old names were wrong on two counts:

- `WorkspaceProfile` / `HostCompatible` overclaimed a general "isolation" axis and
  read as "this is the host / localhost", when host-compatible is actually
  isolated in every dimension except network.
- The variants were not even lexically parallel: `host_compatible` framed on
  network sharing, `isolated` framed on general isolation.

**Resolution (shipped in the commit above):**

- Renamed the type `WorkspaceProfile` → `NetworkProfile`, scoping the whole
  selector to the network axis. This fixes the overclaim for free —
  `NetworkProfile::Isolated` now unambiguously means network-isolated.
- Renamed the variant `HostCompatible` → `Shared` (paired with `Isolated`) and
  changed the wire/CLI value `host_compatible` → `shared`, so AI/CLI readers stop
  inferring "the host". `Isolated` unchanged.
- Reworded the `create_workspace_session` / `exec_command` descriptions to state
  the workspace shares the host network namespace while staying isolated in
  mount/pid/user — explicitly "it is not the host".
- Confirmed network (profile) is orthogonal to lifecycle (one-shot vs session);
  the one-shot path simply pins `Shared`. Did not leak one-shot/session into the
  profile vocabulary.
- Removed the transitional `host_compatible` parse alias as unused legacy.

**Verification:** `cargo build` (no warnings), `cargo clippy --all-targets`, and
`cargo test` all clean for the changed crates. Net diff 120 +/127 −.
