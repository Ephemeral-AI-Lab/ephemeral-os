/goal Implement Phase 4 of the namespace-execution migration ("Mount family onto the engine") strictly per `docs/namespace_execution_migration/phase-4-spec.md`; it already made every hard decision — follow it, don't re-design. Touch ONLY crates `workspace` and `daemon`. Obey `CLAUDE.md`: no inline comments in prod code (`///` only), no inline `#[cfg(test)]` in sources (tests in `tests/` suites), daemon sources stay `#[cfg]`-free.

Edits (spec §5):
- `workspace/Cargo.toml`: add dep `sandbox-runtime-namespace-execution` (default features). [R1]
- `workspace/src/model.rs`: `impl From<WorkspaceEntry> for NamespaceTarget` reusing existing `From<WorkspaceEntryFds> for NsFds`; `upperdir/workdir → Some`. [R2]
- `workspace/src/namespace/mod.rs`: give `NamespaceRuntime` a workspace-local `Arc<NamespaceExecutionEngine>` field built with a no-op observer; `new()`→`new(setup_timeout_s)`; drop `#[derive(Default)]`. [R8]
- `workspace/src/profile/manager.rs`: `WorkspaceModeManager::new` passes `caps.setup_timeout_s` into `NamespaceRuntime::new` (its own 3-arg signature unchanged). [R8/R12]
- `workspace/src/namespace/setns_runner.rs`: rewrite `mount_overlay`/`remount_overlay` to `engine.run_mount(flag, target, id, args, parse).wait()` — mount: `"--mount-overlay"`, `json!({})`, `|_| Ok(())`; remount: `"--remount-overlay"`, probe args, `|o| Ok(RemountOverlayResult::from_payload(o.payload()))`. `target = NamespaceTarget::from(WorkspaceHandle::from(handle).entry().map_err(setup_error)?)`; `id = engine.allocate_id()`. DELETE `run_child`, `wait_for_child`, `terminate_child`, `read_pipe`, `ns_runner_request` (method+free fn), `mount_overlay_child`, `remount_overlay_child`, and the `isolated-{mode}-{id}` id. Drop the `setup_timeout_s` param from both methods; update callers `create.rs:45`, `transaction.rs:53`, and the non-linux stubs. Leave `signal_net_ready` untouched. [R3-R7,R12]
- `daemon/src/runner.rs`: `MountOverlay` failure → `RunResult{exit_code:1, payload:{"error":…}}` via a `pub(crate) mount_overlay_result` helper (stop `?`-propagating; success/`ok_result()` unchanged); rename `dispatch_runner_mode` param `mode`→`operation`. Do NOT touch `--start-ack-fd`/`wait_for_start_ack` (Phase 6). [R9/R14/R18]
- Tests: delete `workspace/tests/setns_runner.rs`; add daemon D1 (`mount_overlay_result` Ok⇒exit0, Err⇒exit1+error) in `tests/unit/runner.rs`; add workspace tests per §9.

Non-negotiable (§3.3/§3.1): mount failure ⇒ `wait()` `Err`; remount verification-failure stays `Ok(mount_verified=false)` and caller `apply_remount` errors on the flag; mount/remount NEVER appear in `active_namespace_executions` (no-op observer, no `begin`); engine crate keeps zero `workspace` dep.

Verify the Phase-2 API FIRST (§2/§12); if any differ, STOP and report, don't invent: `run_mount` w/ the `args` param (§12-D), `allocate_id`, `ExecutionHandle::wait`, `RunnerOutcome::payload`, the exit-code short-circuit (§12-A), engine ctor + no-op observer (§12-B), piped setup-timeout w/ SIGTERM→SIGKILL (§12-G), API un-gated from `test-support` (§12-E).

Verify: `cargo fmt --check`; build+test `-p sandbox-runtime-workspace`; `cargo build -p sandbox-daemon && cargo test -p sandbox-daemon --test unit`; `cargo test -p sandbox-runtime observability`; clippy `-D warnings` on both crates; `cargo test -p xtask --test cfg_policy`; greps `fn run_child|fn ns_runner_request|fn wait_for_child|fn terminate_child|fn read_pipe` and `isolated-` return nothing. Don't commit or push.
