# Workspace Migration Implementation Record

Date: 2026-06-17
Repo: `/Users/yifanxu/machine_learning/LoVC/ephemeral-ai/ephemeral-os`
Scope: Unified workspace refactor Phase 1 public model scaffold, Phase 2
workspace root resolution, and Phase 3 host lifecycle ownership.

## Phase Status

Phase 1 is done. Phase 2 is done. Phase 3 is done.

The Phase 1 scaffold adds the unified public workspace vocabulary beside the
existing isolated-workspace exports.

The Phase 2 slice makes `sandbox.isolation.enter` and the test compact-remount
path accept caller-facing `workspace_root`, resolves that root to the
LayerStack workspace binding internally, and keeps legacy `layer_stack_root`
accepted through explicit compatibility adapters. It does not implement the
future create/run/capture/destroy lifecycle APIs.

Phase 2 adversarial review follow-up tightened the root-resolution contract:
discovered bindings must live at their declared `layer_stack_root`, cached
runtime state now tracks `workspace_root` as part of its binding identity,
stateful resolution no longer bypasses ambiguity checks, malformed dual-root
payloads cannot silently fall back to legacy `layer_stack_root`, and
test-remount invalid root requests preserve `invalid_argument` error shaping.

The Phase 3 slice moves Host command workspace lifecycle ownership out of
hidden command start behavior and into `WorkspaceRuntime`. Host command routes
now ask the runtime to resolve the legacy `layer_stack_root` compatibility
binding, acquire the bounded base lease, create the `EphemeralWorkspace`, and
then hand a command context to `CommandOps`. `CommandOps` still owns process
spawning, wait/progress/stdin/cancel mechanics, registry state, transcripts,
capture, and finalization.

## Files Changed

- `crates/daemon/workspace/src/model.rs`
  - Added `WorkspaceId`, `CallerId`, `BaseRevision`, `NetworkMode`, and the new
    unified `WorkspaceHandle`.
  - Added `CreateWorkspaceRequest`, `RunCommandRequest`, `RunCommandResult`,
    `CaptureChangesRequest`, `CaptureChangesResult`,
    `DestroyWorkspaceRequest`, and `DestroyWorkspaceResult`.
  - Added workspace-local `CommandStatus`, `ChangedPathKind`,
    `ProtectedPathDropReason`, and `ProtectedPathDrop` variants to avoid a
    dependency cycle with `operation`.
  - Added conversions from the legacy isolated handle and
    `IsolatedWorkspaceBinding` into the new unified public handle.
  - Added focused tests for borrowed and owned isolated handle conversion,
    borrowed and owned isolated binding conversion, public DTO
    construction/derive behavior, and public DTO debug output not exposing
    internal storage/namespace fields.
- `crates/daemon/workspace/src/error.rs`
  - Added `WorkspaceError` with the Phase 1 spec shape.
  - Implemented `Display` and `std::error::Error` manually so
    `SnapshotAcquire { source: String }` keeps the spec field name without
    `thiserror` treating it as an error source.
- `crates/daemon/workspace/src/service.rs`
  - Added the `WorkspaceService` trait scaffold only.
- `crates/daemon/workspace/src/lib.rs`
  - Exposed the new `model`, `error`, and `service` modules.
  - Preserved required legacy exports: `EphemeralWorkspace`,
    `IsolatedManager`, and `IsolatedWorkspaceBinding`.
  - Preserved existing root `workspace::WorkspaceHandle` compatibility for the
    legacy isolated handle and exposed the new unified scaffold handle as
    `workspace::model::WorkspaceHandle` and `workspace::UnifiedWorkspaceHandle`.
- `crates/daemon/operation/src/isolation/contract.rs`
  - Added `WorkspaceRootInput` so isolation enter and compact-remount test
    inputs accept `workspace_root` while rejecting ambiguous dual-root input.
  - Preserved `layer_stack_root` parsing as legacy compatibility.
- `crates/daemon/core/src/runtime/workspace.rs`
  - Added `ResolvedWorkspaceRoot` and root-resolution helpers that discover
    LayerStack workspace bindings from caller-facing `workspace_root`.
  - Added explicit legacy `layer_stack_root` compatibility entrypoints for
    isolation enter and compact remount.
  - Bound isolated manager state from resolved workspace bindings instead of
    trusting caller-supplied storage roots directly.
- `crates/daemon/core/src/op_adapter/isolation.rs`
  - Routed `WorkspaceRootInput` through the new runtime resolution APIs.
  - Records `workspace_root` for new enter traces and labels legacy
    compatibility roots as `legacy_layer_stack_root`.
- `crates/daemon/core/tests/unit/workspace_runtime.rs`
  - Added runtime coverage for `workspace_root` resolution, legacy root
    compatibility, ambiguous bindings, rebinding, and normalized snapshot entry.
- `crates/daemon/core/tests/unit/isolated_workspace/service.rs`
  - Updated enter trace assertions for the new `workspace_root` trace detail.
- `crates/daemon/core/src/op_adapter/command.rs`
  - Routes legacy Host command execution through
    `WorkspaceRuntime::create_host_workspace_for_legacy_layer_stack_root_locked`
    before calling `CommandOps`.
  - Preserves adapter-owned route selection for Phase 3; central routing
    remains Phase 4 work.
- `crates/daemon/core/src/runtime/workspace.rs`
  - Added internal `LeasedBaseRevision` and `HostWorkspaceLifecycle`.
  - Added explicit Host workspace creation helpers.
  - Moved Host bounded snapshot acquisition into `WorkspaceRuntime`.
  - Releases the lease when Host workspace creation fails after acquisition.
- `crates/daemon/layerstack/src/service.rs`
  - Added exact-once `LeaseReleaseHandle` and `LeaseReleaseReport`.
  - `LeaseReleaseHandle::release` consumes the stored release state under a
    mutex before calling LayerStack release, so repeated cleanup attempts do not
    issue repeated storage release calls.
  - `LeaseReleaseHandle` also releases on last-owner drop to protect pre-start
    Host command rejection paths.
- `crates/daemon/operation/src/command/service.rs`
  - Added `HostCommandWorkspace` as the command-start handoff container.
  - Removed command-owned Host lease lifecycle types in favor of the LayerStack
    release handle.
- `crates/daemon/operation/src/command/service/exec.rs`
  - Host command start now consumes a runtime-created `HostCommandWorkspace`
    instead of acquiring the snapshot lease or creating `EphemeralWorkspace`
    internally.
  - Process spawning, runner request artifact writing, and registry activation
    remain in `CommandOps`.
- `crates/daemon/operation/src/command/service/lifecycle.rs`
  - Command finalization and force-discard cleanup release Host leases through
    the exact-once lease handle instead of directly calling LayerStack release.
- `crates/daemon/operation/src/command/registry.rs`
  - Tracks the Host lease release handle beside the existing ephemeral command
    process, snapshot, and workspace fields.
- `crates/daemon/operation/src/command/mod.rs`
  - Re-exported the Host command context needed by `daemon` runtime without
    widening the command facade with lease lifecycle types.
- `crates/daemon/workspace/src/ephemeral_workspace/workspace.rs`
  - Added `EphemeralWorkspace::create_runtime_overlay` so runtime code can
    preserve the legacy host scratch placement while owning creation.
- `crates/daemon/core/tests/unit/workspace_runtime.rs`
  - Added Host lifecycle coverage for create acquiring a leased base revision,
    create failure releasing the lease, and explicit release exactly once.
- `crates/daemon/operation/tests/command/service.rs`
  - Added focused exact-once, last-owner drop, pre-start rejection, and
    finalization coverage for the Host lease release handle.
- `crates/daemon/operation/tests/command/registry.rs`
  - Updated the ephemeral registry scaffold to carry the Host lease handle.
- `docs/daemon/unified-workspace-refactor_ROADMAP.md`
  - Marked Phase 1 tasks and exit criteria complete.
  - Marked Phase 2 root resolution done.
  - Marked Phase 3 host lifecycle ownership done.
  - Appended Phase 1, Phase 2, and Phase 3 evidence rows, including
    intermediate fixed failures, final passing gates, skipped gates, residual
    blockers, and the handle-collision decision.
- `docs/daemon/unified-workspace-refactor_SPEC.md`
  - Was already dirty before Phase 0 per the roadmap evidence log; kept as the
    current source spec for this implementation record.

## Legacy/New `WorkspaceHandle` Collision

The repo currently has downstream daemon/core and operation code importing
`workspace::WorkspaceHandle` as the legacy isolated handle. Phase 1's allowed
edit scope did not include those downstream call sites, so changing the root
export would have created unrelated migration churn.

Resolution for Phase 1:

- `workspace::WorkspaceHandle` remains the legacy isolated handle.
- `workspace::IsolatedWorkspaceHandle` is an explicit alias for the same legacy
  isolated handle.
- `workspace::model::WorkspaceHandle` is the new unified public scaffold type.
- `workspace::UnifiedWorkspaceHandle` is a root-level alias for the new unified
  public scaffold type.

This keeps existing code compiling while making the new public scaffold
available for later phases.

## Conversion Rules Implemented

Conversions from both the legacy isolated handle and
`IsolatedWorkspaceBinding` into the unified public handle use:

- `NetworkMode::Isolated`
- `BaseRevision.version` from `manifest_version`
- `BaseRevision.root_hash` from `manifest_root_hash`
- `BaseRevision.layer_count` from `layer_paths.len()`

The new public handle does not expose `layer_stack_root`, `upperdir`, `workdir`,
namespace FDs, holder PID, cgroup path, veth data, or DNS details.

## Host Lease Custody

Phase 3 adds a narrow Host lifecycle path without introducing the Phase 4
central route authority.

- `WorkspaceRuntime` acquires the bounded Host base revision and stores the
  internal `LeasedBaseRevision` fields: `lease_id`, `version`, `root_hash`, and
  `layer_paths`.
- `WorkspaceRuntime` creates the Host `EphemeralWorkspace` before command start
  and releases the lease immediately if workspace creation fails after
  acquisition.
- `CommandOps` receives a `HostCommandWorkspace` only after runtime lifecycle
  creation succeeds, then keeps responsibility for process spawn, wait,
  progress, stdin, cancel, transcripts, capture, and finalization.
- `layerstack::service::LeaseReleaseHandle` is an exact-once release handle
  shared with the active command record. It takes the stored release state
  before calling LayerStack release, so command finalization, force-discard
  cleanup, explicit release, and repeated cleanup attempts cannot issue
  duplicate release calls through the handle.
- `LeaseReleaseHandle` also releases on last-owner drop. This protects
  pre-start rejection paths where a runtime-created Host command workspace is
  dropped before `CommandOps` consumes the `ExecTarget`, including command
  validation and active-command admission failures.
- Host command execution still uses the existing ephemeral/FreshNs preparation
  path. Holder/setns-only Host execution remains deferred to Phase 7.
- Host command capture/publish behavior is unchanged. The explicit
  non-publishing `capture_changes` API remains deferred to Phase 5.

## Verification

Final closeout commands used
`CARGO_TARGET_DIR=/tmp/eos-unified-workspace-phase2-target`
where applicable.

| Command | Result | Notes |
|---|---|---|
| `cargo fmt` | Pass | Formatted Phase 2 runtime, adapter, contract, tests, and docs. |
| `cargo test -p workspace` | Pass | 24 passed, 0 failed; doc tests 0 passed, 0 failed. |
| `cargo test -p operation file` | Pass | 14 passed, 0 failed, 70 filtered out; checkpoint and contract test targets had 0 matching tests. |
| `cargo test -p daemon workspace_runtime` | Pass | 8 focused runtime tests passed. |
| `cargo test -p operation isolation` | Pass | 5 focused isolation contract tests passed. |
| `cargo test -p daemon enter_trace_events_include_holder_and_dns_configuration` | Pass | 1 focused adapter trace test passed. |
| `git diff --check` | Pass | No whitespace errors. |

Follow-up verification after adversarial review fixes used
`CARGO_TARGET_DIR=/tmp/eos-adversarial-phase2-target` where applicable.

| Command | Result | Notes |
|---|---|---|
| `cargo fmt` | Pass | Formatted runtime, contract, tests, and roadmap edits. |
| `cargo test -p operation isolation` | Pass | 9 focused isolation contract tests passed, including malformed dual-root rejection and compact-remount `workspace_root` parsing. |
| `cargo test -p daemon workspace_runtime` | Pass | 12 focused runtime tests passed, including copied-binding rejection, same-stack workspace-root rebinding, cached-state ambiguity rejection, and compact-remount `workspace_root` resolution. |
| `cargo test -p workspace` | Pass | 24 passed, 0 failed; doc tests 0 passed, 0 failed. |
| `cargo test -p operation file` | Pass | 14 passed, 0 failed, 74 filtered out; checkpoint and contract test targets had 0 matching tests. |
| `cargo test -p daemon --test phase2_read_paths isolated_workspace_lifecycle_ops_open_status_list_and_exit_when_enabled` | Pass | Dispatch lifecycle test now enters through `workspace_root`. |
| `git diff --check` | Pass | No whitespace errors. |

Phase 3 verification used
`CARGO_TARGET_DIR=/tmp/eos-unified-workspace-phase3-target` where applicable.

| Command | Result | Notes |
|---|---|---|
| `cargo fmt` | Pass | Formatted Phase 3 Rust sources. |
| `cargo test -p daemon workspace_runtime` | Pass | 15 focused runtime tests passed, including Host create, create failure release, explicit exact-once release, and Phase 2 root-resolution coverage. |
| `cargo test -p workspace` | Pass | 24 passed, 0 failed; doc tests 0 passed, 0 failed. |
| `cargo test -p operation command` | Pass | Command filter passed: 62 operation command unit tests plus filtered checkpoint/contract tests; includes exact-once Host lease release coverage. |
| `cargo test -p operation file` | Pass | 14 passed, 0 failed, 75 filtered out; checkpoint and contract test targets had 0 matching tests. |
| `cargo test -p operation finalize` | Pass | Optional finalization filter passed: 34 operation tests plus the filtered contract fixture. |
| `cargo test -p daemon command` | Pass | Optional daemon command filter passed: 15 command/dispatch tests. |
| `git diff --check` | Pass | No whitespace errors. |

Phase 3 adversarial review follow-up verification used
`CARGO_TARGET_DIR=/tmp/eos-host-lease-fix-target`.

| Command | Result | Notes |
|---|---|---|
| `cargo fmt` | Pass | Formatted the Host lease RAII remediation and regression tests. |
| `cargo test -p operation command` | Pass | 66 command tests plus filtered checkpoint/contract targets passed, including Host lease last-owner drop, empty-command validation rejection, active-command admission rejection, and command finalization release coverage. |
| `cargo test -p daemon workspace_runtime` | Pass | 15 focused runtime tests passed after the Host lease RAII fix. |

Phase 3 cleanup follow-up verification used
`CARGO_TARGET_DIR=/tmp/eos-host-lease-cleanup-target`.

| Command | Result | Notes |
|---|---|---|
| `cargo fmt` | Pass | Formatted the LayerStack lease-handle refactor. |
| `cargo test -p operation command` | Pass | 66 command tests plus filtered checkpoint/contract targets passed after moving Host lease custody into LayerStack service. |
| `cargo test -p daemon workspace_runtime` | Pass | 15 focused runtime tests passed after removing the dead runtime destroy shim. |

Cleanup pass:

- Removed the unused `WorkspaceRootInput::as_path` helper and its stale `Path`
  import.
- Removed the duplicate active-command guard from the private
  `enter_resolved_locked` path after both public enter paths began checking it
  under the same mode gate.
- Renamed the test-remount workspace error mapper from setup-specific wording to
  neutral isolated-error wording after it began preserving invalid-request
  errors.
- Kept explicit legacy `layer_stack_root` compatibility adapters because
  repo-wide references still route old isolation enter and test-remount callers
  through them.

Intermediate fixed issues:

- Initial `cargo test -p workspace` failed because `thiserror` interpreted the
  `SnapshotAcquire { source: String }` field as an error source. The fix kept
  the field shape and implemented `Display`/`Error` manually.
- A DTO leak assertion initially matched the approved
  `evicted_upperdir_bytes` metric. The assertion now checks internal debug
  field labels such as `upperdir:` instead of raw substrings.
- The daemon runtime test filter `workspace_runtime` initially compiled targets
  but ran zero tests. The Phase 2 runtime tests were renamed with a
  `workspace_runtime_` prefix so the required gate now runs 8 focused tests.
- macOS canonicalized `/var/...` workspace temp paths to `/private/var/...`;
  runtime assertions now compare against canonicalized workspace roots because
  the resolver normalizes roots.

## Skipped Gates And Known Baseline Blockers

Skipped by current slice scope:

- Live Docker/Linux E2E.
- `cargo run -p xtask -- package`.
- `cargo run -p xtask -- check-contract`.
- `cargo clippy -p daemon --all-targets --locked -- -D warnings`.
- Ops.json regeneration.

Known Phase 0 baseline blockers remain:

- `cargo run -p xtask -- check-contract` fails because
  `crates/daemon/operation/ops.json` is stale.
- `cargo clippy -p daemon --all-targets --locked -- -D warnings` fails on the
  pre-existing `clippy::double_must_use` warning in
  `crates/daemon/layerstack/src/lease_aware.rs:97`.

These blockers were not fixed because they do not directly block the Phase 1
public model scaffold, Phase 2 root-resolution slice, or Phase 3 Host lifecycle
ownership slice and are outside this slice's scope. Packaging, live E2E, and
ops.json regeneration were also skipped because the Phase 3 instructions
explicitly excluded them.
