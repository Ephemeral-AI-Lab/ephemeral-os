# VERIFY — Model provider + SSE + prompt/context assembly (agent-core)

Independent verifier: re-derived from source, trusting nothing. Python source = ground truth; `docs/architecture` + parity corpus = corroboration. Every verdict carries a bilateral file:line.

Headline corrections to the investigation:
- **D2 / D3 / D4 are FALSE ALARMS in the Python-parity sense.** `OpenAiClient` is a net-new generic OpenAI Responses client wired to `https://api.openai.com` (app_state.rs:504), NOT the Codex/ChatGPT client (`chatgpt.com/backend-api/codex/responses`, codex.py:56). It has **no Python ground truth**; comparing it field-by-field against `codex.py` compares two different clients targeting two different endpoints. Reclassified to investigator_overstated → fold into D8; they need real-OpenAI-Responses-API validation, not Codex parity.
- **D7 is real but MEDIUM is overstated → LOW.** The golden's seq `1,2,3` is hand-authored literals in `capture.py:130,138,145`, matching neither real Python (`1,1,1` — request.py:31 shares one seq across all three rows) nor real Rust (`0,0,0`). No parity test compares seq; no consumer keys on absolute seq. The pre- vs post-increment divergence is genuine but cosmetic. NEW finding: the golden's seq is not the "faithful recorder output" the README claims.
- **NEW investigator_missed (MEDIUM):** Python `build_provider_messages` / `sanitize_tool_sequence` (provider_history.py:20-175) drops orphaned `tool_use`/`tool_result` pairs and reduces stale background-task history on the provider-facing view. The Rust query loop has **no equivalent** — it passes raw `messages` straight into `LlmRequest` (loop_.rs:119 → request.rs:25). This is squarely in the area's "context assembly" scope and was not covered.

## Invariant verdict table

| # | Invariant | independent_status | severity | decisive bilateral anchor |
|---|---|---|---|---|
| 1 | Anthropic + OpenAI SSE streaming parity | confirmed_match (Anthropic) + investigator_overstated (OpenAI sub-claims D2/D3/D4) | low | Anthropic decode faithful: anthropic.rs:129-271 ↔ anthropic_native.py:234-296. OpenAI has no Python source: app_state.rs:504 (`api.openai.com`) ≠ codex.py:56 (`chatgpt.com/backend-api/codex/responses`) |
| 2 | tool_use / Reasoning / text block parsing parity | confirmed_match (parsing) + confirmed_disparity (D5 tag, D6 id) | medium | parse: anthropic.rs:203-244 ↔ message.py:177-201. D5 tag: message.rs:63-67 emits `reasoning` ↔ message.py:33 `thinking`. D6: anthropic.rs:218-227 / openai.rs:169-178 fail-fast ↔ message.py:189-190 mint `toolu_<uuid>` |
| 3 | Prompt assembly (system + runtime + context) parity | confirmed_disparity | high | runtime_prompt.rs:9-28 (rewritten text, no WARNING/wrapper) ↔ runtime_prompt.py:52-61; `build_runtime_system_prompt`+fast_mode ABSENT (0 hits for `fast_mode` in agent-core) ↔ runtime_prompt.py:24-29; provider-history sanitization ABSENT (0 hits) ↔ provider_history.py:20-175 |
| 4 | prompt_report golden parity | confirmed_match (shapes) + confirmed_disparity (D7 seq, LOW) | low | event shapes/fields match: prompt_report.rs:35-63 ↔ prompt_report_recorder.py:59-99. seq: prompt_report.rs:90-95 post-increment (first=0) ↔ prompt_report_recorder.py:33-35 pre-increment (first=1). Golden 1/2/3 is hand-authored: capture.py:130/138/145 |

Extra constants re-derived equal (bilateral):
- `DEFAULT_MAX_TOKENS=32768`: types.rs:112 ↔ types.py:45. MATCH.
- RetryConfig defaults: providers.rs:29-32 (`3 / 1.0 / 30.0 / {429,500,502,503,529}`) ↔ config/sections/providers.py:20-23 ↔ anthropic_native.py:67-69. MATCH (opened the Rust defaults directly; not relying on the investigation).
- Status→kind: error.rs:68-73 (401|403→Auth, 429→RateLimit, {500,502,503,529}→Server, else→Request) ↔ anthropic_native.py:301-317 + _categorize:60-61. MATCH (5xx-outside-set → Request mirrors Python `unknown`/RequestFailure).
- Backoff `min(base*2^attempt, max)`: retry.rs:111-112 ↔ anthropic_native.py:173. MATCH.
- Retry budget: Rust `max_retries=3` → 4 attempts total (retry.rs test:253-265) ↔ Python `range(MAX_RETRIES+1)`=4 (anthropic_native.py:144). MATCH.
- `[DONE]` skip: sse.rs:123 ↔ codex.py:341. MATCH (Anthropic never sends it; harmless).
- `parse_tool_args`→`{}` on malformed: sse.rs:204-211 ↔ anthropic_native.py:276-277 / codex.py:394-395. MATCH.
- tool_result wire omits metadata+is_terminal: anthropic.rs:318-328 / openai.rs:284-292 ↔ message.py:169-174. MATCH (tested anthropic.rs:528-554).
- system_notification wrap `<system-reminder>\n{text}\n</system-reminder>`: anthropic.rs:329-332 / openai.rs:268-273 ↔ message.py:156-159. MATCH byte-for-byte.
- MessageRole rejects `"system"`: message.rs:19-26 + test:175-184 ↔ message.py:90 + README §4. MATCH (structural enforcement of the anomaly fix).

## Disparity adjudication

- **D1 — termination prompt rewritten (HIGH): CONFIRMED.** Python (runtime_prompt.py:52-61) emits `<Termination Condition>` wrapper + the four literal WARNING/lifecycle lines + sorted `` - `name` `` rows over EVERY registered terminal name (factory.py:150-160 passes every `is_terminal_tool=True` name). Rust (runtime_prompt.rs:13-27) emits an unrelated string with no wrapper, no WARNING lines, a `: selection_guidance` suffix from `descriptor()` (terminal.rs:85-117) with no Python analog, `BTreeSet<ToolName>` ordering, and silently skips any terminal whose `TerminalTool::from_tool_name` returns `None` (terminal.rs:59-69, fixed 6-variant match). The strongest one-way-exit guardrail in the Python prompt is gone. Verdict: confirmed_disparity, severity HIGH. No migration doc found justifying the new text.

- **D2 — OpenAI `stop_reason` no default vs Codex `end_turn`: REFUTED (investigator_overstated).** Fact is correct (openai.rs:197-200 → `Option`, `None` when absent; codex.py:413 defaults `end_turn`). But `OpenaiClient` is not the Codex client (app_state.rs:504 → `api.openai.com`; codex.py:56 → `chatgpt.com/backend-api/codex/responses`). No Python parity target exists. Real OpenAI Responses does not emit `stop_reason` at all (it uses `status`/`incomplete_details`), so the fixture (`tests/fixtures/openai/full.sse`) carries a synthetic field. Reclassify: informational, fold into D8. Needs real-API validation, not Codex parity. (Note: `StopReason::EndTurn` exists, events.rs:58 — a future default is one line.)

- **D3 — OpenAI flat-tool/body fields ≠ Codex: REFUTED (investigator_overstated).** Facts correct (openai.rs:234-249 includes `max_output_tokens`, omits `store:false`/`parallel_tool_calls:true`; serialize_openai_tool:307-318 adds `output_schema`). But again `OpenaiClient` ≠ Codex. `output_schema` mapping is an intentional documented choice (types.rs:50 "OpenAI maps it"), tested openai.rs:411-433. Reclassify: informational, fold into D8.

- **D4 — OpenAI tool-buffer id keying drops Codex fallbacks: REFUTED (investigator_overstated).** Facts correct (openai.rs:147-156 keys by `item["id"]`/`call_id` strictly; codex.py:371-376 has `or` fallbacks + uuid). Not a parity target. Reclassify: informational, fold into D8. (Overlaps D6's fail-fast philosophy.)

- **D5 — neutral Reasoning serializes `reasoning` not `thinking` (MEDIUM): CONFIRMED.** message.rs:63-67 serializes `{"type":"reasoning"}` (alias `thinking` decode-only), tested round-trip message.rs:156-171 ↔ message.py:33 `Literal["thinking"]`. The frozen Python schema fixture says `thinking` (parity/schemas/thinking_block.schema.json). `schema_snapshots.rs:20-36` only insta-snapshots the committed fixture against itself — it never compares Rust `ContentBlock` serialization — so the rename is genuinely **uncaught** by parity tests, exactly as the investigator stated. Verdict: confirmed_disparity, MEDIUM. Documented GC-llm-client-01 rename; recommend adding a reasoning-block row to the golden so it is a reviewed diff.

- **D6 — empty/missing tool_use id fails stream (LOW): CONFIRMED.** anthropic.rs:218-227 + openai.rs:169-178 reject empty id with fatal `Decode` (the `ToolUseId` newtype forbids empty). Python mints `toolu_<uuid>` (message.py:189-190) and passes the mid-stream id through verbatim (anthropic_native.py:279). Low: providers always send ids. Verdict: confirmed_disparity, LOW (documented in code comment anthropic.rs:209-217).

- **D7 — next_seq off-by-one (MEDIUM→LOW): CONFIRMED, severity adjusted.** prompt_report.rs:90-95 returns current then increments (first=0); test asserts 0/0/0 (prompt_report.rs:248-250). prompt_report_recorder.py:33-35 pre-increments (first=1). Both share one seq per turn (request.rs:21 + loop_.rs:122/166/194 ↔ request.py:31 + record_assistant/tool_results reuse). Real disparity = 1-based vs 0-based. BUT the golden's `1,2,3` (capture.py:130/138/145) is hand-authored and matches NEITHER live implementation's real per-turn output, so the investigator's "Rust never reproduces frozen Python truth" overstates impact. No parity test compares seq; no consumer keys on it. Verdict: confirmed_disparity, severity LOW.

- **D8 — OAuth/coding-plan routing dropped (informational): CONFIRMED intentional.** auth.rs drops base-url heuristic + macOS OAuth (GC-llm-client-04); a dedicated test `no_legacy_surface.rs:13` forbids `coding_plan`/`class_path`/`keychain` symbols. provider selection is a stub (app_state.rs:491-509, comment "real provider selection is a cutover concern"). Confirmed intentional scope cut. D2/D3/D4 correctly belong here.

- **D9 — refresh-on-401 not ported (informational): CONFIRMED intentional.** retry.rs:94-97 treats Authentication as never-retryable; doc comment retry.rs:10-11 states the refresh-on-401 retry is dropped with the OAuth strategy. Consistent with D8. ↔ anthropic_native.py:156-166 / codex.py:299-321.

## New findings

- **NF1 (investigator_missed, MEDIUM) — provider-history sanitization absent in Rust.** Python `build_provider_messages` (provider_history.py:20-175) deep-copies the durable history and drops orphaned `tool_use`/`tool_result` blocks (`_drop_unmatched_tool_blocks_in_place`) plus reduces stale background-task snapshots (`reduce_background_task_history`) before the provider request. Anthropic rejects a `tool_use` without a matching `tool_result` and vice versa, so this is a correctness invariant, not cosmetics. The Rust loop has **zero** equivalent: `run_query` (loop_.rs:119) hands the raw append-only `messages` to `build_query_run_request`, which copies them verbatim into `LlmRequest` (request.rs:25-30). Severity MEDIUM with caveat: impact depends on whether the Rust loop can yet produce orphaned pairs (background-task history reduction may be out of the current Rust phase scope), but the orphan-block sanitization gap is a real assembly-parity hole the investigation did not flag.

- **NF2 (fixture-fidelity, LOW) — golden seq is not faithful recorder output.** `parity/prompt_report/session_golden.jsonl` seq `1,2,3` are hand-authored constants (capture.py:126-148), not produced by `PromptReportRecorder` semantics. The real recorder shares one seq across a turn's three rows (request.py:31), so a real turn-1 transcript is `1,1,1` (Python) / `0,0,0` (Rust), never `1,2,3`. README §22 calls this the "faithful recorder output," which is inaccurate for the seq field. Recommend regenerating the golden through the actual recorder or annotating the seq as illustrative.

- **NF3 (confirmation, none) — OpenAI message encoder richer than Codex, correctly so.** serialize_openai_message (openai.rs:256-303) handles role-based input_text/output_text, tool_use→function_call, tool_result→function_call_output, system_notification — vs Codex's text-only build_body (codex.py:244-255). Since `OpenaiClient` has no Python source this is a forward-looking generic encoder, not a divergence; needs real-Responses-API validation.

## Overall verdict

The investigation correctly identified D1 (HIGH), D5, D6, D7, D8, D9 and the matched constants — its source reading is accurate. Its **primary defect is a false-alarm cluster (D2/D3/D4)**: it audited a net-new generic OpenAI Responses client (`OpenaiClient`, `api.openai.com`) as if it were the Codex client (`codex.py`, `chatgpt.com`), producing three "confirmed disparities" that have no Python ground truth. Decisive anchor: app_state.rs:504 vs codex.py:56. It also **overstated D7 severity** (golden seq is hand-authored, not faithful) and **missed NF1** (provider-history orphan-block sanitization has no Rust port, a real context-assembly gap).

No FALSE MATCH found that breaks a confirmed-match invariant: the Anthropic encode/decode, error/retry/backoff constants, tool_result/system_notification wire shapes, and MessageRole system-rejection are all genuinely faithful (re-derived bilaterally, with tests). The most actionable genuine gaps are D1 (HIGH — guardrail text lost) and NF1 (MEDIUM — orphaned tool-block sanitization absent).

DONE model_provider_prompt: D1+D5+D6 confirmed_disparity, RetryConfig/DEFAULT_MAX_TOKENS/error-mapping/backoff/tool_result/system_notification/MessageRole confirmed_match; D2/D3/D4 refuted as false alarms (no Python source — OpenAiClient is generic, not Codex); D7 confirmed but LOW (golden seq is hand-authored); 1 investigator_missed = NF1 provider-history orphan-block sanitization absent in Rust.
