# Independent Verification — Isolated Workspace (Rust parity)

Area: isolated workspace (isolated network, persistent upperdir, never OCC-merged, teardown on exit)
Verifier stance: trust nothing; re-derived from primary source (Python = ground truth; in-tree Python host lifecycle is the live truth; `/tmp/oldpy` is the deleted control-plane reference).

## Source map opened
- Python (in-tree, live): `backend/src/sandbox/host/isolated_workspace_lifecycle.py`, `backend/src/tools/isolated_workspace/{enter,exit}_isolated_workspace/definition.py`, `backend/src/tools/_hooks/block_in_isolated_mode.py`, `backend/src/tools/ask_helper/ask_advisor/ask_advisor.py`
- Python (deleted control plane, daemon-authority reference): `/tmp/oldpy/.../isolated_workspace/{pipeline,network}.py`, `_control_plane/{types,workspace_handle_lifecycle,namespace_runtime,orphan_reaper}.py`, `daemon/rpc/dispatcher.py`
- Rust: `sandbox/crates/eos-isolated/src/{session,network,caps}.rs`, `eos-ns-holder/src/lib.rs`, `eos-daemon/src/{isolated,command,plugin/mod}.rs`, `agent-core/crates/eos-sandbox-host/src/isolated_workspace.rs`, `agent-core/crates/eos-tools/src/{meta,hooks,execution,model_tools/isolated}.rs`
- Docs: `docs/architecture/tools/isolated-workspace.html`, `docs/architecture/sandbox/workspaces.html`

## Invariant verdict table

| # | Invariant | Status | Decisive bilateral anchor |
|---|-----------|--------|---------------------------|
| 1 | enter/exit keyed on active `agent_id` handle (no public isolated_workspace_id route param) | confirmed_match | py `isolated_workspace_lifecycle.py:29` `agent_id = request.caller.agent_id`; in-tree tool inputs `enter/exit_isolated_workspace/definition.py` take only `grace_s`/`layer_stack_root`. rs `model_tools/isolated.rs:29-39,55-59,79-83` inputs are `layer_stack_root`/`grace_s` only, routes on `ctx.agent_id()`; `session.rs:320` `enter(&AgentId)`, `:435` `exit(&AgentId, …)`; `isolated.rs:547` `agent_has_active_handle(agent_id)`, `by_agent: HashMap<AgentId, WorkspaceHandleId>` (`session.rs:240`). |
| 2 | Isolated session gets its OWN network namespace | confirmed_match | py deleted-cp `namespace_runtime.py:84-96` `unshare --user --map-root-user --net --pid --mount --fork --kill-child --propagation private`. rs `eos-ns-holder/src/lib.rs:840-841` `unshare(NEWUSER|NEWNS|NEWPID|NEWNET)` + uid/gid map + MS_PRIVATE in-process (PORT anchor at `:825`); `isolated.rs:207` opens `/proc/<pid>/ns/net`; veth+bridge in `network.rs`. Consolidation of `unshare(1)`→in-process is an explicit documented migration, semantics preserved (NEWNET present). |
| 3 | Upperdir storage PERSISTENT across the session (no per-call create/destroy) | confirmed_match | py `pipeline.py:65-71` docstring + `:169-201` `run_tool_call` reuses persistent handle, no `overlay_lifecycle.acquire/destroy`; `workspace_handle_lifecycle.py:70-75` upper/work made once in enter. rs `session.rs:354-362` upper/work created once in `enter()`; `isolated.rs:604-611` `record_tool_call` only bumps `last_activity` (no remount); `command.rs:60-72` routes exec to the persisted `CommandHandle`. |
| 4 | Writes captured + audited but NEVER OCC-published; scratch discard-only | confirmed_match | py `pipeline.py:169-201` `Intent.WRITE_ALLOWED` captures `changed_paths` for audit only, drops at exit via rmtree; deleted-cp has no publish call. rs build-time guard real: `eos-isolated/Cargo.toml:8-14` lists deps with `# NOT eos-occ … must stay absent`, `lib.rs:6-16` no-publish guarantee, `grep eos-occ` finds only comments. `command.rs:1253-1259,1283` `"mutation_source":"isolated_workspace"`, `"published": false`; isolated file branch never calls OCC apply. |
| 5 | Exit tears down namespace + releases lease + removes scratch; changes discarded | confirmed_match | py `workspace_handle_lifecycle.py:234-355`: remove maps first (`:247-249`), then `_teardown` kill_holder→teardown_veth→close_fds→release_lease→cgroup rmdir→rmtree scratch. rs `session.rs:445-450` removes by_agent/handles first; `teardown_handle:928-1009` kill_holder→close_handle_fds→teardown_veth→`release_lease`→`remove_dir(cgroup)`→`remove_dir_all(scratch)`; `exit` returns `evicted_upperdir_bytes` and audits `scratch_removed`. |
| 6 | Enter REJECTS active sandbox-bound background work; exit CANCELS/drains it | confirmed_match | py in-tree `isolated_workspace_lifecycle.py:30-42` `in_flight = max(local_count, daemon_count); if in_flight > 0 → ephemeral_jobs_in_flight`; `:74-78` exit `_cancel_by_agent(grace_s)`. Also pre-hook `RequireNoInflightBackgroundTasks` on BOTH enter+exit (`enter/exit definition.py:34`). rs host `eos-sandbox-host/isolated_workspace.rs:50-70` same MAX gate + `command_session_count_unavailable` fail-closed; `:84-87` exit `cancel_by_agent`; pre-hook wired in `meta.rs:65-67` for `EnterIsolatedWorkspace|ExitIsolatedWorkspace`; daemon `isolated.rs:407-434` `op_exit` gates on `active_command_sessions` + force_cancel drain. |
| 7 | plugin / LSP operations BLOCKED while isolated mode active for that agent | confirmed_match | py deleted-cp `dispatcher.py:90-114,136-137,251-274` `_is_plugin_op(api.plugin.*/plugin.*)` + `iws.get_handle(agent_id)` → `forbidden_in_isolated_workspace`. rs `plugin/mod.rs:325-337` `ensure_plugin_family_allowed` → `agent_has_active_handle` → `PluginError::ForbiddenInIsolatedWorkspace`; called on op_ensure(`:121`), op_status(`:207`), op_invoke(`:253`), and `:1074,:1591`; test `mod.rs:2042` asserts `forbidden_in_isolated_workspace`. LSP in this codebase is plugin-routed (no separate LSP op family in either dispatcher), so the plugin-family gate covers it. |

Constants parity (independently extracted, byte-for-byte):
- TTL 1800.0 (`caps.rs:45,68` vs `types.py:166`); total_cap 5 (`caps.rs:47,69` vs `:167`); upperdir 1_073_741_824 = 1 GiB (`caps.rs:49,70` vs `:168-170`); memavail_fraction 0.5 (`caps.rs:51,73` vs `:171`); setup_timeout 30.0 (`caps.rs:53,72` vs `:172`); exit_grace 0.25 with `.max(0.0)` clamp (`caps.rs:55,74,103-104` vs `:173-176`); rfc1918 default allow (`caps.rs:74,105-110` vs `:177-179`); fallback_dns "1.1.1.1" (`caps.rs:59,75` vs `:180`); sample_interval 0.5 clamped `.max(0.01)` (`caps.rs:61,115-119` vs `:181-184`).
- Capacity gate strict `>`: rs `session.rs:1146` `required_bytes > budget_bytes`, `:1155-1159` `(open+1)*upperdir_bytes` saturating; py `pipeline.py:122-123` `(len+1)*upperdir_bytes` and `if required > budget`. MATCH.
- Network literals: BRIDGE_CIDR 10.244.0.0/24 (`network.rs:40` vs `network.py:28`), GATEWAY 10.244.0.1 (`network.rs:42` vs `:29`), pool .2–.254 (`network.rs:60-62,136-137` vs `:55-57`), IMDS 169.254.169.254 (`network.rs:50` + `nft_imds_drop_rule_exprs:560-566` vs `network.py:32,213`), VETH_PREFIX `eos-iws-` shared with HANDLE_PREFIX (`caps.rs:17`, `network.rs:55`), veth name `[:6]` + h/n + 15-char cap (`network.rs:82-90` vs `network.py:231-235`). MATCH.

## Disparity adjudication

- **D1 (medium/missing) Daemon exit drain / per-agent dispatch quiesce (`exit_pending`/`begin_exit_drain`/`acquire_dispatch_slot`) absent in Rust** → **REFUTED (overstated)**. Those symbols belong to the DELETED Python `_control_plane` (`workspace_handle_lifecycle.py:216-253`, `dispatcher.py:94 acquire_dispatch_slot`), an in-process daemon-thread quiesce model. The LIVE in-tree Python host lifecycle (`isolated_workspace_lifecycle.py`) has NO `begin_exit_drain`/`exit_pending` either — its exit does `_cancel_by_agent` then daemon RPC, which Rust mirrors exactly (`eos-sandbox-host/isolated_workspace.rs:84-87`). The daemon-side quiesce is re-expressed in Rust as the `active_command_sessions` gate + force_cancel drain loop (`isolated.rs:407-434`, busy-wait to `grace_s` deadline). This is the porting model's deliberate re-architecture, not a missing dynamic. Severity → none.

- **D2 (medium/divergent) exit drains unconditionally / `RequireNoInflightBackgroundTasks` prehook not wired on isolated tools** → **REFUTED (false alarm — primary catch)**. `meta.rs:65-67` wires `Hook::RequireNoInflightBackgroundTasks` on BOTH `EnterIsolatedWorkspace` AND `ExitIsolatedWorkspace`, exactly matching in-tree `enter/exit_isolated_workspace/definition.py:34`. The hook denies on local>0 (`hooks.rs:512-521`) and on daemon-count>0 / unavailable (`hooks.rs:546-557`). So exit does NOT drain unconditionally — it is refused at the tool layer when background work is in flight, then (if it passes) cancels/drains at the host layer. The investigator's "RequireNoInflightBackgroundTasks not found on enter/exit" is simply wrong (it is in meta.rs). The `BlockInIsolatedMode on exec_command` they cite (`execution.rs:293`) is a TEST FIXTURE; production wires `BlockInIsolatedMode` only on `AskAdvisor` (`meta.rs:85`), matching in-tree Python which wires it only on `ask_advisor.py`. Severity → none.

- **D3 (low/missing) Sampler loop (`isolated_workspace.sampled` cadence) not implemented** → **CONFIRMED (adjusted to low/none-runtime)**. Python `pipeline.py:246-294,339-364` starts `_sampler_loop` in `initialize()` emitting `isolated_workspace.sampled` every `sample_interval_s` (default 0.5s). Rust carries the cap (`caps.rs:61,115-119`) but `grep sampler|sampled|sample_interval` in `eos-isolated/src` + `eos-daemon/src` finds only the cap field — no emitter loop. Genuinely absent. Impact: a pure observability/audit-cadence lane (sample lane), not a correctness/isolation invariant; the cap is preserved so it can be wired later. Severity low.

- **D4 (low/divergent) TTL sweep cadence fixed 500ms vs `max(0.5, min(ttl_s/2, 30.0))`** → **CONFIRMED**. py `pipeline.py:264` `interval = max(0.5, min(ttl_s/2.0, 30.0))`. rs `server.rs:131-143` drives `crate::isolated::ttl_sweep` on a fixed `Duration::from_millis(500)` tick (not read in `session.rs`). The stale CONDITION matches (`session.rs:486-497` `now - last_activity > ttl_s` AND not active), only the tick cadence differs. With the default 1800s TTL, the only observable difference is the daemon waking every 0.5s instead of every 30s — a CPU-cost detail, not an eviction-semantics divergence. Severity low.

- **D5 (low/divergent) TTL 'active' guard uses command-session presence vs `active_calls` counter** → **CONFIRMED (adjusted)**. py `pipeline.py:295-303,366-396` skips stale when `h.active_calls == 0`, where `active_calls` is a per-handle foreground in-flight counter. rs `session.rs:486-497` skips agents in `active_agents` derived from `active_command_sessions` (`isolated.rs:559-569`); `WorkspaceHandle` (`session.rs:57-95`) has no `active_calls` field; `record_tool_call` only bumps `last_activity`. Different "active" definition. In the Rust daemon model the unit of foreground work IS a command session (registered on start, unregistered on finish — `command.rs:706,1172,1438`), so command-session presence is the faithful analogue of `active_calls > 0`. The guard's intent (don't evict a workspace with live foreground work) is preserved; the bookkeeping is re-expressed. Severity low.

- **E8 (low/missing) Orphan reaper does not `/proc`-scan for ns-holder processes lacking a persisted row** → **CONFIRMED**. Python `orphan_reaper.py:257-310,313-339` `_reap_orphan_holder_processes` + `_iter_namespace_holder_processes` walks `/proc/<pid>`, matches cmdline `_NS_HOLDER_MARKER="sandbox.isolated_workspace.scripts.ns_holder"`, and SIGTERM/SIGKILLs stale holder trees whose pid/ppid is not in the live set — i.e. it reaps holders even with NO persisted row. Rust `session.rs:626-644 reap_startup_orphans` only reaps persisted-row holders (`reap_persisted_holder:687-703`) plus named veth/cgroup/scratch sweeps (`:759-819`); there is NO `/proc` scan for marker-matched orphan holders. Adjusted nuance: in Rust the holder is an in-process `eosd ns-holder` child tracked in a `holder_children()` registry (`isolated.rs:77-80,187`) rather than an `unshare`-detached `ns_holder.py`, so the dominant cross-restart leak the Python scan targeted is less likely; but a daemon hard-crash mid-enter (handle never persisted) can still strand a holder the Rust startup path will not collect by /proc scan. Real gap, low severity.

## New findings (not in the investigator report)
- **N1 (informational, not a disparity).** Rust adds peer-isolation nft rules absent from the deleted Python `network.py`: `nft_peer_isolation_rule_exprs` (inet forward) and a bridge-family `eos_iws_bridge_filter` table (`network.rs:401-438,570-619`). This is a SECURITY HARDENING superset (workspace-to-workspace isolation) beyond Python's port-isolation-only (`bridge_slave isolated on`, `network.py:122-132`). It strengthens, not weakens, the isolated-network invariant — flagged only so the docs/parity ledger notes the deliberate add.
- **N2 (informational).** Rust `op_exit` exposes a `force_cancel` arg + structured `force_cancelled/stale/active_command_session_ids_after` annotations (`isolated.rs:397-449,668-691`) that the in-tree Python host path does not surface (Python refuses via the daemon error and relies on the host-side `_cancel_by_agent`). Behavior is a superset (default `force_cancel=false` refuses on active sessions, matching the "exit refuses while command sessions active" contract); no regression.
- **N3 (confirm the OCC guard is real, not cosmetic).** Beyond the comment, `eos-isolated/Cargo.toml` genuinely omits `eos-occ` from `[dependencies]`, and the crate's only layer-stack surface is the `LayerStackSnapshotPort` trait exposing `acquire_snapshot`/`release_lease`/`active_lease_count` ONLY (`session.rs:104-134`) — no publish method exists to call. The "never OCC-merged" invariant is enforced by the type system / dependency graph, the strongest possible form.

## Overall verdict
High-fidelity port. All 7 core isolation invariants (agent-id keyed lifecycle, own net namespace, persistent upperdir, capture-but-never-publish, full teardown-on-exit, enter-rejects/exit-cancels background work, plugin/LSP block) are independently confirmed as correctly implemented in Rust with byte-for-byte constants and the strict `>` capacity gate. The investigator's two MEDIUM disparities (D1, D2) are REFUTED: they describe the DELETED `_control_plane` drain machinery, not the live in-tree contract, and D2 is an outright false alarm — `RequireNoInflightBackgroundTasks` IS wired on both isolated tools in `meta.rs:65-67`. The three genuine gaps are all LOW and observability/cleanup-only: the `isolated_workspace.sampled` sampler loop (D3) is unimplemented (cap preserved), the TTL sweep cadence is a fixed 500ms (D4) and its "active" guard uses command-session presence instead of an `active_calls` counter (D5, faithful re-expression), and the startup orphan reaper lacks a `/proc` ns-holder scan (E8, partly mitigated by in-process holder tracking). None of these weaken the isolation, no-publish, or teardown guarantees. Net: the dangerous "FALSE MATCH" class is absent here; the dangerous direction was actually the opposite — the investigator over-reported two MEDIUM disparities that the Rust already satisfies.
